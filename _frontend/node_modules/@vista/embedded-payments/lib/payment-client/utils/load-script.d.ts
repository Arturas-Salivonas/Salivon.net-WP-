export declare const isScriptLoaded: (url: string) => boolean;
/**
 * Loads a script into the DOM asynchronously
 */
export declare const loadScript: (url: string) => Promise<{}>;
/**
 * Loads an array of scripts into the DOM
 */
export declare const loadScripts: (urls: string[]) => Promise<{}[]>;
