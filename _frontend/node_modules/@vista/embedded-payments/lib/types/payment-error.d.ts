import { PaymentErrorResultCode } from './payment-error-result-code';
/**
 * A custom error returned by the PaymentClient when a payment has failed.
 * Includes a resultCode that can be used to display error-appropriate UI.
 */
export declare class PaymentError extends Error {
    /**
     * The specific result code of the payment error.
     */
    resultCode: PaymentErrorResultCode;
    constructor(resultCode: PaymentErrorResultCode);
}
