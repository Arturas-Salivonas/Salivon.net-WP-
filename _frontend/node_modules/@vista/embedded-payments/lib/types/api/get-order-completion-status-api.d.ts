import { OrderCompletionStatus } from './order-completion-status';
/**
 * Confirms that the order has been completed after the embedded payment is successful.
 */
export declare type GetOrderCompletionStatusApi = (orderId: string) => Promise<OrderCompletionStatus>;
