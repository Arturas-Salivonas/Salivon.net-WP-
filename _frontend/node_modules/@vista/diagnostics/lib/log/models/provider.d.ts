import { Dictionary } from '../../types/dictionary';
import { ThresholdLevel } from './threshold';
export interface LogProvider {
    log: (configuration: Dictionary, debugByTagPool: string[], severity: ThresholdLevel, method: string, message: string, tags: string[], args: any[]) => void;
}
export interface LogProvidersList {
    [key: string]: LogProvider;
}
