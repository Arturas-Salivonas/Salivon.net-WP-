import { ConsoleConfiguration } from './../models/configuration';
import { LogProvider } from './../models/provider';
/**
 * Wrapper for browser console
 */
export declare class ConsoleLogProvider implements LogProvider {
    private _canStyleLogMessage;
    log(configuration: ConsoleConfiguration, debugByTagPool: string[], severity: number, method: string, message: string, tags: string[], args: any[]): void;
}
/**
 * Exports instance of ConsoleLogProvider
 * to use as the singleton reference for default provider in VistaDiagnosticsLog
 */
export declare const consoleLogProviderInstance: ConsoleLogProvider;
