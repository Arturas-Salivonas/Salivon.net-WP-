export interface ClientMetadata {
    sessionId: string;
    browser: {
        mobile: boolean;
        userAgent: string;
        resolution: string;
    };
}
export declare const STORAGE_KEY = "vista-diagnostics-client-metadata";
/**
 * Returns a metadata object containing sessionId and minimal browsers informations
 */
export declare function getClientMetadata(): ClientMetadata;
