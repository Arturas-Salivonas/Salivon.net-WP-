/**
 * Maps a showtime attribute from the MX API to a showtime attribute for widgets to use.
 * @hidden
 */
export function mapMxShowtimeAttributeToWidgetShowtimeAttribute(attribute) {
    return {
        id: attribute.id,
        shortName: attribute.shortName,
        shortNameTranslations: attribute.shortNameTranslations,
        description: attribute.description,
        descriptionTranslations: attribute.descriptionTranslations,
        displayPriority: attribute.displayPriority,
        isPromoted: attribute.isPromoted
    };
}
//# sourceMappingURL=showtime-attribute-mapper.js.map