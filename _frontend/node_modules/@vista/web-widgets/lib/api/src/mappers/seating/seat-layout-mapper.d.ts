import { MxSeatLayout } from '../../../../mx-api-client/src/mxt/types/seating/seat-layout';
import { AreaCategory as WidgetAreaCategory } from '../../../../types/seating/area-category';
import { Seat as WidgetSeat } from '../../../../types/seating/seat';
import { SeatLayout as WidgetSeatLayout } from '../../../../types/seating/seat-layout';
/**
 * Creates referential links between each seating property, i.e.
 * can navigate directly between a seat, its parent row, area, and
 * area category.
 *
 * Intended to be used after loading the JSON seating layout, which
 * has no circular dependencies.
 *
 * @export
 * @param {SeatLayout} seatLayout
 * @hidden
 */
export declare function mapMxSeatLayoutToWidgetSeatLayout(seatLayout: MxSeatLayout): WidgetSeatLayout;
/**
 * Returns an area category given a specific seat (if exists in the current layout)
 * @param {Seat} seat
 * @param {SeatLayout} seatLayout
 * @returns {AreaCategory | void}
 * @hidden
 */
export declare function getAreaCategoryForSeat(seat: WidgetSeat, seatLayout: WidgetSeatLayout): WidgetAreaCategory;
