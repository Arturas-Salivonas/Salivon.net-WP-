import { MxFilmCategory } from '../../../../mx-api-client/src/mxs/types/browsing/film-category';
import { FilmCategory } from '../../../../types/browsing/film-category';
/**
 * Maps an array of film categories from the MX API to an array of film categories for widgets to use.
 * @hidden
 */
export function mapMxFilmCategoriesToWidgetFilmCategories(categories) {
    if (!categories)
        return [];
    return categories.map(mapMxFilmCategoryToWidgetFilmCategory);
}
/**
 * Maps a film category from the MX API to a film category for widgets to use.
 * @hidden
 */
export function mapMxFilmCategoryToWidgetFilmCategory(category) {
    switch (category) {
        case MxFilmCategory.ComingSoon: return FilmCategory.ComingSoon;
        case MxFilmCategory.NowShowing: return FilmCategory.NowShowing;
    }
}
//# sourceMappingURL=film-category-mapper.js.map