/**
 * Maps an array of screening details from the MX API to an array of screening details for widgets to use.
 * @hidden
 */
export function mapMxScreeningDetailsToWidgetScreeningDetails(screeningDetails) {
    return screeningDetails.map(mapMxScreeningDetailToWidgetScreeningDetail);
}
/**
 * Maps a screening detail from the MX API to a screening detail for widgets to use.
 * @hidden
 */
export function mapMxScreeningDetailToWidgetScreeningDetail(screeningDetail) {
    return {
        filmId: screeningDetail.filmId,
        siteIds: screeningDetail.siteIds
    };
}
//# sourceMappingURL=screening-detail-mapper.js.map