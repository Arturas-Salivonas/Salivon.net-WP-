import { MxVoucherStatus } from '../../../../../mx-api-client/src/mxt/types/vouchers/voucher-status';
import { mapMxVoucherToWidgetVoucher } from '../voucher-mapper';
import { VoucherStatus } from './../../../../../types/vouchers/voucher-status';
describe('mapMxVoucherToWidgetVoucher', function () {
    var voucherBarcode = 'barcode';
    var voucherPin = 'pin';
    var mxVoucherResponse = {
        name: 'voucher name',
        status: MxVoucherStatus.Valid,
        ticketRedemptions: {
            remainingRedemptions: 3,
            ticketTypes: [
                { ticketTypeId: '1' },
                { ticketTypeId: '2' }
            ]
        }
    };
    it('can map a MxVoucherResponse to a Voucher', function () {
        var result = mapMxVoucherToWidgetVoucher(mxVoucherResponse, voucherBarcode, voucherPin);
        expect(result).toEqual({
            name: 'voucher name',
            barcode: voucherBarcode,
            pin: voucherPin,
            status: VoucherStatus.Valid,
            ticketRedemptions: {
                remainingRedemptions: 3,
                ticketTypes: [
                    { ticketTypeId: '1', ticketTypeKey: 'voucher-ticket-type-1' },
                    { ticketTypeId: '2', ticketTypeKey: 'voucher-ticket-type-2' }
                ]
            }
        });
    });
});
//# sourceMappingURL=voucher-mapper.test.js.map