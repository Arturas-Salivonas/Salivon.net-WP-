import { mapMxSiteLocationToWidgetSiteLocation } from '../site-location-mapper';
import { mxSiteLocation, widgetSiteLocation } from './browsing-test-data';
describe('mapMxSiteLocationToWidgetSiteLocation', function () {
    it('maps a MxSiteLocation to a Widget SiteLocation', function () {
        var result = mapMxSiteLocationToWidgetSiteLocation(mxSiteLocation);
        expect(result).toEqual(widgetSiteLocation);
    });
});
//# sourceMappingURL=site-location-mapper.test.js.map