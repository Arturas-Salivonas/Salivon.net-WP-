import { mapMxVoucherStatusToWidgetVoucherStatus } from './voucher-status-mapper';
export function mapMxVoucherToWidgetVoucher(mxVoucherResponse, barcode, pin) {
    return {
        name: mxVoucherResponse.name,
        barcode: barcode,
        pin: pin,
        ticketRedemptions: mapTicketRedemptionInfo(mxVoucherResponse.ticketRedemptions),
        status: mapMxVoucherStatusToWidgetVoucherStatus(mxVoucherResponse.status)
    };
}
function mapTicketRedemptionInfo(voucherTicketRedemptionInfo) {
    return {
        remainingRedemptions: voucherTicketRedemptionInfo.remainingRedemptions,
        ticketTypes: voucherTicketRedemptionInfo.ticketTypes.map(mapMxVoucherTicketType)
    };
}
function mapMxVoucherTicketType(ticketType) {
    return {
        ticketTypeId: ticketType.ticketTypeId,
        ticketTypeKey: "voucher-ticket-type-" + ticketType.ticketTypeId
    };
}
//# sourceMappingURL=voucher-mapper.js.map