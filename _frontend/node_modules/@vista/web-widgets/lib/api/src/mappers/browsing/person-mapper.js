/**
 * Maps a person from the MX API to a person for widgets to use.
 * @hidden
 */
export function mapMxPersonToWidgetPerson(person) {
    return {
        id: person.id,
        givenName: person.givenName,
        familyName: person.familyName
    };
}
//# sourceMappingURL=person-mapper.js.map