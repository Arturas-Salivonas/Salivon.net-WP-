import { mapMxGenreToWidgetGenre } from '../genre-mapper';
import { mxGenre, widgetGenre } from './browsing-test-data';
describe('mapMxGenreToWidgetGenre', function () {
    it('maps a MxGenre to a Widget Genre', function () {
        var result = mapMxGenreToWidgetGenre(mxGenre);
        expect(result).toEqual(widgetGenre);
    });
});
//# sourceMappingURL=genre-mapper.test.js.map