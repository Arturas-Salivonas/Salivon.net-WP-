import { parse } from 'date-fns';
/**
 * Maps a showtime from the mx api to a showtime object for widgets to use.
 *
 * @export
 * @param {MxShowtime} showtime
 * @returns {Showtime}
 * @hidden
 */
export function mapMxShowtimeToWidgetShowtime(showtime) {
    return {
        id: showtime.id,
        filmId: showtime.filmId,
        siteId: showtime.siteId,
        startTime: parse(showtime.startTime),
        businessDate: showtime.businessDate,
        attributeIds: showtime.attributeIds,
        isAllocatedSeating: showtime.isAllocatedSeating,
        screenName: showtime.screenName,
        screenNameTranslations: showtime.screenNameTranslations
    };
}
//# sourceMappingURL=showtime-mapper.js.map