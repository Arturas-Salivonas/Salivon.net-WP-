import { getSeatLabelsByPosition } from '../../stores/seating/helpers/utils/seat-utils';
import { seatingStoreInstance } from '../../stores/seating/seating-store';
export function mapMxSelectedSeatToOrderSeat(seat) {
    var seatLabel = getSeatLabelsByPosition(seatingStoreInstance.data.seatLayout, {
        areaNumber: seat.areaNumber,
        rowIndex: seat.rowId,
        columnIndex: seat.columnId
    });
    return {
        position: {
            areaNumber: seat.areaNumber,
            rowIndex: seat.rowId,
            columnIndex: seat.columnId,
        },
        rowLabel: seatLabel ? seatLabel.rowLabel : '',
        seatLabel: seatLabel ? seatLabel.seatLabel : '',
        areaCategoryCode: seat.areaCategoryCode
    };
}
//# sourceMappingURL=selected-seat-mapper.js.map