import { mapMxSiteAddressToWidgetSiteAddress } from './site-address-mapper';
import { mapMxSiteContactDetailsToWidgetSiteContactDetails } from './site-contact-details-mapper';
import { mapMxSiteLocationToWidgetSiteLocation } from './site-location-mapper';
/**
 * Maps a site from the mx api to a site object for widgets to use.
 *
 * @export
 * @param {MxSite} site
 * @returns {WidgetSite}
 * @hidden
 */
export function mapMxSiteToWidgetSite(site) {
    return {
        id: site.id,
        name: site.name,
        nameTranslations: site.nameTranslations,
        address: site.address ? mapMxSiteAddressToWidgetSiteAddress(site.address) : undefined,
        location: site.location ? mapMxSiteLocationToWidgetSiteLocation(site.location) : undefined,
        contactDetails: mapMxSiteContactDetailsToWidgetSiteContactDetails(site.contactDetails)
    };
}
//# sourceMappingURL=site-mapper.js.map