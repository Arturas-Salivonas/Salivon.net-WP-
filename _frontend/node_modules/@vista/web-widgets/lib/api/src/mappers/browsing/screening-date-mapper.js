import { mapMxScreeningDetailsToWidgetScreeningDetails } from './screening-detail-mapper';
/**
 * Maps a screening date from the MX API to a screening date for widgets to use.
 * @hidden
 */
export function mapMxScreeningDateToWidgetScreeningDate(screeningDate) {
    return {
        businessDate: screeningDate.businessDate,
        screeningDetails: mapMxScreeningDetailsToWidgetScreeningDetails(screeningDate.screeningDetails)
    };
}
//# sourceMappingURL=screening-date-mapper.js.map