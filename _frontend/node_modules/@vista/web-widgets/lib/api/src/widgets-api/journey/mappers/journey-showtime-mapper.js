export var mapMxJourneyShowtime = function (showtime) {
    return {
        id: showtime.id,
        filmId: showtime.filmId,
        siteId: showtime.siteId,
        screenId: showtime.screenId,
        startTime: {
            local: new Date(showtime.startTime.local),
            utc: new Date(showtime.startTime.utc)
        },
        endTime: {
            local: new Date(showtime.endTime.local),
            utc: new Date(showtime.endTime.utc)
        }
    };
};
//# sourceMappingURL=journey-showtime-mapper.js.map