import { isPast } from 'date-fns';
import { getMinutesUntilDate } from '../../../../../widgets/src/utils/date-utils';
import { configurationStoreInstance } from '../../../stores/configuration/configuration-store';
export var isJourneyShowtimePlaying = function (showtime) {
    return getMinutesUntilDate(showtime.startTime.utc) <= 0 && !hasJourneyShowtimeEnded(showtime);
};
export var hasJourneyShowtimeEnded = function (showtime) {
    return isPast(showtime.endTime.utc);
};
export var isJourneyShowtimeAdmitting = function (showtime) {
    var admittingDuration = configurationStoreInstance.data.ticketing.admittingDuration;
    var minutesUntilShowtime = getMinutesUntilDate(showtime.startTime.utc);
    if (admittingDuration <= 0 || minutesUntilShowtime <= 0)
        return false;
    return minutesUntilShowtime < admittingDuration;
};
//# sourceMappingURL=journey-showtime-utils.js.map