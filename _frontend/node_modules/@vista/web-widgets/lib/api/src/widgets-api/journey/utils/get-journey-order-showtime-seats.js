import { concatObservableArrays } from '../../../../../widgets/src/utils/array-utils';
/**
 * Returns a flat list of seats for a journey order showtime.
 */
export function getJourneyOrderShowtimeSeats(showtime) {
    return showtime.tickets.reduce(function (acc, ticket) {
        return concatObservableArrays(acc, ticket.seats);
    }, new Array());
}
//# sourceMappingURL=get-journey-order-showtime-seats.js.map