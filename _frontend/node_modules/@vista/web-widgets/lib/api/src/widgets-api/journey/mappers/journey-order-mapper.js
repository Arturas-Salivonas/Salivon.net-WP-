import { mapMxJourneyOrderBooking } from './journey-order-booking-mapper';
import { mapMxJourneyOrderItem } from './journey-order-item-mapper';
import { mapMxJourneyOrderShowtime } from './journey-order-showtime-mapper';
export var mapMxJourneyOrder = function (order) {
    return {
        totalPrice: order.totalPrice,
        originalPrice: order.originalPrice,
        booking: order.booking && mapMxJourneyOrderBooking(order.booking),
        showtimes: order.showtimes.map(mapMxJourneyOrderShowtime),
        items: order.items.map(mapMxJourneyOrderItem)
    };
};
//# sourceMappingURL=journey-order-mapper.js.map