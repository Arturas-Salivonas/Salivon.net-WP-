export interface PollSettings {
    timeout?: number;
    interval?: number;
}
export declare type PollResult<T> = {
    success: true;
    payload: T;
} | {
    success: false;
};
export declare function poll<T>(pollingFunction: () => Promise<T>, pollingCriteria: (result: T) => Boolean, pollSettings?: PollSettings): Promise<PollResult<T>>;
