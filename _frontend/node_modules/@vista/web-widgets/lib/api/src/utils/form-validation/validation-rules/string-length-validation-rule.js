import { FormValidationError } from './validation-error';
export var stringLengthValidationRule = function (key, minLength, maxLength) {
    return function (obj) {
        var property = obj[key];
        var isValid = typeof property === 'string' && property.trim().length >= minLength && property.trim().length <= maxLength;
        if (!isValid) {
            return FormValidationError.Length;
        }
    };
};
//# sourceMappingURL=string-length-validation-rule.js.map