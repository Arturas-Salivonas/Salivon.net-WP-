import { isSerializerValueObject } from '../serializer-value';
describe('isSerializerValueObject', function () {
    it('returns true if the object contains "_type_" and "_value_"', function () {
        var result = isSerializerValueObject({
            '_type_': 'test',
            '_value_': 'test'
        });
        expect(result).toEqual(true);
    });
    it('returns false if the object contains "_type_" but not "_value_"', function () {
        var result = isSerializerValueObject({
            '_type_': 'test'
        });
        expect(result).toEqual(false);
    });
    it('returns false if the object contains "_value_" but not "_type_"', function () {
        var result = isSerializerValueObject({
            '_value_': 'test'
        });
        expect(result).toEqual(false);
    });
    it('returns false if the object does not contain "_type_" or "_value_"', function () {
        var result = isSerializerValueObject({});
        expect(result).toEqual(false);
    });
});
//# sourceMappingURL=serializer-value.test.js.map