import { Validator } from './validator';
/**
 * Validates a field of a given object using a given validator.
 * Deletes the field from the object if invalid.
 */
export declare const fieldValidator: <T, K extends keyof T>(object: Partial<T>) => (validator: Validator, key: K, errorMsg: string) => Partial<T>;
