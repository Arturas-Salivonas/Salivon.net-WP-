/**
 * Validates a field of a given object using a given validator.
 * Deletes the field from the object if invalid.
 */
export var fieldValidator = function (object) {
    return function (validator, key, errorMsg) {
        if (key in object) {
            var valid = validator.validate(object[key], errorMsg);
            if (!valid)
                delete object[key];
        }
        return object;
    };
};
//# sourceMappingURL=field-validator.js.map