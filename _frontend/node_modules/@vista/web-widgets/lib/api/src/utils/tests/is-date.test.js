import { isDate } from '../is-date';
describe('isDate', function () {
    [new Date(), new Date(2012, 4, 23)]
        .map(function (value) {
        it('returns true for date objects', function () {
            var result = isDate(value);
            expect(result).toBe(true);
        });
    });
    [undefined, null, {}, '', 0, function (a) { return a; }]
        .map(function (value) {
        it('returns false for non-date values', function () {
            var result = isDate(value);
            expect(result).toBe(false);
        });
    });
    ['Mon April 23 2012', '2012-04-23']
        .map(function (value) {
        it('returns false for date strings', function () {
            var result = isDate(value);
            expect(result).toBe(false);
        });
    });
});
//# sourceMappingURL=is-date.test.js.map