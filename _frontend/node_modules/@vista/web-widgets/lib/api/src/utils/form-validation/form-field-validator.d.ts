import { ValidationRule } from './validation-rules/validation-rule';
export interface IFormFieldValidator<T> {
    addValidationRule(validationRule: ValidationRule<T>): any;
    validate(obj: T): Promise<string | undefined>;
}
export declare class FormFieldValidator<T> implements IFormFieldValidator<T> {
    private isRequired;
    private validationRules;
    private requiredRule;
    constructor(key: string, isRequired: boolean);
    addValidationRule(validationRule: ValidationRule<T>): void;
    validate(obj: T): Promise<string | undefined>;
}
