import { FormValidationError } from './validation-error';
export var requiredValidationRule = function (key) {
    return function (obj) {
        var property = obj[key];
        var isValid = property !== null && property !== undefined && (typeof property !== 'string' || property.trim().length > 0);
        if (!isValid) {
            return FormValidationError.Required;
        }
    };
};
//# sourceMappingURL=required-validation-rule.js.map