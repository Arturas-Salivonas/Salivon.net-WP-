import { FormValidationError } from './validation-error';
export var regexFormatValidationRule = function (key, regex) {
    return function (obj) {
        var property = obj[key];
        var isValid = typeof property === 'string' && regex.test(property);
        if (!isValid) {
            return FormValidationError.Format;
        }
    };
};
//# sourceMappingURL=regex-format-validation-rule.js.map