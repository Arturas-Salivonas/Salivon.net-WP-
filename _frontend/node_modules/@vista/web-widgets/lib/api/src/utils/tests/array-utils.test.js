import { compactMap } from '../array-utils';
describe('compactMap', function () {
    it('Transforms the array using the mapping function', function () {
        var sourceArray = [1, 2, 3, 4, 5];
        var result = compactMap(sourceArray, function (item) { return item * 10; });
        expect(result).toEqual([10, 20, 30, 40, 50]);
    });
    it('Removes any undefined values', function () {
        var sourceArray = [1, 2, 3, 4, 5, 6];
        var result = compactMap(sourceArray, function (item) {
            return (item % 2 === 0) ? item * 10 : undefined;
        });
        expect(result).toEqual([20, 40, 60]);
    });
});
//# sourceMappingURL=array-utils.test.js.map