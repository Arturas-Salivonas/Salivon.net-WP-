import { VistaDiagnosticsLog } from '@vista/diagnostics';
/**
 * Logger singleton
 * Can be used for internal logs, or more widely adopted by individual widgets.
 *
 * **Example 1: Basic logging**
 * ```js
 *     vista.logger.info("Here's some info about Vista Web Widgets")
 * ```
 *
 * **Example 2: Controlling log output**
 *
 * In this example, we've set the logging threshold to only display error messages.
 * Other messages won't display in the output.
 * ```js
 *     vista.logger.configure('console', { thresholdLevel: 3});
 *     vista.logger.warn("This warning is above threshold");
 * ```
 *
 * **Example 3: Providing additional log information**
 *
 * In this example, additional information is available along with the warning message.
 * ```js
 *      vista.logger.warn("Warning message", { some: 'content' });
 * ```
 */
export declare const Logger: VistaDiagnosticsLog;
