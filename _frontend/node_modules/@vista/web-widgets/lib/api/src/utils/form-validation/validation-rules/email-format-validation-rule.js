import { FormValidationError } from './validation-error';
// Regex used by Chrome for email, sourced from https://rgxdb.com
var emailRegex = /^[a-z0-9!#$%&'*+\/=?^_`{|}~.-]+@[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\.[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)*$/i;
export var emailFormatValidationRule = function (key) {
    return function (obj) {
        var property = obj[key];
        var isValid = typeof property === 'string' && emailRegex.test(property);
        if (!isValid) {
            return FormValidationError.Format;
        }
    };
};
//# sourceMappingURL=email-format-validation-rule.js.map