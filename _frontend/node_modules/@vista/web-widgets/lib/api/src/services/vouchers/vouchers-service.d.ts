import { AddVoucherToOrderResult } from '../../../../types/vouchers/add-voucher-to-order-result';
import { Voucher } from '../../../../types/vouchers/voucher';
/**
 * Service to fetch details about vouchers from the API and add them to the in-progress order.
 */
export interface VouchersService {
    validateVoucher: (siteId: string, cinemaChainId: string, voucherBarcode: string, voucherPin?: string) => Promise<Voucher>;
    addVoucherToOrder: (showtimeId: string, voucherBarcode: string, voucherPin?: string) => Promise<AddVoucherToOrderResult>;
}
/**
 * Default instance of the vouchers service
 */
export declare const vouchersService: VouchersService;
