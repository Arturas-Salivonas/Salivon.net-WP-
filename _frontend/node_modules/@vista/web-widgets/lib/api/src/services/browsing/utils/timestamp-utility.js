/**
 * Generates a timestamp with the current date.
 * Uses Date.now() internally so the date can be mocked in unit tests.
 */
export var getTimestamp = function () {
    return new Date(Date.now());
};
//# sourceMappingURL=timestamp-utility.js.map