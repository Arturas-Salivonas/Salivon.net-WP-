import { TicketTypeKey } from '../../../../types/sales-data/ticket-type';
/**
 * Service to make changes to the current order.
 */
export interface OrderService {
    createOrder: () => Promise<string>;
    saveCustomerDetails: () => Promise<void>;
    setTicketQuantity: (showtimeId: string, ticketTypeKey: TicketTypeKey, quantity: number, voucherBarcode?: string) => void;
    updateShowtimeTicketingDetails: (showtimeId: string) => Promise<void>;
    addShowtimeById: (showtimeId: string) => Promise<void>;
    saveSeatSelection: (showtimeId: string) => Promise<void>;
    saveItemSelection: () => Promise<void>;
}
/**
 * Default instance of the order service
 */
export declare const orderService: OrderService;
