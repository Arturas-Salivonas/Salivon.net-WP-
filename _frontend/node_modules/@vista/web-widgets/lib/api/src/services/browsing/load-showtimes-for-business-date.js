var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var _this = this;
import { mxsApi } from '../../../../mx-api-client/src/mxs/api/mxs-api';
import { LoadingState } from '../../../../types/loading-state';
import { mapMxCensorRatingToWidgetCensorRating } from '../../mappers/browsing/censor-rating-mapper';
import { mapMxFilmToWidgetFilm } from '../../mappers/browsing/film-mapper';
import { mapMxGenreToWidgetGenre } from '../../mappers/browsing/genre-mapper';
import { mapMxPersonToWidgetPerson } from '../../mappers/browsing/person-mapper';
import { mapMxShowtimeAttributeToWidgetShowtimeAttribute } from '../../mappers/browsing/showtime-attribute-mapper';
import { mapMxShowtimeToWidgetShowtime } from '../../mappers/browsing/showtime-mapper';
import { mapMxSiteToWidgetSite } from '../../mappers/browsing/site-mapper';
import { browsingStoreInstance } from '../../stores/browsing/browsing-store';
import { generateShowtimeQueryKey } from '../../stores/browsing/helpers/utils/query-key-generator';
import { getValidatedCachedData } from '../../stores/cache-tools/cache-utilities';
import { getLoadingFetchedData } from '../../stores/cache-tools/fetched-data';
import { configurationStoreInstance } from '../../stores/configuration/configuration-store';
import { compactMap } from '../../utils/array-utils';
import { getTimestamp } from './utils/timestamp-utility';
export var loadShowtimesForBusinessDate = function (businessDate, query) { return __awaiter(_this, void 0, void 0, function () {
    var cinemaChainId, showtimesCacheTimeMinutes, cachedShowtimesForQuery, cachedShowtimesForQueryOnDate, validatedCachedData, showtimes, _a, showtimes, relatedData, widgetShowtimes, films, sites, showtimeAttributes, people, genres, censorRatings, showtimesForQueryOnDate, err_1, failureData;
    return __generator(this, function (_b) {
        switch (_b.label) {
            case 0:
                cinemaChainId = configurationStoreInstance.data.api.cinemaChainId;
                showtimesCacheTimeMinutes = configurationStoreInstance.data.cache.durations.showtimes;
                cachedShowtimesForQuery = browsingStoreInstance.data.showtimesByQuery[generateShowtimeQueryKey(query)];
                cachedShowtimesForQueryOnDate = cachedShowtimesForQuery && cachedShowtimesForQuery[businessDate];
                validatedCachedData = getValidatedCachedData(cachedShowtimesForQueryOnDate, showtimesCacheTimeMinutes);
                if (validatedCachedData) {
                    showtimes = compactMap(validatedCachedData.payload, function (id) { return browsingStoreInstance.data.showtimesById.get(id); });
                    return [2 /*return*/, Promise.resolve(showtimes)];
                }
                browsingStoreInstance.actions.setShowtimesForQueryOnBusinessDate(getLoadingFetchedData(), query, businessDate);
                _b.label = 1;
            case 1:
                _b.trys.push([1, 3, , 4]);
                return [4 /*yield*/, mxsApi.showtimes.getShowtimesForBusinessDate(cinemaChainId, businessDate, query)];
            case 2:
                _a = _b.sent(), showtimes = _a.showtimes, relatedData = _a.relatedData;
                widgetShowtimes = showtimes.map(mapMxShowtimeToWidgetShowtime);
                // Add related data to browsing store
                if (relatedData) {
                    films = relatedData.films, sites = relatedData.sites, showtimeAttributes = relatedData.showtimeAttributes, people = relatedData.people, genres = relatedData.genres, censorRatings = relatedData.censorRatings;
                    films && browsingStoreInstance.actions.addFilms(films.map(mapMxFilmToWidgetFilm));
                    sites && browsingStoreInstance.actions.addSites(sites.map(mapMxSiteToWidgetSite));
                    showtimeAttributes && browsingStoreInstance.actions.addShowtimeAttributes(showtimeAttributes.map(mapMxShowtimeAttributeToWidgetShowtimeAttribute));
                    people && browsingStoreInstance.actions.addPeople(people.map(mapMxPersonToWidgetPerson));
                    genres && browsingStoreInstance.actions.addGenres(genres.map(mapMxGenreToWidgetGenre));
                    censorRatings && browsingStoreInstance.actions.addCensorRatings(censorRatings.map(mapMxCensorRatingToWidgetCensorRating));
                }
                // Add Showtime objects to browsing store
                browsingStoreInstance.actions.addShowtimes(widgetShowtimes);
                showtimesForQueryOnDate = {
                    loadingState: LoadingState.Success,
                    payload: widgetShowtimes.map(function (showtime) { return showtime.id; }),
                    timestamp: getTimestamp(),
                };
                browsingStoreInstance.actions.setShowtimesForQueryOnBusinessDate(showtimesForQueryOnDate, query, businessDate);
                return [2 /*return*/, widgetShowtimes.reduce(function (acc, showtimes) { return acc.concat([showtimes]); }, [])];
            case 3:
                err_1 = _b.sent();
                failureData = { loadingState: LoadingState.Failure };
                browsingStoreInstance.actions.setShowtimesForQueryOnBusinessDate(failureData, query, businessDate);
                throw err_1;
            case 4: return [2 /*return*/];
        }
    });
}); };
//# sourceMappingURL=load-showtimes-for-business-date.js.map