var _a;
import { TicketTypeCategory } from '../../../../../types/sales-data/ticket-type';
var ticketSortingLevels = (_a = {},
    _a[TicketTypeCategory.Voucher] = 0,
    _a[TicketTypeCategory.Loyalty] = 1,
    _a[TicketTypeCategory.Package] = 2,
    _a[TicketTypeCategory.Standard] = 2,
    _a);
var ticketCompareFn = function (a, b) {
    var ticketAPriority = ticketSortingLevels[a.ticketType.category];
    var ticketBPriority = ticketSortingLevels[b.ticketType.category];
    if (ticketAPriority === ticketBPriority)
        return 0;
    if (ticketAPriority < ticketBPriority)
        return -1;
    return 1;
};
export function getSortedOrderTickets(tickets) {
    return tickets.sort(function (a, b) { return ticketCompareFn(a, b); });
}
//# sourceMappingURL=get-sorted-order-tickets.js.map