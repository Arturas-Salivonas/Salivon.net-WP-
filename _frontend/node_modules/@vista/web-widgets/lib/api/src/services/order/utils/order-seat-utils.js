export var getSeatsCountPerAreaCategory = function (seats) {
    return seats.reduce(function (acc, seat) {
        var areaCategoryCode = seat.areaCategoryCode;
        var previousCount = acc.get(areaCategoryCode) || 0;
        return acc.set(areaCategoryCode, previousCount + 1);
    }, new Map());
};
//# sourceMappingURL=order-seat-utils.js.map