import { OrderTicketCategory } from './../../../../../types/order/order-ticket';
import { orderStoreInstance } from './../../../stores/order/order-store';
import { getTicketTypeTicketCount } from './ticket-type-utils';
export var getTicketsCountPerAreaCategory = function (showtimeId, tickets) {
    return tickets.reduce(function (acc, ticket) {
        var ticketType = orderStoreInstance.selectors.getTicketTypeByKey(showtimeId, ticket.ticketTypeKey);
        var areaCategoryCode = ticketType.areaCategoryCode;
        var previousCount = acc.get(areaCategoryCode) || 0;
        var ticketCount = getTicketTypeTicketCount(ticketType);
        return acc.set(areaCategoryCode, previousCount + (ticket.quantity * ticketCount));
    }, new Map());
};
export var isVoucherTicket = function (ticket) {
    return ticket.category === OrderTicketCategory.Voucher;
};
//# sourceMappingURL=order-ticket-utils.js.map