import { SeatLayout } from './../../../../types/seating/seat-layout';
/**
 * Retrieves the seat layout for a showtime, when following the ticket first ordering mode.
 */
export declare function loadSeatLayoutForOrder(orderId: string, showtimeId: string): Promise<SeatLayout>;
