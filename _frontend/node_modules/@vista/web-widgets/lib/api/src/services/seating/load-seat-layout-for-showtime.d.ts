import { SeatLayout } from './../../../../types/seating/seat-layout';
/**
 * Retrieves the seat layout for a showtime, when following the seat first ordering mode.
 */
export declare function loadSeatLayoutForShowtime(showtimeId: string): Promise<SeatLayout>;
