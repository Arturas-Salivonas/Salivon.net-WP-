import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var addPeople = action(function (people) {
    if (people.length === 0)
        return;
    browsingStoreInstance.data.peopleById.merge(people.map(function (person) { return [person.id, person]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=add-people.js.map