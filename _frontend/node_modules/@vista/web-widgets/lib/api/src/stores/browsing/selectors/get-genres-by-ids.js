import { Logger } from '../../../utils/logger';
import { browsingStoreInstance } from '../browsing-store';
// Todo: improve caching by using a transformer
export var getGenresByIds = function (genreIDs) {
    var genresById = browsingStoreInstance.data.genresById;
    return genreIDs.reduce(function (acc, genreId) {
        var genre = genresById.get(genreId);
        if (genre)
            acc.push(genre);
        else
            Logger.error("Couldn't find genre with ID " + genreId);
        return acc;
    }, new Array());
};
//# sourceMappingURL=get-genres-by-ids.js.map