var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { exampleOrderShowtime } from '../../../../../../example-data/order/order-showtime';
import { exampleStandardTicket1 } from '../../../../../../example-data/order/order-ticket';
import { exampleStandardTicketType1 } from '../../../../../../example-data/order/order-ticket-type';
import { orderStoreInstance } from '../../order-store';
import { getOrderTotalCost } from '../get-order-total-cost';
describe('getOrderTotalCost', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns 0 if the order has no showtimes or booking fee', function () {
        orderStoreInstance.data.order.showtimes = [];
        orderStoreInstance.data.order.bookingFee = 0;
        var result = getOrderTotalCost();
        expect(result).toEqual(0);
    });
    it('returns the total value of the order', function () {
        var ticketTypes = new Map([
            [exampleStandardTicketType1.key, __assign({}, exampleStandardTicketType1, { price: 5 })]
        ]);
        var showtime = __assign({}, exampleOrderShowtime, { tickets: [
                __assign({}, exampleStandardTicket1, { quantity: 1 })
            ], ticketTypes: ticketTypes });
        orderStoreInstance.data.order.showtimes = [showtime];
        orderStoreInstance.data.order.bookingFee = 10;
        orderStoreInstance.data.order.items = [
            {
                id: '1',
                itemModelId: '',
                price: 5
            }
        ];
        var result = getOrderTotalCost();
        expect(result).toEqual(20);
    });
});
//# sourceMappingURL=get-order-total-cost.test.js.map