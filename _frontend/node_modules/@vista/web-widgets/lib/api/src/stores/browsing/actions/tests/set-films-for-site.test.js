import { toJS } from 'mobx';
import { browsingStoreInstance } from '../../browsing-store';
import { setFilmsForSite } from '../set-films-for-site';
import { exampleFilms } from './../../../../../../example-data/browsing/film';
describe('setFilmsForSite', function () {
    var siteId = '123';
    beforeEach(function () {
        browsingStoreInstance.flush();
    });
    it('can set the films for a given site', function () {
        setFilmsForSite(siteId, exampleFilms);
        expect(toJS(browsingStoreInstance.data.filmsBySiteId.get(siteId))).toEqual(exampleFilms);
    });
});
//# sourceMappingURL=set-films-for-site.test.js.map