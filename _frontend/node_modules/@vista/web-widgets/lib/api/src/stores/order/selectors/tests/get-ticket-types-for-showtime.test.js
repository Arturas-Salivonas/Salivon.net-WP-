var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { observable } from 'mobx';
import { exampleOrderShowtime } from '../../../../../../example-data/order/order-showtime';
import { exampleStandardTicketType1 } from '../../../../../../example-data/order/order-ticket-type';
import { getTicketTypesForShowtime } from '../get-ticket-types-for-showtime';
import { exampleStandardTicketType2 } from './../../../../../../example-data/order/order-ticket-type';
import { orderStoreInstance } from './../../order-store';
describe('getTicketTypesForShowtime', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('throws an error if the showtime does not exist in the order', function () {
        expect(function () { return getTicketTypesForShowtime('123'); }).toThrow();
    });
    it('throws an error if the showtime ticketTypes are undefined', function () {
        var showtime = __assign({}, exampleOrderShowtime, { ticketTypes: undefined });
        orderStoreInstance.data.order.showtimes = [showtime];
        expect(function () { return getTicketTypesForShowtime(showtime.showtimeId); }).toThrow();
    });
    it('returns the showtime ticketTypes', function () {
        var ticketTypes = new Map([
            [exampleStandardTicketType1.key, exampleStandardTicketType1],
            [exampleStandardTicketType2.key, exampleStandardTicketType2],
        ]);
        var showtime = __assign({}, exampleOrderShowtime, { ticketTypes: ticketTypes });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = getTicketTypesForShowtime(showtime.showtimeId);
        expect(result).toEqual(observable.map(ticketTypes));
    });
});
//# sourceMappingURL=get-ticket-types-for-showtime.test.js.map