var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { getSelectedSeatsCountForShowtime } from '../get-selected-seats-count-for-showtime';
import { exampleOrderSeatA1, exampleOrderSeatA2 } from './../../../../../../example-data/order/order-seat';
import { exampleOrderShowtime } from './../../../../../../example-data/order/order-showtime';
import { orderStoreInstance } from './../../order-store';
describe('getSelectedSeatsCountForShowtime', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('throws an error if the showtime does not exist in the order', function () {
        expect(function () { return getSelectedSeatsCountForShowtime('123'); }).toThrow();
    });
    it('returns 0 if the showtime has no selected seats', function () {
        var showtime = __assign({}, exampleOrderShowtime, { seats: [] });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = getSelectedSeatsCountForShowtime(showtime.showtimeId);
        expect(result).toEqual(0);
    });
    it('returns 0 if the showtime seats are undefined', function () {
        var showtime = __assign({}, exampleOrderShowtime, { seats: undefined });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = getSelectedSeatsCountForShowtime(showtime.showtimeId);
        expect(result).toEqual(0);
    });
    it('returns the count of selected seats', function () {
        var showtime = __assign({}, exampleOrderShowtime, { seats: [
                exampleOrderSeatA1,
                exampleOrderSeatA2
            ] });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = getSelectedSeatsCountForShowtime(showtime.showtimeId);
        expect(result).toEqual(2);
    });
});
//# sourceMappingURL=get-selected-seats-count-for-showtime.test.js.map