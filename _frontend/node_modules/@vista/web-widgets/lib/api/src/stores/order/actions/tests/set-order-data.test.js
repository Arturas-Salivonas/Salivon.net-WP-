var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { exampleOrder } from '../../../../../../example-data/order/order';
import { orderStoreInstance } from './../../order-store';
import { setOrderData } from './../set-order-data';
describe('setOrderData', function () {
    it('merges and sets the order data', function () {
        orderStoreInstance.data.order.orderId = '123';
        orderStoreInstance.setData = jest.fn();
        var order = exampleOrder;
        delete order.orderId;
        setOrderData(order);
        expect(orderStoreInstance.setData).toBeCalledWith({ order: __assign({}, order, { orderId: '123' }) });
    });
});
//# sourceMappingURL=set-order-data.test.js.map