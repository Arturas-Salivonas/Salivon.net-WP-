import { orderStoreInstance } from '../order-store';
export var getSelectedItemModelQuantity = function (itemModelId) {
    var order = orderStoreInstance.data.order;
    if (!order)
        return 0;
    return order.items.filter(function (item) { return item.itemModelId === itemModelId; }).length;
};
//# sourceMappingURL=get-selected-item-model-quantity.js.map