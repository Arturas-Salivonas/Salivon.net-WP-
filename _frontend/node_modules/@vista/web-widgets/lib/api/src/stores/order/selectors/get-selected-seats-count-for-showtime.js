import { orderStoreInstance } from '../order-store';
export function getSelectedSeatsCountForShowtime(showtimeId) {
    var showtime = orderStoreInstance.selectors.getShowtimeById(showtimeId);
    if (!showtime.seats)
        return 0;
    return showtime.seats.length;
}
//# sourceMappingURL=get-selected-seats-count-for-showtime.js.map