import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var addShowtimes = action(function (showtimes) {
    if (showtimes.length === 0) {
        return;
    }
    browsingStoreInstance.data.showtimesById
        .merge(showtimes.map(function (showtime) { return [showtime.id, showtime]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=add-showtimes.js.map