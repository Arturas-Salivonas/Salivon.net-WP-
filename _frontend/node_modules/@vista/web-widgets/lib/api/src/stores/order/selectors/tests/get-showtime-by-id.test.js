var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { toJS } from 'mobx';
import { getShowtimeById } from '../get-showtime-by-id';
import { exampleOrderShowtime } from './../../../../../../example-data/order/order-showtime';
import { orderStoreInstance } from './../../order-store';
describe('getShowtimeById', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('throws an error if the showtime does not exist in the order', function () {
        expect(function () { return getShowtimeById('123'); }).toThrow();
    });
    it('returns the showtime', function () {
        var showtime = __assign({}, exampleOrderShowtime, { ticketTypes: undefined });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = getShowtimeById(showtime.showtimeId);
        expect(toJS(result)).toEqual(showtime);
    });
});
//# sourceMappingURL=get-showtime-by-id.test.js.map