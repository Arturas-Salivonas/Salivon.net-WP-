import { toJS } from 'mobx';
import { orderStoreInstance } from '../../order-store';
import { updateCustomerDetails } from '../update-customer-details';
import { exampleOrderCustomerDetails } from './../../../../../../example-data/order/order-customer-details';
describe('updateCustomerDetails', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('can update details', function () {
        updateCustomerDetails(exampleOrderCustomerDetails);
        var currentCustomerDetails = toJS(orderStoreInstance.data.customerDetails);
        expect(currentCustomerDetails).toEqual(exampleOrderCustomerDetails);
    });
    it('does not change the object reference', function () {
        var previousCustomerDetails = orderStoreInstance.data.customerDetails;
        updateCustomerDetails(exampleOrderCustomerDetails);
        expect(orderStoreInstance.data.customerDetails).toBe(previousCustomerDetails);
    });
});
//# sourceMappingURL=update-customer-details.test.js.map