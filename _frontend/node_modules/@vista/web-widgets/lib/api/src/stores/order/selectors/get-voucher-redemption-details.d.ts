export declare function getVoucherRedemptionDetails(voucherBarcode: string): VoucherRedemptionDetails;
export interface VoucherRedemptionDetails {
    max: number;
    used: number;
    remaining: number;
}
