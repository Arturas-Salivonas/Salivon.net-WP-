import { defaultOrderStoreData } from '../../../../../types/stores/order-store-data';
import { OrderStore } from './../order-store';
/**
 * Order Store class test
 */
describe('Order Store', function () {
    var orderStore;
    beforeEach(function () {
        orderStore = new OrderStore();
    });
    it('has defaults', function () {
        // use JSON.stringify because store data is proxied by mobx
        expect(JSON.stringify(orderStore.data)).toEqual(JSON.stringify(defaultOrderStoreData));
    });
});
//# sourceMappingURL=order-store.test.js.map