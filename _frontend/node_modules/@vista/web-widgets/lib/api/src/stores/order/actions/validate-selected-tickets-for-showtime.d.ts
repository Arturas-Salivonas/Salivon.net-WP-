import { OrderStoreActions } from './actions';
/**
 * Returns whether the current tickets selection comparing with selected seats is valid or not.
 */
export declare const validateSelectedTicketsForShowtime: OrderStoreActions['validateSelectedTicketsForShowtime'];
