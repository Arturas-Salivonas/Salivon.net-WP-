import { action } from 'mobx';
import { orderStoreInstance } from '../order-store';
/**
 * Initializes a new order with defaults
 */
export var initializeOrder = action(function (cinemaChainId) {
    orderStoreInstance.actions.setOrderData({
        hasUnsavedChanges: true,
        cinemaChainId: cinemaChainId,
        bookingFee: 0,
        showtimes: []
    });
});
//# sourceMappingURL=initialize-order.js.map