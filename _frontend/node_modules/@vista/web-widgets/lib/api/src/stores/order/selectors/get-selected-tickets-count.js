import { calculateSelectedTicketsCountForShowtime } from '../../../services/order/utils/order-showtime-utils';
import { orderStoreInstance } from '../order-store';
export function getSelectedTicketsCount() {
    return orderStoreInstance.data.order.showtimes.reduce(function (total, showtime) {
        return total += calculateSelectedTicketsCountForShowtime(showtime);
    }, 0);
}
//# sourceMappingURL=get-selected-tickets-count.js.map