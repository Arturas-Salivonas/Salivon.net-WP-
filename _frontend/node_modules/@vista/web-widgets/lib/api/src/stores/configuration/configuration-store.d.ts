import { LocaleTags } from '@vista/i18n';
import { ConfigurationStoreData } from '../../../../types/stores/configuration-store-data';
import { BaseStore } from '../base-store';
import { NestedPartial } from './../../../../types/nested-partial';
/**
 * Class for storing configuration settings.
 */
export declare class ConfigurationStore extends BaseStore<ConfigurationStoreData> {
    constructor();
    setConfig(data: NestedPartial<ConfigurationStoreData>): void;
    /**
     * Sets the global languageTag configuration
     * @param languageTag
     */
    setLanguageTag(languageTag: LocaleTags): void;
}
export declare const configurationStoreInstance: ConfigurationStore;
