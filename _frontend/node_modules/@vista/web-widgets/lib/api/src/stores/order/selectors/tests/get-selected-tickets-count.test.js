var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { getSelectedTicketsCount } from '../get-selected-tickets-count';
import { exampleOrderShowtime } from './../../../../../../example-data/order/order-showtime';
import { exampleStandardTicket1, exampleStandardTicket2 } from './../../../../../../example-data/order/order-ticket';
import { orderStoreInstance } from './../../order-store';
describe('getSelectedTicketsCount', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns 0 if the order has no showtimes', function () {
        orderStoreInstance.data.order.showtimes = [];
        var result = getSelectedTicketsCount();
        expect(result).toEqual(0);
    });
    it('returns the count of selected tickets in the order', function () {
        var showtime = __assign({}, exampleOrderShowtime, { tickets: [
                __assign({}, exampleStandardTicket1, { quantity: 1 }),
                __assign({}, exampleStandardTicket2, { quantity: 2 }),
            ] });
        orderStoreInstance.data.order.showtimes = [showtime, showtime];
        var result = getSelectedTicketsCount();
        expect(result).toEqual(6);
    });
});
//# sourceMappingURL=get-selected-tickets-count.test.js.map