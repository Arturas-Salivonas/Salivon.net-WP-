import { orderStoreInstance } from '../order-store';
export function hasTicketTypesForShowtime(showtimeId) {
    var showtime = orderStoreInstance.data.order.showtimes.find(function (showtime) { return showtime.showtimeId === showtimeId; });
    return !!(showtime && showtime.ticketTypes && showtime.ticketTypes.size > 0);
}
//# sourceMappingURL=has-ticket-types-for-showtime.js.map