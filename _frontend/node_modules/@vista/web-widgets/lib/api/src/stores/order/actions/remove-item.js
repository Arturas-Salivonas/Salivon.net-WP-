import { action } from 'mobx';
import { orderStoreInstance } from '../order-store';
export var removeItem = action(function (orderItemId) {
    var items = orderStoreInstance.data.order.items.filter(function (item) { return item.id !== orderItemId; });
    orderStoreInstance.data.order.items = items;
    orderStoreInstance.syncToCache();
});
//# sourceMappingURL=remove-item.js.map