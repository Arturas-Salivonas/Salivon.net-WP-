import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var removeSelectedSite = action(function (siteId) {
    var selectedSites = browsingStoreInstance.data.selectedSiteIds.filter(function (s) { return s !== siteId; });
    browsingStoreInstance.data.selectedSiteIds = selectedSites;
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=remove-selected-site.js.map