import { orderStoreInstance } from '../order-store';
export var getTicketsTotalCostForShowtime = function (showtimeId) {
    var order = orderStoreInstance.data.order;
    if (!order)
        return 0;
    var orderShowtimes = order.showtimes || [];
    var showtime = orderShowtimes.find(function (s) { return s.showtimeId === showtimeId; });
    return showtime === undefined ? 0 : getShowtimeTicketsTotal(showtime);
};
export var getTicketsTotalCostForOrder = function () {
    var showtimes = orderStoreInstance.data.order.showtimes || [];
    return showtimes.reduce(function (ticketsTotal, showtime) { return ticketsTotal + getShowtimeTicketsTotal(showtime); }, 0);
};
var getShowtimeTicketsTotal = function (showtime) {
    return showtime.tickets.reduce(function (ticketsTotal, ticket) {
        var ticketType = showtime.ticketTypes.get(ticket.ticketTypeKey);
        return ticketsTotal + (ticket.quantity * ticketType.price);
    }, 0);
};
//# sourceMappingURL=get-tickets-total-cost-for-showtime.js.map