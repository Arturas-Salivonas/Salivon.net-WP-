import { browsingStoreInstance } from './browsing/browsing-store';
import { configurationStoreInstance } from './configuration/configuration-store';
import { journeyStoreInstance } from './journey/journey-store';
import { loyaltyStoreInstance } from './loyalty/loyalty-store';
import { orderStoreInstance } from './order/order-store';
import { salesDataStoreInstance } from './sales-data/sales-data-store';
import { seatingStoreInstance } from './seating/seating-store';
export var stores = {
    browsing: browsingStoreInstance,
    order: orderStoreInstance,
    seating: seatingStoreInstance,
    configuration: configurationStoreInstance,
    loyalty: loyaltyStoreInstance,
    salesData: salesDataStoreInstance,
    journey: journeyStoreInstance
};
/**
 * Hydrates the data for each store based on the cached state inside local storage
 */
export function hydrateStores() {
    Object.keys(stores).forEach(function (key) {
        stores[key].hydrate();
    });
}
//# sourceMappingURL=stores.js.map