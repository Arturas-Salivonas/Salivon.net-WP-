import { Area } from '../../../../../../types/seating/area';
import { AreaCategory } from '../../../../../../types/seating/area-category';
import { RowBuilder } from './row-builder';
export declare class AreaBuilder {
    private readonly areaCategory;
    private readonly area;
    constructor(areaCategory: AreaCategory, area: Area);
    setDescription(description: string): AreaBuilder;
    addRow(rowLabel: string, rowBuilderFn: (rowBuilder: RowBuilder) => void): AreaBuilder;
}
