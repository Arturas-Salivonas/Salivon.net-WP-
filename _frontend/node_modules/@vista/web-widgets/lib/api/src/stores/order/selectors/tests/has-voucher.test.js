import { exampleVoucherSingleUse } from '../../../../../../example-data/vouchers';
import { orderStoreInstance } from '../../order-store';
import { hasVoucher } from '../has-voucher';
describe('hasVoucher', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns false when the order does not contain the given voucher barcode', function () {
        orderStoreInstance.data.order.vouchers = {};
        var result = hasVoucher('123');
        expect(result).toEqual(false);
    });
    it('returns true when the order does contain the given voucher barcode', function () {
        var _a;
        orderStoreInstance.data.order.vouchers = (_a = {},
            _a[exampleVoucherSingleUse.barcode] = exampleVoucherSingleUse,
            _a);
        var result = hasVoucher(exampleVoucherSingleUse.barcode);
        expect(result).toEqual(true);
    });
});
//# sourceMappingURL=has-voucher.test.js.map