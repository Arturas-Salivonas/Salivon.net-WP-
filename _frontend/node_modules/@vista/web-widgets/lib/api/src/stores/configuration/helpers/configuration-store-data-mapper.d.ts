import { ConfigurationStoreData } from '../../../../../types/stores/configuration-store-data';
import { NestedPartial } from './../../../../../types/nested-partial';
/**
 * Merges the configuration store data provided with the current configuration.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the current values.
 */
export declare function mapConfigurationStoreData(currentConfig: ConfigurationStoreData, data?: NestedPartial<ConfigurationStoreData>): ConfigurationStoreData;
