import { Area } from '../../../../../../types/seating/area';
import { AreaCategory } from '../../../../../../types/seating/area-category';
import { AreaBuilder } from './area-builder';
export declare class AreaCategoryBuilder {
    areas: Area[];
    private readonly areaCategory;
    constructor(areaCategory: AreaCategory);
    addArea(areaBuilderFn: (areaBuilder: AreaBuilder) => void): this;
    setTotalSeatsToSelect(totalSeatsToSelect: number): void;
}
