import { configurationStoreInstance } from './../../../configuration/configuration-store';
import { browsingStoreInstance } from './../../browsing-store';
import { hasMaximumSelectedSites } from './../has-maximum-selected-sites';
describe('hasMaximumSelectedSites', function () {
    beforeEach(function () {
        configurationStoreInstance.data.browsing.maximumSelectedSites = 2;
    });
    afterEach(function () {
        browsingStoreInstance.flush();
    });
    it('returns false if no sites have been selected', function () {
        browsingStoreInstance.data.selectedSiteIds = [];
        var result = hasMaximumSelectedSites();
        expect(result).toEqual(false);
    });
    it('returns false if less than the maximum number of sites have been selected', function () {
        browsingStoreInstance.data.selectedSiteIds = ['1'];
        var result = hasMaximumSelectedSites();
        expect(result).toEqual(false);
    });
    it('returns true if the maximum number of sites have been selected', function () {
        browsingStoreInstance.data.selectedSiteIds = ['1', '2'];
        var result = hasMaximumSelectedSites();
        expect(result).toEqual(true);
    });
});
//# sourceMappingURL=has-maximum-selected-sites.test.js.map