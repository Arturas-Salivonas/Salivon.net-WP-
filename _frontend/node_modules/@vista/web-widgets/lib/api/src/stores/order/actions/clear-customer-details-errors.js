import { action } from 'mobx';
import { orderStoreInstance } from '../order-store';
export var clearCustomerDetailsErrors = action(function (fieldNames) {
    if (fieldNames === undefined) {
        orderStoreInstance.data.customerDetailsErrors.validationErrors.clear();
    }
    else {
        for (var _i = 0, fieldNames_1 = fieldNames; _i < fieldNames_1.length; _i++) {
            var fieldName = fieldNames_1[_i];
            orderStoreInstance.data.customerDetailsErrors.validationErrors.delete(fieldName);
        }
    }
    orderStoreInstance.syncToCache();
});
//# sourceMappingURL=clear-customer-details-errors.js.map