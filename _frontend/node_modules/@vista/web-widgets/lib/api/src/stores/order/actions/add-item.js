import { action } from 'mobx';
import { ItemModelType } from '../../../../../types/sales-data/item-models/item-model';
import { createUUID } from '../../../utils/uuid-utils';
import { orderStoreInstance } from '../order-store';
export var addItem = action(function (itemModel) {
    if (itemModel.type !== ItemModelType.Normal)
        throw new Error('Only basic items are currently supported');
    orderStoreInstance.data.order.items.push({
        id: createUUID(),
        itemModelId: itemModel.id,
        price: itemModel.price
    });
    orderStoreInstance.syncToCache();
});
//# sourceMappingURL=add-item.js.map