import { orderStoreInstance } from '../order-store';
import { getItemsTotalCostForOrder } from './get-items-total-cost-for-order';
import { getTicketsTotalCostForOrder } from './get-tickets-total-cost-for-showtime';
/**
 * Calculates and returns the current order total (booking fee inclusive).
 */
export function getOrderTotalCost() {
    var order = orderStoreInstance.data.order;
    if (!order)
        return 0;
    var bookingFee = order.bookingFee || 0;
    return bookingFee + getTicketsTotalCostForOrder() + getItemsTotalCostForOrder();
}
//# sourceMappingURL=get-order-total-cost.js.map