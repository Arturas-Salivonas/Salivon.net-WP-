import { Seat } from '../../../../../../types/seating/seat';
export interface SelectedSeats {
    [areaCategoryCode: string]: Seat[];
}
export interface SelectedSeatsQueueManager {
    isSeatSelected(seat: Seat): boolean;
    selectSeat(seat: Seat): void;
    deselectSeat(seat: Seat): void;
    popSeatToAccomodateNewSeat(newSeat: Seat): Seat | undefined;
    discardChanges(): void;
    saveChanges(): void;
}
