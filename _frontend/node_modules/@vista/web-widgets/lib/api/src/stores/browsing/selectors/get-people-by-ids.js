import { Logger } from '../../../utils/logger';
import { browsingStoreInstance } from '../browsing-store';
// Todo: improve caching by using a transformer
export var getPeopleByIds = function (peopleIds) {
    var peopleById = browsingStoreInstance.data.peopleById;
    return peopleIds.reduce(function (acc, personId) {
        var person = peopleById.get(personId);
        if (person)
            acc.push(person);
        else
            Logger.error("Couldn't find person with ID " + personId);
        return acc;
    }, new Array());
};
//# sourceMappingURL=get-people-by-ids.js.map