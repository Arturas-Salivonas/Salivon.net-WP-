import { orderStoreInstance } from '../order-store';
import { getItemsTotalCostForOrder } from './get-items-total-cost-for-order';
import { getTicketsTotalCostForOrder } from './get-tickets-total-cost-for-showtime';
/**
 * Calculates and returns the current order subtotal (booking fee exclusive).
 */
export function getOrderSubtotalCost() {
    var order = orderStoreInstance.data.order;
    if (!order)
        return 0;
    return getTicketsTotalCostForOrder() + getItemsTotalCostForOrder();
}
//# sourceMappingURL=get-order-subtotal-cost.js.map