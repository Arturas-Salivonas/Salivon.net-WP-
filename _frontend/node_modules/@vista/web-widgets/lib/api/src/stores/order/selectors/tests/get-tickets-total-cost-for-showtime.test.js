var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { exampleOrderShowtime } from '../../../../../../example-data/order/order-showtime';
import { exampleStandardTicket1 } from '../../../../../../example-data/order/order-ticket';
import { exampleStandardTicketType1 } from '../../../../../../example-data/order/order-ticket-type';
import { orderStoreInstance } from '../../order-store';
import { getTicketsTotalCostForShowtime } from '../get-tickets-total-cost-for-showtime';
describe('getTicketsTotalForShowtime', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns the total value of all tickets in all showtimes', function () {
        var ticketTypes = new Map([
            [exampleStandardTicketType1.key, __assign({}, exampleStandardTicketType1, { price: 5 })]
        ]);
        var showtime1 = __assign({}, exampleOrderShowtime, { showtimeId: '1st showtime', tickets: [__assign({}, exampleStandardTicket1, { quantity: 1 })], ticketTypes: ticketTypes });
        var showtime2 = __assign({}, showtime1, { showtimeId: '2nd showtime' });
        orderStoreInstance.data.order.showtimes = [showtime1, showtime2];
        var result = getTicketsTotalCostForShowtime(showtime1.showtimeId);
        expect(result).toEqual(5);
    });
});
//# sourceMappingURL=get-tickets-total-cost-for-showtime.test.js.map