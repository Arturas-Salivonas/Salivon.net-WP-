import { action } from 'mobx';
import { orderStoreInstance } from './../order-store';
export var addShowtime = action(function (showtime) {
    if (orderStoreInstance.selectors.hasShowtime(showtime.showtimeId))
        return;
    orderStoreInstance.data.order.showtimes.push(showtime);
    orderStoreInstance.syncToCache();
});
//# sourceMappingURL=add-showtime.js.map