var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { defaultLoyaltyStoreData } from '../../../../types/stores/loyalty-store-data';
import { browserSessionServiceInstance } from '../../user/browser-session-service';
import { cacheServiceInstance } from '../../user/cache-service';
import { BaseStore } from '../base-store';
import { loyaltyStoreActions } from './actions/actions';
import { loyaltyStoreSelectors } from './selectors/selectors';
/**
 * User Context Store class
 */
var LoyaltyStore = /** @class */ (function (_super) {
    __extends(LoyaltyStore, _super);
    function LoyaltyStore() {
        // enforce defaults via BaseStore constructor
        return _super.call(this, 'loyalty-store', browserSessionServiceInstance, cacheServiceInstance, defaultLoyaltyStoreData) || this;
    }
    Object.defineProperty(LoyaltyStore.prototype, "selectors", {
        /**
         * Selector methods for the Loyalty Store.
         */
        get: function () {
            return loyaltyStoreSelectors;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(LoyaltyStore.prototype, "actions", {
        /**
         * Actions for the Loyalty Store.
         */
        get: function () {
            return loyaltyStoreActions;
        },
        enumerable: true,
        configurable: true
    });
    return LoyaltyStore;
}(BaseStore));
export { LoyaltyStore };
export var loyaltyStoreInstance = new LoyaltyStore();
//# sourceMappingURL=loyalty-store.js.map