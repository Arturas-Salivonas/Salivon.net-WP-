import { toJS } from 'mobx';
import { exampleFilm } from '../../../../../../example-data/browsing/film';
import { browsingStoreInstance } from '../../browsing-store';
import { addFilms } from '../add-films';
describe('addFilms', function () {
    it('can add a film to the films store', function () {
        var _a;
        addFilms([exampleFilm]);
        expect(toJS(browsingStoreInstance.data.filmsById)).toEqual((_a = {}, _a[exampleFilm.id] = exampleFilm, _a));
    });
});
//# sourceMappingURL=add-films.test.js.map