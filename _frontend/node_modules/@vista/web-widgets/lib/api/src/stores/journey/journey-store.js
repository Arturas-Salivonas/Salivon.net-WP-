var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { browserSessionServiceInstance } from '../../user/browser-session-service';
import { cacheServiceInstance } from '../../user/cache-service';
import { BaseStore } from '../base-store';
import { defaultJourneyStoreData } from './../../../../types/stores/journey-store-data';
import { journeyStoreActions } from './actions/actions';
import { journeyStoreSelectors } from './selectors/selectors';
/**
 * Class for creating objects that store journey information.
 */
var JourneyStore = /** @class */ (function (_super) {
    __extends(JourneyStore, _super);
    function JourneyStore() {
        return _super.call(this, 'journey-store', browserSessionServiceInstance, cacheServiceInstance, defaultJourneyStoreData) || this;
    }
    Object.defineProperty(JourneyStore.prototype, "selectors", {
        /**
         * Selector methods for the Journey Store.
         */
        get: function () {
            return journeyStoreSelectors;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(JourneyStore.prototype, "actions", {
        /**
         * Actions for the Journey Store.
         */
        get: function () {
            return journeyStoreActions;
        },
        enumerable: true,
        configurable: true
    });
    return JourneyStore;
}(BaseStore));
export { JourneyStore };
// create store instance with default values
export var journeyStoreInstance = new JourneyStore();
//# sourceMappingURL=journey-store.js.map