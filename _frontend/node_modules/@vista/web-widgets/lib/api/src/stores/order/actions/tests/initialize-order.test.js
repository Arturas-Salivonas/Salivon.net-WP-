import { orderStoreInstance } from './../../order-store';
import { initializeOrder } from './../initialize-order';
describe('initializeOrder', function () {
    it('calls setOrderData with the default values and given cinemaChainId', function () {
        orderStoreInstance.actions.setOrderData = jest.fn();
        initializeOrder('cinemaChainId');
        expect(orderStoreInstance.actions.setOrderData).toBeCalledWith({
            hasUnsavedChanges: true,
            cinemaChainId: 'cinemaChainId',
            bookingFee: 0,
            showtimes: []
        });
    });
});
//# sourceMappingURL=initialize-order.test.js.map