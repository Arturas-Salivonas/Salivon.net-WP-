var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { defaultSalesDataStoreData } from '../../../../types/stores/sales-data-store-data';
import { browserSessionServiceInstance } from '../../user/browser-session-service';
import { cacheServiceInstance } from '../../user/cache-service';
import { BaseStore } from '../base-store';
import { SalesDataStoreActions } from './actions/actions';
import { salesDataStoreSelectors } from './selectors/selectors';
/**
 * Class for creating objects that store browsing information.
 */
var SalesDataStore = /** @class */ (function (_super) {
    __extends(SalesDataStore, _super);
    function SalesDataStore() {
        return _super.call(this, 'sales-data-store', browserSessionServiceInstance, cacheServiceInstance, defaultSalesDataStoreData) || this;
    }
    Object.defineProperty(SalesDataStore.prototype, "selectors", {
        /**
         * Selector methods for the Browsing Store.
         */
        get: function () {
            return salesDataStoreSelectors;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(SalesDataStore.prototype, "actions", {
        /**
         * Actions for the Browsing Store.
         */
        get: function () {
            return SalesDataStoreActions;
        },
        enumerable: true,
        configurable: true
    });
    return SalesDataStore;
}(BaseStore));
export { SalesDataStore };
// create store instance with default values
export var salesDataStoreInstance = new SalesDataStore();
//# sourceMappingURL=sales-data-store.js.map