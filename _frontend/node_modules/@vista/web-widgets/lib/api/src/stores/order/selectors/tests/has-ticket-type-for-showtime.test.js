var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { hasTicketTypeForShowtime } from '../has-ticket-type-for-showtime';
import { exampleOrderShowtime } from './../../../../../../example-data/order/order-showtime';
import { exampleStandardTicketType1 } from './../../../../../../example-data/order/order-ticket-type';
import { orderStoreInstance } from './../../order-store';
describe('hasTicketTypeForShowtime', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns false if the showtime does not exist in the order', function () {
        var result = hasTicketTypeForShowtime('123', '456');
        expect(result).toEqual(false);
    });
    it('returns false if the showtime ticketTypes are undefined', function () {
        var showtime = __assign({}, exampleOrderShowtime, { ticketTypes: undefined });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = hasTicketTypeForShowtime(showtime.showtimeId, '456');
        expect(result).toEqual(false);
    });
    it('returns false if the showtime ticketTypes are an empty map', function () {
        var showtime = __assign({}, exampleOrderShowtime, { ticketTypes: new Map() });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = hasTicketTypeForShowtime(showtime.showtimeId, '456');
        expect(result).toEqual(false);
    });
    it('returns true if the showtime has the given ticketType', function () {
        var showtime = __assign({}, exampleOrderShowtime, { ticketTypes: new Map([
                [exampleStandardTicketType1.key, exampleStandardTicketType1]
            ]) });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = hasTicketTypeForShowtime(showtime.showtimeId, exampleStandardTicketType1.key);
        expect(result).toEqual(true);
    });
});
//# sourceMappingURL=has-ticket-type-for-showtime.test.js.map