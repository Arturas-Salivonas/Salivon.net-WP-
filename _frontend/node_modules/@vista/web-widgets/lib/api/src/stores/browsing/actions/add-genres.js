import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var addGenres = action(function (genres) {
    if (genres.length === 0)
        return;
    browsingStoreInstance.data.genresById.merge(genres.map(function (genre) { return [genre.id, genre]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=add-genres.js.map