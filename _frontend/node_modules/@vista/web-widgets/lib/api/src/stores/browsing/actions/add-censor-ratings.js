import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var addCensorRatings = action(function (censorRatings) {
    if (censorRatings.length === 0)
        return;
    browsingStoreInstance.data.censorRatingsById.merge(censorRatings.map(function (censorRating) { return [censorRating.id, censorRating]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=add-censor-ratings.js.map