var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { getSelectedTicketsCountForShowtime } from '../get-selected-tickets-count-for-showtime';
import { exampleOrderShowtime } from './../../../../../../example-data/order/order-showtime';
import { exampleStandardTicket1, exampleStandardTicket2 } from './../../../../../../example-data/order/order-ticket';
import { orderStoreInstance } from './../../order-store';
describe('getSelectedTicketsCountForShowtime', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('throws an error if the showtime does not exist in the order', function () {
        expect(function () { return getSelectedTicketsCountForShowtime('123'); }).toThrow();
    });
    it('returns 0 if the showtime has no selected tickets', function () {
        var showtime = __assign({}, exampleOrderShowtime, { tickets: [] });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = getSelectedTicketsCountForShowtime(showtime.showtimeId);
        expect(result).toEqual(0);
    });
    it('returns the count of selected tickets', function () {
        var showtime = __assign({}, exampleOrderShowtime, { tickets: [
                __assign({}, exampleStandardTicket1, { quantity: 1 }),
                __assign({}, exampleStandardTicket2, { quantity: 2 }),
            ] });
        orderStoreInstance.data.order.showtimes = [showtime];
        var result = getSelectedTicketsCountForShowtime(showtime.showtimeId);
        expect(result).toEqual(3);
    });
});
//# sourceMappingURL=get-selected-tickets-count-for-showtime.test.js.map