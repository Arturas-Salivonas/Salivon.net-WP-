import { orderStoreInstance } from './../order-store';
export function getTicketTypesForShowtime(showtimeId) {
    var showtime = orderStoreInstance.selectors.getShowtimeById(showtimeId);
    if (!showtime.ticketTypes)
        throw new Error("Showtime " + showtimeId + " has no ticket types. Cannot get ticket type by key");
    return showtime.ticketTypes;
}
//# sourceMappingURL=get-ticket-types-for-showtime.js.map