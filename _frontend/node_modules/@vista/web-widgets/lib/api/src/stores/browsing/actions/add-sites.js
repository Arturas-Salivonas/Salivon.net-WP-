import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var addSites = action(function (sites) {
    if (sites.length === 0)
        return;
    browsingStoreInstance.data.sitesById.merge(sites.map(function (site) { return [site.id, site]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=add-sites.js.map