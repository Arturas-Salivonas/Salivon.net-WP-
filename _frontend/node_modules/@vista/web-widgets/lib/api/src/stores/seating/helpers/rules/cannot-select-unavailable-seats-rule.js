import { seatIsUnavailable } from '../utils/seat-utils';
import { SeatSelectionRuleName } from './seat-selection-rule';
export var cannotSelectUnavailableSeatsRule = function () { return ({
    name: SeatSelectionRuleName.CannotSelectUnavailableSeats,
    preventSeatSelection: true,
    validate: function (seatLayoutStateManager) {
        var hasSelectedUnavailableSeats = seatLayoutStateManager.getSelectedSeats()
            .some(function (seat) { return seatIsUnavailable(seat); });
        return {
            ruleName: this.name,
            valid: !hasSelectedUnavailableSeats
        };
    }
}); };
//# sourceMappingURL=cannot-select-unavailable-seats-rule.js.map