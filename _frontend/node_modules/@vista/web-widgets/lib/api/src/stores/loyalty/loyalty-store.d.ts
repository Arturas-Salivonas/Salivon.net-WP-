import { LoyaltyStoreData } from '../../../../types/stores/loyalty-store-data';
import { BaseStore } from '../base-store';
import { LoyaltyStoreActions } from './actions/actions';
import { LoyaltyStoreSelectors } from './selectors/selectors';
/**
 * User Context Store class
 */
export declare class LoyaltyStore extends BaseStore<LoyaltyStoreData> {
    constructor();
    /**
     * Selector methods for the Loyalty Store.
     */
    readonly selectors: LoyaltyStoreSelectors;
    /**
     * Actions for the Loyalty Store.
     */
    readonly actions: LoyaltyStoreActions;
}
export declare const loyaltyStoreInstance: LoyaltyStore;
