import { orderStoreInstance } from '../../order-store';
import { getOrderId } from '../get-order-id';
describe('getOrderId', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns undefined if the order does not have an order id', function () {
        orderStoreInstance.data.order.orderId = undefined;
        var result = getOrderId();
        expect(result).toEqual(undefined);
    });
    it('returns the current order id', function () {
        orderStoreInstance.data.order.orderId = '123';
        var result = getOrderId();
        expect(result).toEqual('123');
    });
});
//# sourceMappingURL=get-order-id.test.js.map