import { toJS } from 'mobx';
import { examplePeople } from '../../../../../../example-data/browsing/person';
import { browsingStoreInstance } from '../../browsing-store';
import { addPeople } from '../add-people';
describe('addPeople', function () {
    it('can add a person to the people store', function () {
        var _a;
        addPeople([examplePeople[0]]);
        expect(toJS(browsingStoreInstance.data.peopleById)).toEqual((_a = {}, _a[examplePeople[0].id] = examplePeople[0], _a));
    });
});
//# sourceMappingURL=add-people.test.js.map