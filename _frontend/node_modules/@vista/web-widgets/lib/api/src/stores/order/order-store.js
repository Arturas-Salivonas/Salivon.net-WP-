var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { defaultOrderStoreData } from '../../../../types/stores/order-store-data';
import { browserSessionServiceInstance } from '../../user/browser-session-service';
import { cacheServiceInstance } from '../../user/cache-service';
import { BaseStore } from '../base-store';
import { orderStoreActions } from './actions/actions';
import { orderStoreSelectors } from './selectors/selectors';
/**
 * Class for storing details about the in-progress order.
 */
var OrderStore = /** @class */ (function (_super) {
    __extends(OrderStore, _super);
    function OrderStore() {
        // enforce defaults via BaseStore constructor
        return _super.call(this, 'order-store', browserSessionServiceInstance, cacheServiceInstance, defaultOrderStoreData) || this;
    }
    Object.defineProperty(OrderStore.prototype, "actions", {
        /**
         * Actions for the Order Store.
         */
        get: function () {
            return orderStoreActions;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(OrderStore.prototype, "selectors", {
        /**
         * Selectors for the Order Store.
         */
        get: function () {
            return orderStoreSelectors;
        },
        enumerable: true,
        configurable: true
    });
    return OrderStore;
}(BaseStore));
export { OrderStore };
export var orderStoreInstance = new OrderStore();
//# sourceMappingURL=order-store.js.map