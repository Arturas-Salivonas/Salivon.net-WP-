import { orderStoreInstance } from '../order-store';
export function getShowtimeById(showtimeId) {
    var showtime = orderStoreInstance.data.order.showtimes.find(function (showtime) { return showtime.showtimeId === showtimeId; });
    if (!showtime)
        throw new Error("Could not find showtime with id " + showtimeId);
    return showtime;
}
//# sourceMappingURL=get-showtime-by-id.js.map