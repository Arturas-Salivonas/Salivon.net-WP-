import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var addShowtimeAttributes = action(function (showtimeAttributes) {
    if (showtimeAttributes.length === 0) {
        return;
    }
    browsingStoreInstance.data.showtimeAttributesById
        .merge(showtimeAttributes.map(function (showtimeAttribute) { return [showtimeAttribute.id, showtimeAttribute]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=add-showtime-attributes.js.map