export function generateShowtimeQueryKey(showtimesQuery) {
    var queryKeyComponents = ['query'];
    if (showtimesQuery.siteIds && showtimesQuery.siteIds.length > 0) {
        var sitesComponent = 'sites-' + showtimesQuery.siteIds.sort().join(',');
        queryKeyComponents.push(sitesComponent);
    }
    if (showtimesQuery.filmIds && showtimesQuery.filmIds.length > 0) {
        var filmsComponent = 'films-' + showtimesQuery.filmIds.sort().join(',');
        queryKeyComponents.push(filmsComponent);
    }
    return queryKeyComponents.join('_');
}
//# sourceMappingURL=query-key-generator.js.map