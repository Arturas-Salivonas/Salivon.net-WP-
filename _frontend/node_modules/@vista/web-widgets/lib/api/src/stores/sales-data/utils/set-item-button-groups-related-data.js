import { salesDataStoreInstance } from '../sales-data-store';
export var setItemButtonGroupsRelatedData = function (relatedData) {
    if (!relatedData) {
        return;
    }
    var itemModels = relatedData.itemModels;
    (itemModels || []).forEach(function (itemModel) {
        salesDataStoreInstance.data.itemModelsById.set(itemModel.id, itemModel);
    });
};
//# sourceMappingURL=set-item-button-groups-related-data.js.map