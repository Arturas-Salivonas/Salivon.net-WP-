import { browsingStoreInstance } from '../browsing-store';
export var getUnselectedSites = function () {
    var _a = browsingStoreInstance.data, selectedSiteIds = _a.selectedSiteIds, sitesById = _a.sitesById;
    var selectedSitesSet = new Set(selectedSiteIds);
    var sitesArray = Array.from(sitesById.values());
    return sitesArray.filter(function (site) { return !selectedSitesSet.has(site.id); });
};
//# sourceMappingURL=get-unselected-sites.js.map