import { OrderTicket } from '../../../../../types/order/order-ticket';
import { TicketType } from '../../../../../types/sales-data/ticket-type';
export declare enum AllowedTicketQuantityStatus {
    NotAllowed = 0,
    Allowed = 1,
    Invalid = 2
}
export interface AllowedTicketQuantity {
    status: AllowedTicketQuantityStatus;
    quantity: number;
}
/**
 * Returns the allowed ticket quantity of a given ticket.
 * Todo: This should possibly be moved out of the api directory as it is a UI concern only
 */
export declare function getAllowedTicketQuantity(showtimeId: string, ticket: OrderTicket, ticketType: TicketType): AllowedTicketQuantity;
