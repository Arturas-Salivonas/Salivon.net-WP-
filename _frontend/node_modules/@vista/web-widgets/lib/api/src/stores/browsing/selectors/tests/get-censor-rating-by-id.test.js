import { toJS } from 'mobx';
import { browsingStoreInstance } from '../../browsing-store';
import { exampleCensorRating } from './../../../../../../example-data/browsing/censor-rating';
import { getCensorRatingById } from './../get-censor-rating-by-id';
describe('getCensorRatingById', function () {
    beforeAll(function () {
        browsingStoreInstance.setData({
            censorRatingsById: new Map([[exampleCensorRating.id, exampleCensorRating]])
        });
    });
    it('can get a censor rating by id', function () {
        var result = getCensorRatingById(exampleCensorRating.id);
        expect(toJS(result)).toEqual(exampleCensorRating);
    });
    it('returns undefined if the censor rating isn\'t present', function () {
        var result = getCensorRatingById('UNKNOWN');
        expect(result).toBeUndefined();
    });
});
//# sourceMappingURL=get-censor-rating-by-id.test.js.map