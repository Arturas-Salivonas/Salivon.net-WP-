import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var addSelectedSite = action(function (siteId) {
    var selectedSiteIds = browsingStoreInstance.data.selectedSiteIds;
    if (selectedSiteIds.find(function (s) { return s === siteId; }))
        return;
    if (browsingStoreInstance.selectors.hasMaximumSelectedSites()) {
        throw new Error("Maximum number of selected sites reached. Cannot add site with ID " + siteId);
    }
    selectedSiteIds.push(siteId);
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=add-selected-site.js.map