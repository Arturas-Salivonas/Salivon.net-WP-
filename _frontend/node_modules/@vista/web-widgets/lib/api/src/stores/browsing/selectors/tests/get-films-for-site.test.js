import { getFilmsForSite } from '../get-films-for-site';
import { exampleFilms } from './../../../../../../example-data/browsing/film';
import { getJsValue } from './../../../../utils/get-js-value';
import { browsingStoreInstance } from './../../browsing-store';
describe('getFilmsForSite', function () {
    var siteId = '123';
    beforeEach(function () {
        browsingStoreInstance.flush();
    });
    it('returns the films for the given site', function () {
        browsingStoreInstance.actions.setFilmsForSite(siteId, exampleFilms);
        var films = getFilmsForSite(siteId);
        expect(getJsValue(films)).toEqual(getJsValue(exampleFilms));
    });
    it('returns undefined if the films haven\'t been loaded for the given site', function () {
        var films = getFilmsForSite(siteId);
        expect(films).toEqual(undefined);
    });
});
//# sourceMappingURL=get-films-for-site.test.js.map