import { toJS } from 'mobx';
import { exampleSites } from '../../../../../../example-data/browsing/site';
import { browsingStoreInstance } from '../../browsing-store';
import { setSites } from '../set-sites';
describe('setSites', function () {
    var exampleSiteOne = exampleSites[0], exampleSiteTwo = exampleSites[1], exampleSiteThree = exampleSites[2];
    afterEach(function () {
        browsingStoreInstance.flush();
    });
    it('can add sites to the sites store', function () {
        var _a;
        setSites([exampleSiteOne, exampleSiteTwo]);
        expect(toJS(browsingStoreInstance.data.sitesById)).toEqual((_a = {}, _a[exampleSiteOne.id] = exampleSiteOne, _a[exampleSiteTwo.id] = exampleSiteTwo, _a));
    });
    it('replaces all sites in the sites store', function () {
        var _a;
        browsingStoreInstance.data.sitesById.set(exampleSiteTwo.id, exampleSiteTwo);
        setSites([exampleSiteOne, exampleSiteThree]);
        expect(toJS(browsingStoreInstance.data.sitesById)).toEqual((_a = {}, _a[exampleSiteOne.id] = exampleSiteOne, _a[exampleSiteThree.id] = exampleSiteThree, _a));
    });
});
//# sourceMappingURL=set-sites.test.js.map