import { orderStoreInstance } from '../order-store';
export function getVoucherRedemptionDetails(voucherBarcode) {
    var voucher = orderStoreInstance.selectors.getVoucherByBarcode(voucherBarcode);
    var voucherTickets = orderStoreInstance.selectors.getVoucherTicketsForBarcode(voucherBarcode);
    var max = voucher.ticketRedemptions.remainingRedemptions;
    var used = voucherTickets.reduce(function (acc, ticket) { return acc + ticket.quantity; }, 0);
    var remaining = max - used;
    return {
        max: max,
        used: used,
        remaining: remaining
    };
}
//# sourceMappingURL=get-voucher-redemption-details.js.map