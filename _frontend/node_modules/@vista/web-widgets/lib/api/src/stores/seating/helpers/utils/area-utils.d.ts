import { Area } from '../../../../../../types/seating/area';
import { AreaCategory } from '../../../../../../types/seating/area-category';
import { SeatLayout } from '../../../../../../types/seating/seat-layout';
/**
 * Finds and returns an area within a seat layout by its area number.
 */
export declare const getAreaByNumber: (areaNumber: number, seatLayout: SeatLayout) => Area | undefined;
/**
 * Makes a deep copy of a given area category.
 */
export declare const cloneAreaCategory: (areaCategory: AreaCategory) => AreaCategory;
