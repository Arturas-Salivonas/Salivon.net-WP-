import { toJS } from 'mobx';
import { exampleSite, exampleSites } from '../../../../../../example-data/browsing/site';
import { browsingStoreInstance } from '../../browsing-store';
import { getSiteById } from '../get-site-by-id';
describe('getSiteById', function () {
    beforeAll(function () {
        browsingStoreInstance.actions.setSites(exampleSites);
    });
    it('can get a site by id', function () {
        var site = getSiteById(exampleSite.id);
        expect(toJS(site)).toEqual(exampleSite);
    });
    it('returns undefined if the site isn\'t present', function () {
        var site = getSiteById('UNKNOWN');
        expect(site).toBeUndefined();
    });
});
//# sourceMappingURL=get-site-by-id.test.js.map