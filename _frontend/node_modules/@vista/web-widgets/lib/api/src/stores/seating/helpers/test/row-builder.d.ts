import { Area } from '../../../../../../types/seating/area';
import { AreaCategory } from '../../../../../../types/seating/area-category';
import { Row } from '../../../../../../types/seating/row';
import { Seat } from '../../../../../../types/seating/seat';
export declare class RowBuilder {
    private readonly areaCategory;
    private readonly area;
    private readonly row;
    constructor(areaCategory: AreaCategory, area: Area, row: Row);
    addSeat(seatProps: Partial<Seat>): this;
    addSofa(seatsInGroup: Partial<Seat>[]): this;
    addWheelchair(seatProps: {
        wheelchairSeat: Partial<Seat>;
        companionSeats?: Partial<Seat>[];
    }): this;
    addAisle(): this;
    private addSeatToLayout;
}
