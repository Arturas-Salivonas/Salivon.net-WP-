import { orderStoreInstance } from '../order-store';
export function getVoucherByBarcode(voucherBarcode) {
    var voucher = orderStoreInstance.data.order.vouchers[voucherBarcode];
    if (!voucher)
        throw new Error("Could not find voucher with barcode " + voucherBarcode);
    return voucher;
}
//# sourceMappingURL=get-voucher-by-barcode.js.map