import { SeatSelectionRuleName } from './seat-selection-rule';
var noSeatsSelected = function (seatLayoutStateManager) {
    return seatLayoutStateManager.getSelectedSeats().length === 0;
};
export var mustSelectAtLeastOneSeatRule = function () { return ({
    name: SeatSelectionRuleName.MustSelectAtLeastOneSeat,
    preventSeatSelection: false,
    validate: function (seatLayoutStateManager) {
        var valid = seatLayoutStateManager.isSeatFirstOrdering
            ? !noSeatsSelected(seatLayoutStateManager)
            : true;
        return {
            ruleName: this.name,
            valid: valid
        };
    }
}); };
//# sourceMappingURL=must-select-at-leat-one-seat-rule.js.map