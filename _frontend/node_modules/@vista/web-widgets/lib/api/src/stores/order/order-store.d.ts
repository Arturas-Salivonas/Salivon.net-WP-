import { OrderStoreData } from '../../../../types/stores/order-store-data';
import { BaseStore } from '../base-store';
import { OrderStoreActions } from './actions/actions';
import { OrderStoreSelectors } from './selectors/selectors';
/**
 * Class for storing details about the in-progress order.
 */
export declare class OrderStore extends BaseStore<OrderStoreData> {
    constructor();
    /**
     * Actions for the Order Store.
     */
    readonly actions: OrderStoreActions;
    /**
     * Selectors for the Order Store.
     */
    readonly selectors: OrderStoreSelectors;
}
export declare const orderStoreInstance: OrderStore;
