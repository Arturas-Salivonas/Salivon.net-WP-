import { exampleOrderCustomerDetails } from '../../../../../../example-data/order/order-customer-details';
import { updateCustomerDetails } from '../../actions/update-customer-details';
import { orderStoreInstance } from '../../order-store';
import { hasCustomerDetails } from '../has-customer-details';
describe('hasCustomerDetails', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns false when no value is set', function () {
        updateCustomerDetails({
            email: undefined,
            familyName: null,
            givenName: '',
        });
        var result = hasCustomerDetails.get();
        expect(result).toEqual(false);
    });
    it('returns true when customer details are set', function () {
        updateCustomerDetails(exampleOrderCustomerDetails);
        var result = hasCustomerDetails.get();
        expect(result).toEqual(true);
    });
    it('returns true when a single value has been set', function () {
        updateCustomerDetails({ givenName: 'test' });
        var result = hasCustomerDetails.get();
        expect(result).toEqual(true);
    });
});
//# sourceMappingURL=has-customer-details.test.js.map