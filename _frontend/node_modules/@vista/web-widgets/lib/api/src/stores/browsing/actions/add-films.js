import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var addFilms = action(function (films) {
    if (films.length === 0)
        return;
    browsingStoreInstance.data.filmsById.merge(films.map(function (film) { return [film.id, film]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=add-films.js.map