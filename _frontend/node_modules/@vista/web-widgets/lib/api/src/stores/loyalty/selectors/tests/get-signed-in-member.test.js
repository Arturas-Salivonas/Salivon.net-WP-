import { exampleLoyaltyStoreDataSignedIn } from '../../../../../../example-data/loyalty/loyalty-store-data';
import { loyaltyStoreInstance } from '../../loyalty-store';
import { exampleLoyaltyStoreDataSignedOut } from './../../../../../../example-data/loyalty/loyalty-store-data';
import { getSignedInMember } from './../get-signed-in-member';
describe('getSignedInMember', function () {
    afterEach(function () {
        loyaltyStoreInstance.flush();
    });
    it('returns the signed in member', function () {
        loyaltyStoreInstance.setData(exampleLoyaltyStoreDataSignedIn);
        var member = getSignedInMember();
        expect(member).toEqual(exampleLoyaltyStoreDataSignedIn.signedInMemberDetails);
    });
    it('returns undefined if the member is not logged in', function () {
        loyaltyStoreInstance.setData(exampleLoyaltyStoreDataSignedOut);
        var member = getSignedInMember();
        expect(member).toEqual(undefined);
    });
});
//# sourceMappingURL=get-signed-in-member.test.js.map