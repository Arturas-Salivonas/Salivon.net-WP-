import { RowBuilder } from './row-builder';
var AreaBuilder = /** @class */ (function () {
    function AreaBuilder(areaCategory, area) {
        this.areaCategory = areaCategory;
        this.area = area;
    }
    AreaBuilder.prototype.setDescription = function (description) {
        this.area.description = description;
        return this;
    };
    AreaBuilder.prototype.addRow = function (rowLabel, rowBuilderFn) {
        var row = {
            rowLabel: rowLabel,
            areaNumber: this.area.number,
            seats: [],
            rowIndex: this.area.rowCount
        };
        this.area.rows.push(row);
        this.area.rowCount++;
        var rowBuilder = new RowBuilder(this.areaCategory, this.area, row);
        rowBuilderFn(rowBuilder);
        return this;
    };
    return AreaBuilder;
}());
export { AreaBuilder };
//# sourceMappingURL=area-builder.js.map