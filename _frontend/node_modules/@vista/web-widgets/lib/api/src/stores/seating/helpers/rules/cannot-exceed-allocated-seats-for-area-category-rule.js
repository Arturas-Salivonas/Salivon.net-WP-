import { SeatSelectionRuleName } from './seat-selection-rule';
var hasExceededAreaCategoryAllocationLimit = function (seatLayoutStateManager) {
    return seatLayoutStateManager.areaCategories.some(function (ac) { return ac.selectedSeats.length > ac.seatsToAllocate; });
};
export var cannotExceedAllocatedSeatsForAreaCategoryRule = function () { return ({
    name: SeatSelectionRuleName.CannotExceedAllocatedSeatsForAreaCategory,
    preventSeatSelection: true,
    validate: function (seatLayoutStateManager) {
        var valid = seatLayoutStateManager.isSeatFirstOrdering
            ? true
            : !hasExceededAreaCategoryAllocationLimit(seatLayoutStateManager);
        return {
            ruleName: this.name,
            valid: valid
        };
    }
}); };
//# sourceMappingURL=cannot-exceed-allocated-seats-for-area-category-rule.js.map