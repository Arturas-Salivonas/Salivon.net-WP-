import { getSelectedSeats } from './seat-selection-utils';
export var getSeatLayoutAutoAllocationStatus = function (seatLayout) {
    if (seatLayout.areaCategories
        && seatLayout.areaCategories.every(function (areaCategory) { return areaCategory.seatsNotAllocatedCount === 0; })
        && !seatLayout.areaCategories.every(function (areaCategory) { return areaCategory.seatsToAllocate === 0; })) {
        return {
            autoAllocated: true,
            seatPositions: getSelectedSeats(seatLayout).map(function (seat) { return seat.position; }) || []
        };
    }
    else {
        return {
            autoAllocated: false
        };
    }
};
//# sourceMappingURL=seat-layout-utils.js.map