import { exampleLoyaltyStoreDataSignedIn } from '../../../../../../example-data/loyalty/loyalty-store-data';
import { loyaltyStoreInstance } from '../../loyalty-store';
import { isSignedIn } from '../is-signed-in';
import { exampleLoyaltyStoreDataSignedOut } from './../../../../../../example-data/loyalty/loyalty-store-data';
describe('isSignedIn', function () {
    afterEach(function () {
        loyaltyStoreInstance.flush();
    });
    it('returns true if the member is signed in', function () {
        loyaltyStoreInstance.setData(exampleLoyaltyStoreDataSignedIn);
        var result = isSignedIn();
        expect(result).toEqual(true);
    });
    it('returns false if the member is not signed in', function () {
        loyaltyStoreInstance.setData(exampleLoyaltyStoreDataSignedOut);
        var result = isSignedIn();
        expect(result).toEqual(false);
    });
});
//# sourceMappingURL=is-signed-in.test.js.map