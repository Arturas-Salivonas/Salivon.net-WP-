import { toJS } from 'mobx';
import { exampleSite } from '../../../../../../example-data/browsing/site';
import { browsingStoreInstance } from '../../browsing-store';
import { addSites } from '../add-sites';
describe('addSites', function () {
    it('can add a site to the sites store', function () {
        var _a;
        addSites([exampleSite]);
        expect(toJS(browsingStoreInstance.data.sitesById)).toEqual((_a = {}, _a[exampleSite.id] = exampleSite, _a));
    });
});
//# sourceMappingURL=add-sites.test.js.map