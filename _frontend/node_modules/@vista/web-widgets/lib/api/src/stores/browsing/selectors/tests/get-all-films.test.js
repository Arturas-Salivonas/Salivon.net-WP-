import { exampleFilms } from '../../../../../../example-data/browsing/film';
import { browsingStoreInstance } from '../../browsing-store';
import { getAllFilms } from '../get-all-films';
import { getJsValue } from './../../../../utils/get-js-value';
describe('getAllFilms', function () {
    beforeEach(function () {
        browsingStoreInstance.flush();
    });
    it('can get all films', function () {
        browsingStoreInstance.actions.setFilms(exampleFilms);
        browsingStoreInstance.setData({ allFilmsLoaded: true });
        var films = getAllFilms();
        expect(getJsValue(films)).toEqual(getJsValue(exampleFilms));
    });
    it('returns an empty array when there are no films', function () {
        browsingStoreInstance.setData({ allFilmsLoaded: true });
        var films = getAllFilms();
        expect(films).toEqual([]);
    });
    it('returns undefined when all films have not been loaded', function () {
        browsingStoreInstance.setData({ allFilmsLoaded: false });
        var films = getAllFilms();
        expect(films).toEqual(undefined);
    });
});
//# sourceMappingURL=get-all-films.test.js.map