import { calculateSelectedTicketsCountForShowtime } from '../../../services/order/utils/order-showtime-utils';
import { orderStoreInstance } from '../order-store';
/**
 * Returns the count of selected tickets for a given showtime.
 * Flattens the quantity of package tickets.
 */
export function getSelectedTicketsCountForShowtime(showtimeId) {
    var showtime = orderStoreInstance.selectors.getShowtimeById(showtimeId);
    return calculateSelectedTicketsCountForShowtime(showtime);
}
//# sourceMappingURL=get-selected-tickets-count-for-showtime.js.map