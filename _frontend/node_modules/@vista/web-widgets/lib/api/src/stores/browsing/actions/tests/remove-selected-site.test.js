import { toJS } from 'mobx';
import { removeSelectedSite } from '../remove-selected-site';
import { browsingStoreInstance } from './../../browsing-store';
describe('removeSelectedSite', function () {
    it('can remove a selected site from the browsing store', function () {
        browsingStoreInstance.data.selectedSiteIds = ['1', '2', '3'];
        removeSelectedSite('1');
        expect(toJS(browsingStoreInstance.data.selectedSiteIds)).toEqual(['2', '3']);
    });
});
//# sourceMappingURL=remove-selected-site.test.js.map