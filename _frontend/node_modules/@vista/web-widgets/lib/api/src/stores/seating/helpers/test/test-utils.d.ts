import { Seat } from '../../../../../../types/seating/seat';
import { SeatLayout } from '../../../../../../types/seating/seat-layout';
import { SeatLayoutStateManager } from '../utils/seat-layout-state-manager';
import { RowBuilder } from './row-builder';
export declare const getSeat: (rowLabel: string, seatLabel: string, seatLayout: SeatLayout) => Seat | undefined;
export declare const selectSeat: (rowLabel: string, seatLabel: string, seatLayoutStateManager: SeatLayoutStateManager) => void;
/**
 * Creates a single row seat layout with 'A' as the rows label
 * @param rowBuilderFn
 */
export declare const createSingleRowSeatLayout: (rowBuilderFn: (rowBuilder: RowBuilder) => void) => SeatLayout;
