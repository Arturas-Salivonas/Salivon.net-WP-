import { computed } from 'mobx';
import { orderStoreInstance } from '../order-store';
export var hasCustomerDetails = computed(function () {
    var customerDetails = orderStoreInstance.data.customerDetails;
    return Object.values(customerDetails).some(function (detail) { return !!detail; });
});
//# sourceMappingURL=has-customer-details.js.map