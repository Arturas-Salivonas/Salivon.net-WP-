import { orderStoreInstance } from '../order-store';
export function hasTicketTypeForShowtime(showtimeId, ticketTypeKey) {
    var showtime = orderStoreInstance.data.order.showtimes.find(function (showtime) { return showtime.showtimeId === showtimeId; });
    return !!(showtime && showtime.ticketTypes && showtime.ticketTypes.has(ticketTypeKey));
}
//# sourceMappingURL=has-ticket-type-for-showtime.js.map