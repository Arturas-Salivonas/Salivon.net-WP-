import { isLoyaltyTicketType } from '../../../services/order/utils/ticket-type-utils';
import { orderStoreInstance } from '../order-store';
export var hasAnyLoyaltyTickets = function () {
    var showtimes = orderStoreInstance.data.order.showtimes;
    return !!showtimes.find(showtimeHasAnyLoyaltyTickets);
};
var showtimeHasAnyLoyaltyTickets = function (showtime) {
    return !!showtime.tickets.find(function (ticket) {
        if (ticket.quantity === 0)
            return false;
        var ticketType = orderStoreInstance.selectors.getTicketTypeByKey(showtime.showtimeId, ticket.ticketTypeKey);
        return isLoyaltyTicketType(ticketType);
    });
};
//# sourceMappingURL=has-any-loyalty-tickets.js.map