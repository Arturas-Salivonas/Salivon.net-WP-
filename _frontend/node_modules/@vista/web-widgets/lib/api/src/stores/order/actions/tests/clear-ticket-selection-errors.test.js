import { orderStoreInstance } from '../../order-store';
import { clearTicketSelectionErrors } from '../clear-ticket-selection-errors';
describe('clearTicketSelectionErrors', function () {
    it('calls setData correctly', function () {
        orderStoreInstance.setData = jest.fn();
        clearTicketSelectionErrors();
        expect(orderStoreInstance.setData).toBeCalledWith({ ticketSelectionErrors: [] });
    });
});
//# sourceMappingURL=clear-ticket-selection-errors.test.js.map