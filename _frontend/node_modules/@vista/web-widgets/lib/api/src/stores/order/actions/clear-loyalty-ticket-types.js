import { action } from 'mobx';
import { isLoyaltyTicketType } from '../../../services/order/utils/ticket-type-utils';
import { orderStoreInstance } from '../order-store';
export var clearLoyaltyTicketTypes = action(function () {
    var showtimes = orderStoreInstance.data.order.showtimes;
    showtimes.forEach(clearLoyaltyTicketTypesForShowtime);
});
export var clearLoyaltyTicketTypesForShowtime = function (showtime) {
    if (!showtime.ticketTypes)
        return;
    var ticketTypes = showtime.ticketTypes;
    var nonLoyaltyTicketTypes = Array.from(ticketTypes.values()).filter(function (ticketType) { return !isLoyaltyTicketType(ticketType); });
    orderStoreInstance.actions.setTicketTypesForShowtime(showtime.showtimeId, nonLoyaltyTicketTypes);
};
//# sourceMappingURL=clear-loyalty-ticket-types.js.map