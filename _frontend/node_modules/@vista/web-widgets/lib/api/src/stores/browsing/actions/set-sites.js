import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var setSites = action(function (sites) {
    browsingStoreInstance.data.sitesById.replace(sites.map(function (site) { return [site.id, site]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=set-sites.js.map