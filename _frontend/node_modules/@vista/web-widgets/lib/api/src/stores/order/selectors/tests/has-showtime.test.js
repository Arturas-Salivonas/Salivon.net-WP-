import { exampleOrderShowtime } from '../../../../../../example-data/order/order-showtime';
import { orderStoreInstance } from '../../order-store';
import { hasShowtime } from '../has-showtime';
describe('hasShowtime', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns false if the showtime does not exist in the order', function () {
        var result = hasShowtime('123');
        expect(result).toEqual(false);
    });
    it('returns true if the showtime does exist in the order', function () {
        orderStoreInstance.data.order.showtimes = [exampleOrderShowtime];
        var result = hasShowtime(exampleOrderShowtime.showtimeId);
        expect(result).toEqual(true);
    });
});
//# sourceMappingURL=has-showtime.test.js.map