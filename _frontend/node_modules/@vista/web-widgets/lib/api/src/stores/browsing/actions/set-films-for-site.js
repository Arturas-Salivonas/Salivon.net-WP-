import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var setFilmsForSite = action(function (siteId, films) {
    browsingStoreInstance.data.filmsBySiteId.set(siteId, films);
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=set-films-for-site.js.map