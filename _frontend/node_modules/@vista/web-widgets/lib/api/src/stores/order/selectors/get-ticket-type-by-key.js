import { orderStoreInstance } from '../order-store';
export function getTicketTypeByKey(showtimeId, ticketTypeKey) {
    var ticketTypes = orderStoreInstance.selectors.getTicketTypesForShowtime(showtimeId);
    var ticketType = ticketTypes.get(ticketTypeKey);
    if (!ticketType)
        throw new Error("Could not find ticket type with key " + ticketTypeKey);
    return ticketType;
}
//# sourceMappingURL=get-ticket-type-by-key.js.map