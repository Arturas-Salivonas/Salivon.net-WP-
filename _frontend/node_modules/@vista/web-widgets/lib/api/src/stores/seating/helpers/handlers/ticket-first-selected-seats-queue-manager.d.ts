import { Seat } from '../../../../../../types/seating/seat';
import { SeatLayout } from '../../../../../../types/seating/seat-layout';
import { SelectedSeatsQueueManager } from './selected-seats-queue-manager';
export declare class TicketFirstSelectedSeatsQueueManager implements SelectedSeatsQueueManager {
    private nextSelectedSeatsQueue;
    private currentSelectedSeatsQueue;
    constructor(seatLayout: SeatLayout);
    isSeatSelected(seat: Seat): boolean;
    selectSeat(seat: Seat): void;
    deselectSeat(seat: Seat): void;
    popSeatToAccomodateNewSeat(newSeat: Seat): Seat | undefined;
    saveChanges(): void;
    discardChanges(): void;
    private getNextSelectedSeatsQueue;
    private cloneSelectedSeats;
    private buildSelectedSeatsQueueFromLayout;
}
