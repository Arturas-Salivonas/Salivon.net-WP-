import { action } from 'mobx';
import { orderStoreInstance } from '../order-store';
/**
 * Updates the customer details currently associated with the order.
 * @param {Partial<OrderCustomerDetails>} customerDetails The customer details to be updated.
 */
export var updateCustomerDetails = action(function (customerDetails) {
    // Use Object.assign so that the customer details object does not change, preventing unnecessary notification to observers of customer details' properties.
    Object.assign(orderStoreInstance.data.customerDetails, customerDetails);
    orderStoreInstance.syncToCache();
});
//# sourceMappingURL=update-customer-details.js.map