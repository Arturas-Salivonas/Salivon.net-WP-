import { action } from 'mobx';
import { orderStoreInstance } from '../order-store';
/**
 * Adds a voucher to the order if it has not already been added.
 */
export var addVoucher = action(function (voucher) {
    if (orderStoreInstance.selectors.hasVoucher(voucher.barcode))
        return;
    orderStoreInstance.data.order.vouchers[voucher.barcode] = voucher;
    orderStoreInstance.syncToCache();
});
//# sourceMappingURL=add-voucher.js.map