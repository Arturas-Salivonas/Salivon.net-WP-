import { action } from 'mobx';
import { orderStoreInstance } from '../order-store';
/**
 * Add or update seats selected during a ticketing flow.
 */
export var setSeatsForShowtime = action(function (showtimeId, seats) {
    var showtime = orderStoreInstance.selectors.getShowtimeById(showtimeId);
    showtime.seats = seats;
    orderStoreInstance.syncToCache();
});
//# sourceMappingURL=set-seats-for-showtime.js.map