import { toJS } from 'mobx';
import { exampleGenres } from '../../../../../../example-data/browsing/genre';
import { browsingStoreInstance } from '../../browsing-store';
import { addGenres } from '../add-genres';
describe('addGenres', function () {
    it('can add a genre to the genres store', function () {
        var _a;
        addGenres([exampleGenres[0]]);
        expect(toJS(browsingStoreInstance.data.genresById)).toEqual((_a = {}, _a[exampleGenres[0].id] = exampleGenres[0], _a));
    });
});
//# sourceMappingURL=add-genres.test.js.map