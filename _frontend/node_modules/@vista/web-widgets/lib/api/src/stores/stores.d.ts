import { BrowsingStore } from './browsing/browsing-store';
import { ConfigurationStore } from './configuration/configuration-store';
import { JourneyStore } from './journey/journey-store';
import { LoyaltyStore } from './loyalty/loyalty-store';
import { OrderStore } from './order/order-store';
import { SalesDataStore } from './sales-data/sales-data-store';
import { SeatingStore } from './seating/seating-store';
export interface Stores {
    browsing: BrowsingStore;
    order: OrderStore;
    seating: SeatingStore;
    configuration: ConfigurationStore;
    loyalty: LoyaltyStore;
    salesData: SalesDataStore;
    journey: JourneyStore;
}
export declare const stores: Stores;
/**
 * Hydrates the data for each store based on the cached state inside local storage
 */
export declare function hydrateStores(): void;
