import { action } from 'mobx';
import { browsingStoreInstance } from '../browsing-store';
export var setFilms = action(function (films) {
    browsingStoreInstance.data.filmsById.replace(films.map(function (film) { return [film.id, film]; }));
    browsingStoreInstance.syncToCache();
});
//# sourceMappingURL=set-films.js.map