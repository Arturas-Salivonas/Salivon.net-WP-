import { toJS } from 'mobx';
import { exampleShowtimes } from '../../../../../../example-data/browsing/showtimes';
import { browsingStoreInstance } from '../../browsing-store';
import { addShowtimes } from '../add-showtimes';
describe('addShowtimes', function () {
    it('can add a showtime to the showtimes store', function () {
        var _a;
        addShowtimes([exampleShowtimes[0]]);
        expect(toJS(browsingStoreInstance.data.showtimesById)).toEqual((_a = {}, _a[exampleShowtimes[0].id] = exampleShowtimes[0], _a));
    });
});
//# sourceMappingURL=add-showtimes.test.js.map