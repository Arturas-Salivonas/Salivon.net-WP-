import { orderStoreInstance } from '../../order-store';
import { hasOrderId } from '../has-order-id';
describe('hasOrderId', function () {
    beforeEach(function () {
        orderStoreInstance.flush();
    });
    it('returns false if the order does not have an order id', function () {
        orderStoreInstance.data.order.orderId = undefined;
        var result = hasOrderId();
        expect(result).toEqual(false);
    });
    it('returns true if the order does have an order id', function () {
        orderStoreInstance.data.order.orderId = '123';
        var result = hasOrderId();
        expect(result).toEqual(true);
    });
});
//# sourceMappingURL=has-order-id.test.js.map