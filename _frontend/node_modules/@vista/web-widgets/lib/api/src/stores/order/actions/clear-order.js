import { action } from 'mobx';
import { loyaltyStoreInstance } from '../../loyalty/loyalty-store';
import { seatingStoreInstance } from '../../seating/seating-store';
import { orderStoreInstance } from '../order-store';
import { defaultOrderStoreData } from './../../../../../types/stores/order-store-data';
/**
 * Clears up order data after order completion.
 */
export var clearOrder = action(function () {
    var orderStoreData = defaultOrderStoreData;
    // Preserve customer details if signed in
    if (loyaltyStoreInstance.selectors.isSignedIn()) {
        orderStoreData.customerDetails = orderStoreInstance.data.customerDetails;
    }
    orderStoreInstance.setData(orderStoreData);
    seatingStoreInstance.flush();
});
//# sourceMappingURL=clear-order.js.map