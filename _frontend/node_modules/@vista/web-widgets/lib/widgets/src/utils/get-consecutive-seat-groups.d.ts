import { JourneyOrderSeat } from '../../../types/journey/order-seat';
import { OrderSeat } from '../../../types/order/order-seat';
/**
 * Groups consecutive seats in a given array of journey order seats or order seats.
 * Returns an array of seat groups.
 */
export declare function getConsecutiveSeatGroups<T extends JourneyOrderSeat | OrderSeat>(seats: T[]): T[][];
