/**
 * Builds BEM-style classes for blocks and elements with optional modifiers.
 */
export interface BemClassBuilder {
    /**
     * Returns a block class with optional modifiers.
     */
    block: (modifiers?: BemClassBuilderModifiers) => string;
    /**
     * Returns an element class with optional modifiers.
     */
    element: (elementName: string, modifiers?: BemClassBuilderModifiers) => string;
}
/**
 * A set of modifier options for the BEM class builder.
 */
export interface BemClassBuilderModifiers {
    [name: string]: boolean | string | null | undefined;
}
export declare const bemClassBuilderFactory: (blockName: string) => BemClassBuilder;
