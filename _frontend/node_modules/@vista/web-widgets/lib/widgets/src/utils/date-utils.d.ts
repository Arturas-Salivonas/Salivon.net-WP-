export declare const getMinutesUntilDate: (date: Date) => number;
export interface DateCountdown {
    days: number;
    hours: number;
    minutes: number;
    seconds: number;
}
/**
 * Returns the time until a date in days, hours, minutes and seconds.
 */
export declare function getCountdownForDate(date: Date): DateCountdown;
