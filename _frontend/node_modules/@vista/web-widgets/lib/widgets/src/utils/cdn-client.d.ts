import { CdnClient as VistaCdnClient } from '@vista/cdn-client';
/**
 * CDN Client - used to build CDN URLs for media assets (e.g. Film poster images).
 */
export declare const CdnClient: {
    readonly Instance: VistaCdnClient;
};
