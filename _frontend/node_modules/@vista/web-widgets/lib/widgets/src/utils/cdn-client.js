import { CdnClient as VistaCdnClient } from '@vista/cdn-client';
import { configurationStoreInstance } from '../../../api/src/stores/configuration/configuration-store';
/**
 * CDN Client - used to build CDN URLs for media assets (e.g. Film poster images).
 */
export var CdnClient = {
    get Instance() {
        var cdn = configurationStoreInstance.data.cdn;
        return new VistaCdnClient(cdn.url, {
            mediaApiToken: cdn.mediaApiToken,
            referenceScheme: cdn.referenceScheme,
            allowPlaceHolder: true
        });
    }
};
//# sourceMappingURL=cdn-client.js.map