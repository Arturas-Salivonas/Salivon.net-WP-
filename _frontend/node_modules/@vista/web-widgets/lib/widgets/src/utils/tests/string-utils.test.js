import { convertToKebabCase } from '../string-utils';
describe('convertToKebabCase', function () {
    describe('camelCase', function () {
        it('converts camelCase to kebab-case', function () {
            var camelCase = 'camelCase';
            var result = convertToKebabCase(camelCase);
            expect(result).toEqual('camel-case');
        });
        it('converts camelCase to kebab-case for multi-name variables', function () {
            var camelCase = 'camelCaseMultiNameVariables';
            var result = convertToKebabCase(camelCase);
            expect(result).toEqual('camel-case-multi-name-variables');
        });
    });
    describe('PascalCase', function () {
        it('converts PascalCase to kebab-case', function () {
            var pascalCase = 'PascalCase';
            var result = convertToKebabCase(pascalCase);
            expect(result).toEqual('pascal-case');
        });
        it('converts PascalCase to kebab-case for multi-name variables', function () {
            var pascalCase = 'PascalCaseMultiNameVariables';
            var result = convertToKebabCase(pascalCase);
            expect(result).toEqual('pascal-case-multi-name-variables');
        });
    });
});
//# sourceMappingURL=string-utils.test.js.map