export function sortArrayByProperty(array, propertyName) {
    if (!array)
        return [];
    return array.sort(function (a, b) {
        if (a[propertyName] < b[propertyName])
            return -1;
        if (a[propertyName] > b[propertyName])
            return 1;
        return 0;
    });
}
export function convertArrayToMap(array, key) {
    return new Map(array.map(function (value) { return [value[key], value]; }));
}
export function concatObservableArrays(array1, array2) {
    var newArray = [];
    array1.forEach(function (item) { return newArray.push(item); });
    array2.forEach(function (item) { return newArray.push(item); });
    return newArray;
}
//# sourceMappingURL=array-utils.js.map