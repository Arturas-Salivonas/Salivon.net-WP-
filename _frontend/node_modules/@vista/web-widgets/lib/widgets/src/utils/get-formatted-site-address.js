export var getFormattedSiteAddress = function (address) {
    if (!address)
        return undefined;
    var addressComponents = [];
    var line1 = address.line1, line2 = address.line2, city = address.city;
    if (line1)
        addressComponents.push(line1);
    if (line2)
        addressComponents.push(line2);
    if (city)
        addressComponents.push(city);
    return addressComponents.length > 0 ? addressComponents.join(', ') : undefined;
};
//# sourceMappingURL=get-formatted-site-address.js.map