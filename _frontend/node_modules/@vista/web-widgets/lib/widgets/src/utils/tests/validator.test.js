import { isEmptyOrWhitespace } from '../validator';
describe('validator', function () {
    it('validate string is empty or whitespace', function () {
        var emptyString = ' ';
        var nonEmptyString = 'this is not an empty string';
        expect(isEmptyOrWhitespace(emptyString)).toEqual(true);
        expect(isEmptyOrWhitespace(nonEmptyString)).toEqual(false);
    });
});
//# sourceMappingURL=validator.test.js.map