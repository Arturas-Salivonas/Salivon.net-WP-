import { Formatter as VistaFormatter } from '@vista/i18n';
import { configurationStoreInstance } from '../../../api/src/stores/configuration/configuration-store';
/**
 * Formatter
 * Can be used for formatting dates, currency and templates into localised string.
 */
export var Formatter = {
    get Instance() {
        var _a = configurationStoreInstance.data.localisation, languageTag = _a.languageTag, currency = _a.currency, date = _a.date;
        return new VistaFormatter({
            localeTag: languageTag,
            currency: currency,
            date: date
        });
    }
};
//# sourceMappingURL=formatter.js.map