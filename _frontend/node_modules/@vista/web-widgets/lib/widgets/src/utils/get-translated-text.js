import { configurationStoreInstance } from '../../../api/src/stores/configuration/configuration-store';
export function getTranslatedTextOrDefault(object, propertyName) {
    if (!object)
        return undefined;
    var translations = object[propertyName + 'Translations'];
    var defaultText = object[propertyName];
    return getTranslatedText(translations) || defaultText;
}
export function getTranslatedText(translations) {
    if (!translations)
        return undefined;
    var languageTag = configurationStoreInstance.data.localisation.languageTag;
    var translation = translations.find(function (t) { return t.languageTag === languageTag; });
    return translation ? translation.text : undefined;
}
//# sourceMappingURL=get-translated-text.js.map