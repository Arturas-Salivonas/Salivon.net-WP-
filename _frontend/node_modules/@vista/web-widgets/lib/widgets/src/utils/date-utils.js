import { differenceInSeconds } from 'date-fns';
export var getMinutesUntilDate = function (date) {
    return differenceInSeconds(date, Date.now()) / 60;
};
/**
 * Returns the time until a date in days, hours, minutes and seconds.
 */
export function getCountdownForDate(date) {
    var time = date.getTime();
    var currentTime = new Date().getTime();
    var duration = time - currentTime;
    return {
        days: Math.floor(duration / (1000 * 60 * 60 * 24)),
        hours: Math.floor((duration % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)),
        minutes: Math.floor((duration % (1000 * 60 * 60)) / (1000 * 60)),
        seconds: Math.floor((duration % (1000 * 60)) / 1000)
    };
}
//# sourceMappingURL=date-utils.js.map