import { Site } from '../../../../../types/browsing/site';
/**
 * Events for the Site List widget.
 */
export interface SiteListEvents {
    /**
     * Callback function that is called when a site is clicked.
     */
    onSiteClicked?: (site: Site) => void;
}
