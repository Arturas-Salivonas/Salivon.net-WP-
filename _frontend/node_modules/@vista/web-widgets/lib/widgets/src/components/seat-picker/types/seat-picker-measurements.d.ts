export declare type SeatPickerBoundary = SeatPickerDimension & SeatPickerCoordinate;
export interface SeatPickerDimension {
    width: number;
    height: number;
}
export interface SeatPickerCoordinate {
    top: number;
    left: number;
}
