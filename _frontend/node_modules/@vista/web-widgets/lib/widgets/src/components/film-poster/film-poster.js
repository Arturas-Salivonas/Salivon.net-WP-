var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import * as React from 'react';
import { configurationStoreInstance } from '../../../../api/src/stores/configuration/configuration-store';
import { bemClassBuilderFactory } from '../../utils/bem-class-builder';
import { CdnClient } from '../../utils/cdn-client';
import { Formatter } from '../../utils/formatter';
import { getTranslatedTextOrDefault } from '../../utils/get-translated-text';
var FilmPoster = /** @class */ (function (_super) {
    __extends(FilmPoster, _super);
    function FilmPoster() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    FilmPoster.prototype.render = function () {
        var _a = this.props, film = _a.film, linkUrl = _a.linkUrl;
        var b = bemClassBuilderFactory('v-film-poster');
        var posterUrl = CdnClient.Instance.film(film.id).poster();
        var image = React.createElement("img", { className: b.element('img'), src: posterUrl, alt: getFormattedFilmPosterAltText(film) });
        return linkUrl
            ? React.createElement("a", { href: linkUrl, className: b.block() }, image)
            : React.createElement("div", { className: b.block() }, image);
    };
    return FilmPoster;
}(React.Component));
export { FilmPoster };
var getFormattedFilmPosterAltText = function (film) {
    var filmPosterAltTextFormat = configurationStoreInstance.data.localisation.filmPosterAltTextFormat;
    return Formatter.Instance.template(filmPosterAltTextFormat, {
        filmTitle: getTranslatedTextOrDefault(film, 'title'),
    });
};
//# sourceMappingURL=film-poster.js.map