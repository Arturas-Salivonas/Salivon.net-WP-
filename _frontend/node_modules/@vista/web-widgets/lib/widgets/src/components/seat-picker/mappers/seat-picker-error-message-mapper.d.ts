import { SeatingError } from '../../../../../api/src/stores/seating/helpers/types';
import { SeatPickerWidgetDisplayText } from '../types/display-text';
/**
 * Map the error message provided via the API to the display text required by the Seat picker widget.
 */
export declare function mapSeatPickerErrorMessage(error: SeatingError, displayText: SeatPickerWidgetDisplayText): string;
