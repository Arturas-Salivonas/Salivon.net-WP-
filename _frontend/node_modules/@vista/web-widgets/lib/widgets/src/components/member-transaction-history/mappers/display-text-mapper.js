var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultMemberTransactionHistoryDisplayText } from '../types/display-text';
/**
 * Maps the display text provided via the API to the display text required by the Member Transaction History widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapMemberTransactionHistoryDisplayText(displayText) {
    if (!displayText)
        return defaultMemberTransactionHistoryDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Member Transaction History display text');
    if (!valid)
        return defaultMemberTransactionHistoryDisplayText;
    var validatedDisplayText = getValidatedMemberTransactionHistoryDisplayText(displayText);
    return __assign({}, defaultMemberTransactionHistoryDisplayText, validatedDisplayText);
}
/**
 * Validates the display text values provided for the Member Transaction History.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedMemberTransactionHistoryDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'tableColumnDateHeaderLabel', 'Member Transaction History tableColumnDateHeaderLabel');
    validateField(validator, 'tableColumnDetailsHeaderLabel', 'Member Transaction History tableColumnDetailsHeaderLabel');
    validateField(validator, 'tableColumnPointsHeaderLabel', 'Member Transaction History tableColumnPointsHeaderLabel');
    validateField(validator, 'buttonLoadMoreLabel', 'Member Transaction History buttonLoadMoreLabel');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map