var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultTicketPickerAssets } from '../../types/assets';
import { defaultTicketPickerDisplayText } from '../../types/display-text';
import { TicketPickerMessage, TicketPickerMessageType } from '../ticket-picker-message';
describe('TicketPickerMessage', function () {
    it('should render a FailedToLoadData message correctly', function () {
        var testProps = {
            messageType: TicketPickerMessageType.FailedToLoadData,
            displayText: defaultTicketPickerDisplayText,
            assets: defaultTicketPickerAssets
        };
        compareToSnapshot(React.createElement(TicketPickerMessage, __assign({}, testProps)));
    });
});
//# sourceMappingURL=ticket-picker-message.test.js.map