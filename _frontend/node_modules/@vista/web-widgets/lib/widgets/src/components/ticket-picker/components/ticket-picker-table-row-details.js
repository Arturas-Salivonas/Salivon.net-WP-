import * as React from 'react';
import { isVoucherTicket } from '../../../../../api/src/services/order/utils/order-ticket-utils';
import { bemClassBuilderFactory } from '../../../utils/bem-class-builder';
import { getTranslatedTextOrDefault } from '../../../utils/get-translated-text';
import { getTicketSubDescription } from '../utils/order-ticket-utils';
/**
 * Renders the details of a ticket type.
 */
export var TicketPickerTableRowDetails = function (_a) {
    var ticket = _a.ticket, ticketType = _a.ticketType;
    var subDescription = getTicketSubDescription(ticketType, ticket);
    var b = bemClassBuilderFactory('v-ticket-picker-table-row-details');
    return (React.createElement("div", { className: b.block() },
        React.createElement("span", { className: b.element('description') }, getTranslatedTextOrDefault(ticketType, 'description')),
        isVoucherTicket(ticket) &&
            React.createElement("span", { className: b.element('barcode') }, ticket.barcode),
        subDescription &&
            React.createElement("span", { className: b.element('sub-description') }, subDescription)));
};
//# sourceMappingURL=ticket-picker-table-row-details.js.map