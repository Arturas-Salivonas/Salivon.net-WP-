import React from 'react';
export interface TabsProps {
    centered?: boolean;
    defaultSelectedTabIndex?: number;
    children: any;
}
export interface TabsState {
    canScrollLeft: boolean;
    canScrollRight: boolean;
    indicatorStyle: React.CSSProperties;
    selectedTabIndex: number;
}
export declare class Tabs extends React.Component<TabsProps, TabsState> {
    private tablistRef;
    private scrollButtonLeftRef;
    private scrollButtonRightRef;
    private animator?;
    constructor(props: TabsProps);
    render(): JSX.Element;
    componentDidMount(): void;
    componentDidUpdate(_: TabsProps, prevState: TabsState): void;
    componentWillUnmount(): void;
    private handleTabClicked;
    private handleLeftScrollClick;
    private handleRightScrollClick;
    private handleTabsScroll;
    private handleWindowResize;
    private scrollBy;
    private scrollTo;
    private handleScrollAnimationFrame;
    private getSelectedTab;
    private getSelectedTabContent;
    private scrollSelectedTabIntoView;
    private updateScrollState;
    private updateIndicatorStyleState;
}
