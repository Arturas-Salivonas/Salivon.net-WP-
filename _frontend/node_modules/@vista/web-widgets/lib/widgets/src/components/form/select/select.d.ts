import * as React from 'react';
import { FormElementProps } from '../form-element-props';
export interface SelectProps extends FormElementProps<string | number | string[]> {
    icon?: string;
    multiple?: boolean;
}
/**
 * Renders a select component.
 */
export declare class Select extends React.Component<SelectProps> {
    render(): JSX.Element;
    handleChange: (event: any) => void;
}
