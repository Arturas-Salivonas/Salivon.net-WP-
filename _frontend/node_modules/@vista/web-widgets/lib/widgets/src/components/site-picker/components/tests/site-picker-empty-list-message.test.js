var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultSitePickerAssets } from '../../types/assets';
import { defaultSitePickerDisplayText } from '../../types/display-text';
import { SitePickerEmptyListMessage } from '../site-picker-empty-list-message';
describe('SitePickerEmptyListMessage', function () {
    it('should render correctly', function () {
        var testProps = {
            searchTerm: 'search term',
            displayText: defaultSitePickerDisplayText,
            assets: defaultSitePickerAssets
        };
        compareToSnapshot(React.createElement(SitePickerEmptyListMessage, __assign({}, testProps)));
    });
});
//# sourceMappingURL=site-picker-empty-list-message.test.js.map