import { BusinessDate } from './../../../../types/browsing/business-date';
export interface DatePickerDateValue {
    businessDate: BusinessDate;
    isActive: boolean;
    isSelected: boolean;
}
export declare const getDatePickerDateValues: (businessDates: string[], selectedBusinessDate: string) => DatePickerDateValue[];
