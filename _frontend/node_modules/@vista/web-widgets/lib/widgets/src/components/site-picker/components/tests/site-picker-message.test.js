var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultSitePickerAssets } from '../../types/assets';
import { defaultSitePickerDisplayText } from '../../types/display-text';
import { SitePickerMessage, SitePickerMessageType } from '../site-picker-message';
describe('SitePickerMessage', function () {
    it('should render a FailedToLoadData message correctly', function () {
        var testProps = {
            messageType: SitePickerMessageType.FailedToLoadData,
            displayText: defaultSitePickerDisplayText,
            assets: defaultSitePickerAssets
        };
        compareToSnapshot(React.createElement(SitePickerMessage, __assign({}, testProps)));
    });
});
//# sourceMappingURL=site-picker-message.test.js.map