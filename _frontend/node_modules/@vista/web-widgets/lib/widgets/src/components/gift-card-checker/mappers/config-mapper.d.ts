import { NestedPartial } from '../../../../../types/nested-partial';
import { GiftCardCheckerWidgetConfig } from '../types/config';
/**
 * Maps the config provided via the API to the config required by the Gift Card Checker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 * @param config
 */
export declare function mapGiftCardCheckerConfig(config?: NestedPartial<GiftCardCheckerWidgetConfig>): GiftCardCheckerWidgetConfig;
