import * as React from 'react';
import { OrderStore } from '../../../../../api/src/stores/order/order-store';
import { OrderTicket } from '../../../../../types/order/order-ticket';
import { TicketType, TicketTypeKey } from '../../../../../types/sales-data/ticket-type';
import { TicketPickerWidgetConfig } from '../types/config';
export interface TicketPickerTableBodyProps {
    orderStore: OrderStore;
    showtimeId: string;
    config: TicketPickerWidgetConfig;
    onTicketQuantityUpdate: (showtimeId: string, ticketTypeKey: TicketTypeKey, quantity: number, voucherBarcode?: string) => void;
}
export declare class TicketPickerTableBody extends React.Component<TicketPickerTableBodyProps> {
    render(): JSX.Element | null;
    getKeyForTicket(ticket: OrderTicket, ticketType: TicketType): string;
}
