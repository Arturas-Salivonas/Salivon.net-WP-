var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultItemPickerDisplayText } from '../types/display-text';
export function mapItemPickerDisplayText(displayText) {
    if (!displayText)
        return defaultItemPickerDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Item Picker display text');
    if (!valid)
        return defaultItemPickerDisplayText;
    var validatedDisplayText = getValidatedItemPickerDisplayText(displayText);
    return __assign({}, defaultItemPickerDisplayText, validatedDisplayText);
}
function getValidatedItemPickerDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'addToOrderLabel', 'Item Picker addToOrderLabel');
    validateField(validator, 'failedToLoadDataTitle', 'Item Picker failedToLoadDataTitle');
    validateField(validator, 'failedToLoadDataMessage', 'Item Picker failedToLoadDataMessage');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map