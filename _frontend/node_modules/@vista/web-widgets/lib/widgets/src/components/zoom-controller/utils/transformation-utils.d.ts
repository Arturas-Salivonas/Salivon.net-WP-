import { Point } from './measurement-utils';
export interface Transformation {
    scale: number;
    translation: Point;
}
export interface ContentTransformation extends Transformation {
    deltaScale: number;
}
