var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultSeatPickerAssets } from '../../types/assets';
import { defaultSeatPickerDisplayText } from '../../types/display-text';
import { SeatPickerMessage, SeatPickerMessageType } from '../seat-picker-message';
describe('SeatPickerMessage', function () {
    it('should render a FailedToLoadData message correctly', function () {
        var testProps = {
            messageType: SeatPickerMessageType.FailedToLoadData,
            displayText: defaultSeatPickerDisplayText,
            assets: defaultSeatPickerAssets
        };
        compareToSnapshot(React.createElement(SeatPickerMessage, __assign({}, testProps)));
    });
});
//# sourceMappingURL=seat-picker-message.test.js.map