var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultSiteDetailsAssets } from '../../types/assets';
import { defaultSiteDetailsWidgetDisplayText } from '../../types/display-text';
import { SiteDetailsMessage, SiteDetailsMessageType } from '../site-details-message';
describe('SiteDetailsMessage', function () {
    it('should render a FailedToLoadData message correctly', function () {
        var testProps = {
            messageType: SiteDetailsMessageType.FailedToLoadData,
            displayText: defaultSiteDetailsWidgetDisplayText,
            assets: defaultSiteDetailsAssets
        };
        compareToSnapshot(React.createElement(SiteDetailsMessage, __assign({}, testProps)));
    });
});
//# sourceMappingURL=site-details-message.test.js.map