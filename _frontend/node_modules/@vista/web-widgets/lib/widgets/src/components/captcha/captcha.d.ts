import * as React from 'react';
import { CaptchaConfig } from './captcha-config';
/**
 * Props for the Captcha component
 */
export interface CaptchaProps {
    /**
     * Config options for what captcha implementation to render
     */
    config: CaptchaConfig;
    /**
     * Function that is called whenever the captcha updates with a new value
     * This value should be verified server-side to ensure that it is valid
     */
    onValueChanged: (captchaResponse?: string) => void;
}
/**
 * Wrapper component for supporting different captcha implementations
 */
export declare class Captcha extends React.Component<CaptchaProps> {
    private captchaRef;
    reset(): void;
    render(): JSX.Element | null;
}
