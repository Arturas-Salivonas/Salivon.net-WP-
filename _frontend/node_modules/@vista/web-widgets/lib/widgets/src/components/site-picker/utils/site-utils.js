import { getTranslatedTextOrDefault } from '../../../utils/get-translated-text';
export var siteCompareFn = function (a, b) {
    var nameA = getTranslatedTextOrDefault(a, 'name');
    var nameB = getTranslatedTextOrDefault(b, 'name');
    if (!nameA || !nameB)
        return 0;
    if (nameA < nameB) {
        return -1;
    }
    if (nameA > nameB) {
        return 1;
    }
    return 0;
};
//# sourceMappingURL=site-utils.js.map