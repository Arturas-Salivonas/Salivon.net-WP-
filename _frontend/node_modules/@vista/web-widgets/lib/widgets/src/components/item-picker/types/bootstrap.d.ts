import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { ItemPicker } from '../item-picker';
import { ItemPickerAssets } from './assets';
import { ItemPickerDisplayText } from './display-text';
import { ItemPickerEvents } from './events';
/**
 * Bootstrap options to render the Item Picker widget.
 * @export
 * @interface ItemPickerBootstrap
 * @extends {WidgetBootstrap}
 */
export interface ItemPickerBootstrap extends WidgetBootstrap {
    /**
     * Display text for the Item Picker widget.
     */
    displayText?: Partial<ItemPickerDisplayText>;
    /**
     * Events for the Item Picker widget.
     */
    events?: ItemPickerEvents;
    /**
     * Assets for the Item Picker widget.
     */
    assets?: ItemPickerAssets;
}
export declare type ItemPickerBootstrapper = Bootstrapper<ItemPicker, ItemPickerBootstrap>;
