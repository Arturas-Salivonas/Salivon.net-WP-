var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultGiftCardCheckerConfig } from '../types/config';
/**
 * Maps the config provided via the API to the config required by the Gift Card Checker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 * @param config
 */
export function mapGiftCardCheckerConfig(config) {
    if (!config)
        return defaultGiftCardCheckerConfig;
    var valid = new Validator().ofType('object').validate(config, 'Gift card checker config');
    if (!valid)
        return defaultGiftCardCheckerConfig;
    var validatedConfig = getValidatedGiftCardCheckerConfig(config);
    return __assign({}, defaultGiftCardCheckerConfig, validatedConfig);
}
/**
 * Validates the config values provided for the Gift Card Checker widget.
 * Removes any invalid values and logs appropriate warnings.
 * @param config
 */
function getValidatedGiftCardCheckerConfig(config) {
    if ('showFormFieldLabels' in config) {
        var valid = new Validator()
            .ofType('boolean')
            .validate(config.showFormFieldLabels, 'Gift card checker show form field labels');
        if (!valid)
            delete config.showFormFieldLabels;
    }
    return config;
}
//# sourceMappingURL=config-mapper.js.map