var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { FormElementStyle } from '../../form-element-style';
import { TextArea } from '../text-area';
describe('TextArea', function () {
    it('should render the default TextArea component correctly', function () {
        var props = {
            id: 'textarea-id',
            name: 'textarea-name',
            placeholder: 'textarea placeholder',
            value: 'textarea value',
            rows: 5,
            columns: 10
        };
        compareToSnapshot(React.createElement(TextArea, __assign({}, props)));
    });
    it('should render the warning TextArea component correctly', function () {
        var props = {
            id: 'textarea-id-warning',
            style: FormElementStyle.Warning
        };
        compareToSnapshot(React.createElement(TextArea, __assign({}, props)));
    });
    it('should render the error TextArea component correctly', function () {
        var props = {
            id: 'textarea-id-error',
            style: FormElementStyle.Error
        };
        compareToSnapshot(React.createElement(TextArea, __assign({}, props)));
    });
    it('should render the success TextArea component correctly', function () {
        var props = {
            id: 'textarea-id-success',
            style: FormElementStyle.Success
        };
        compareToSnapshot(React.createElement(TextArea, __assign({}, props)));
    });
});
//# sourceMappingURL=text-area.test.js.map