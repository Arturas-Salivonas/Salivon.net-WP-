import { Component } from 'react';
import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { OrderDetailsDisplayText } from './display-text';
/**
 * Bootstrap options to render the Order Details widget.
 * @export
 * @interface OrderDetailsWidgetBootstrap
 * @extends {WidgetBootstrap}
 */
export interface OrderDetailsWidgetBootstrap extends WidgetBootstrap {
    /**
     * The reference of the journey to be displayed.
     */
    journeyReference?: string;
    /**
     * The unique identifier of the journey to be displayed.
     */
    journeyId?: string;
    /**
     * Display text for the Order Details widget.
     */
    displayText?: Partial<OrderDetailsDisplayText>;
}
export declare type OrderDetailsWidgetBootstrapper = Bootstrapper<Component, OrderDetailsWidgetBootstrap>;
