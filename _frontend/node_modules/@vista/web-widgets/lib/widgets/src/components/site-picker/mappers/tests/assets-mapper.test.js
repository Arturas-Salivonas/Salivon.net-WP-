import { defaultSitePickerAssets } from '../../types/assets';
import { mapSitePickerAssets } from '../assets-mapper';
describe('mapSitePickerAssets', function () {
    it('returns the default assets when the provided display text is undefined', function () {
        var result = mapSitePickerAssets(undefined);
        expect(result).toEqual(defaultSitePickerAssets);
    });
    it('returns the default display text when the provided display text is invalid', function () {
        var result = mapSitePickerAssets(true);
        expect(result).toEqual(defaultSitePickerAssets);
    });
    var propertyTestCases = function (defaultValue) {
        return [
            { value: undefined, expected: defaultValue },
            { value: null, expected: defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    describe('noSitesFoundIconName', function () {
        var testCases = propertyTestCases(defaultSitePickerAssets.noSitesFoundIconName);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    noSitesFoundIconName: testCase.value
                };
                var result = mapSitePickerAssets(displayText);
                expect(result.noSitesFoundIconName).toEqual(testCase.expected);
            });
        });
    });
});
//# sourceMappingURL=assets-mapper.test.js.map