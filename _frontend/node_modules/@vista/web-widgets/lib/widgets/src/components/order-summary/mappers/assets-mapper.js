var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultOrderSummaryAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Order Summary widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapOrderSummaryAssets(assets) {
    if (!assets)
        return defaultOrderSummaryAssets;
    var valid = new Validator().ofType('object').validate(assets, 'Order Summary assets');
    if (!valid)
        return defaultOrderSummaryAssets;
    var validatedAssets = getValidatedOrderSummaryAssets(assets);
    return __assign({}, defaultOrderSummaryAssets, validatedAssets);
}
/**
 * Validates the asset values provided for the Order Summary.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedOrderSummaryAssets(assets) {
    var validateField = fieldValidator(assets);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'preparingTicketsIconName', 'Order Summary preparingTicketsIconName');
    validateField(validator, 'failedToLoadDataIconName', 'Order Summary failedToLoadDataIconName');
    return assets;
}
//# sourceMappingURL=assets-mapper.js.map