import { NestedPartial } from '../../../../../types/nested-partial';
import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { OrderCart } from '../order-cart';
import { OrderCartAssets } from './assets';
import { OrderCartConfig } from './config';
import { OrderCartDisplayText } from './display-text';
import { OrderCartEvents } from './events';
/**
 * Bootstrap options to render the Order Cart widget.
 * @export
 * @interface OrderCartBootstrap
 * @extends {WidgetBootstrap}
 */
export interface OrderCartBootstrap extends WidgetBootstrap {
    /**
     * Configuration options for the Order Cart widget.
     */
    config?: NestedPartial<OrderCartConfig>;
    /**
     * Display text for the Order Cart widget.
     */
    displayText?: Partial<OrderCartDisplayText>;
    /**
     * Assets for the Order Cart widget.
     */
    assets?: Partial<OrderCartAssets>;
    /**
     * Events for the Order Cart widget.
     */
    events?: OrderCartEvents;
}
export declare type OrderCartBootstrapper = Bootstrapper<OrderCart, OrderCartBootstrap>;
