import { addWidgetBootstrap } from '../../bootstrap/add-widget-bootstrap';
import { createComponent } from '../../bootstrap/create-component';
import { FilmList } from './film-list';
import { mapFilmListAssets } from './mappers/assets-mapper';
import { mapFilmListConfig } from './mappers/config-mapper';
import { mapFilmListDisplayText } from './mappers/display-text-mapper';
export var filmList = function (bootstrap) {
    var selector = bootstrap.selector, displayText = bootstrap.displayText, config = bootstrap.config, events = bootstrap.events, siteId = bootstrap.siteId, assets = bootstrap.assets;
    var props = {
        displayText: mapFilmListDisplayText(displayText),
        config: mapFilmListConfig(config),
        onFilmClicked: events && events.onFilmClicked,
        siteId: siteId,
        assets: mapFilmListAssets(assets)
    };
    return createComponent(selector, FilmList, props);
};
addWidgetBootstrap('filmList', filmList);
//# sourceMappingURL=bootstrap.js.map