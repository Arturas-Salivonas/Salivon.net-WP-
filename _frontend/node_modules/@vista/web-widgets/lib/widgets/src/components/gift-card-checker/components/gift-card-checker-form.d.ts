import * as React from 'react';
import { MxGiftCardDetails } from '../../../../../mx-api-client/src/mxt/types/gift-card-details';
import { CaptchaConfig } from '../../captcha/captcha-config';
import { GiftCardCheckerWidgetConfig } from '../types/config';
import { GiftCardCheckerWidgetDisplayText } from '../types/display-text';
export interface GiftCardCheckerFormProps {
    displayText: GiftCardCheckerWidgetDisplayText;
    config: GiftCardCheckerWidgetConfig;
    captchaConfig?: CaptchaConfig;
    getGiftCardDetails: (giftCardNumber?: string, captchaResponse?: string) => Promise<MxGiftCardDetails>;
    onCardDetailsRetrieved: (cardDetails?: MxGiftCardDetails) => void;
}
export interface GiftCardCheckerFormState {
    isLoading: boolean;
    cardNumber: string;
    error?: string;
    captchaResponse?: string;
}
export declare enum GiftCardNumberValidationResult {
    Valid = 0,
    CardNumberMissing = 1
}
export declare enum CaptchaValidationResult {
    Valid = 0,
    Invalid = 1
}
/**
 * Renders a form that allows a user to input a gift card number.
 */
export declare class GiftCardCheckerForm extends React.Component<GiftCardCheckerFormProps, GiftCardCheckerFormState> {
    private captchaRef;
    constructor(props: GiftCardCheckerFormProps);
    render(): JSX.Element;
    handleCardNumberChanged: (newCardNumber: string) => void;
    handleCaptchaChanged: (captchaResponse?: string | undefined) => void;
    handleSubmit: (event: React.FormEvent<Element>) => Promise<void>;
    resetCaptcha: () => void;
    validateGiftCardNumber(cardNumber: string): GiftCardNumberValidationResult;
    validateCaptcha(config?: CaptchaConfig, captchaResponse?: string): CaptchaValidationResult;
}
