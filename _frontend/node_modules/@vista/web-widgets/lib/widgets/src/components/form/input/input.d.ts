import * as React from 'react';
import { FormElementProps } from '../form-element-props';
export interface InputProps extends FormElementProps<string> {
    type?: InputType;
    maxLength?: number;
}
/**
 * Input types have been limited to text, email and phone numbers.
 * Use NumberInput for all your number input needs.
 * Specific components for PasswordInput, DateInput, SearchInput and others are likely in future.
 */
export declare type InputType = 'text' | 'email' | 'tel' | 'password';
/**
 * Renders an input component.
 */
export declare class Input extends React.Component<InputProps> {
    render(): JSX.Element;
    handleChange: (event: any) => void;
}
