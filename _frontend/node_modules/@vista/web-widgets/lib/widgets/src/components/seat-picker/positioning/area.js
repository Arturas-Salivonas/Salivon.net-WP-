import { SeatPickerWidgetScreenPosition } from '../types/config';
import { getMapDimension } from './map';
/**
 * Returns the boundary of an area in SVG units.
 */
export function getAreaBoundary(area, mapBoundary, scaleFactor, config) {
    var invertSeatMap = config.screen.position === SeatPickerWidgetScreenPosition.Top;
    var topPercentage = invertSeatMap
        ? 100 - area.top - area.height - mapBoundary.top
        : area.top - mapBoundary.top;
    var leftPercentage = invertSeatMap
        ? 100 - area.left - area.width - mapBoundary.left
        : area.left - mapBoundary.left;
    var screenTopOffset = invertSeatMap ? config.screen.height + config.screen.margin : 0;
    return {
        top: getMapDimension(topPercentage, scaleFactor) + screenTopOffset,
        left: getMapDimension(leftPercentage, scaleFactor),
        width: getMapDimension(area.width, scaleFactor),
        height: getMapDimension(area.height, scaleFactor)
    };
}
//# sourceMappingURL=area.js.map