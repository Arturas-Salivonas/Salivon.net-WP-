import * as React from 'react';
import { Site } from '../../../../../types/browsing/site';
export interface SiteListFilteredSiteProps {
    site: Site;
    onClick: (site: Site) => void;
}
export declare class SiteListFilteredSite extends React.Component<SiteListFilteredSiteProps> {
    render(): JSX.Element;
    private handleClick;
}
