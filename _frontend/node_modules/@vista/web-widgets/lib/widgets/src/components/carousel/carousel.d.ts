import * as React from 'react';
import { CarouselSettings } from './settings';
export interface CarouselIcons {
    prevButton: string;
    nextButton: string;
}
export interface CarouselProps {
    icons?: Partial<CarouselIcons>;
    settings?: Partial<CarouselSettings>;
    scrollToSlide?: number;
    scrollToPage?: number;
    showDots?: boolean;
    children: any;
    refreshOnUpdate?: boolean;
}
export declare class Carousel extends React.Component<CarouselProps, {}> {
    private glider;
    private viewport;
    private nextButton;
    private prevButton;
    private dots;
    render(): JSX.Element;
    componentDidMount(): void;
    componentDidUpdate(): void;
    componentWillUnmount(): void;
    private getGliderOptions;
    private positionGlider;
}
