import * as React from 'react';
import { SeatingStore } from '../../../../api/src/stores/seating/seating-store';
import { SeatPickerAssets } from './types/assets';
import { SeatPickerWidgetConfig } from './types/config';
import { SeatPickerWidgetDisplayText } from './types/display-text';
import { SeatPickerWidgetEvents } from './types/events';
export interface SeatPickerProps {
    seatingStore: SeatingStore;
    config: SeatPickerWidgetConfig;
    displayText: SeatPickerWidgetDisplayText;
    isSeatFirstOrdering: boolean;
    assets: SeatPickerAssets;
    enableLoadingIndicator?: boolean;
    showtimeId?: string;
    events?: SeatPickerWidgetEvents;
}
export interface SeatPickerState {
    failedToLoadSeatLayout?: boolean;
}
export declare class SeatPicker extends React.Component<SeatPickerProps, SeatPickerState> {
    constructor(props: any);
    render(): JSX.Element | null;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private selectedSeatsDisposer;
    private handleNotificationClosed;
    private handleSeatClicked;
    private setSeatLayoutData;
    private setSeatLayoutDataFromApi;
}
