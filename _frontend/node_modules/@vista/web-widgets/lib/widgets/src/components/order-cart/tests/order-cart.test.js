var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { orderStoreInstance } from '../../../../../api/src/stores/order/order-store';
import { exampleOrder } from '../../../../../example-data/order/order';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { exampleSalesDataStore } from '../../item-picker/example/example-sales-data-store';
import { OrderCart } from '../order-cart';
import { defaultOrderCartAssets } from '../types/assets';
import { defaultOrderCartConfig } from '../types/config';
import { defaultOrderCartDisplayText } from '../types/display-text';
describe('OrderCart', function () {
    beforeAll(function () {
        orderStoreInstance.actions.setOrderData(exampleOrder);
    });
    var testProps = {
        orderStore: orderStoreInstance,
        salesDataStore: exampleSalesDataStore,
        displayText: defaultOrderCartDisplayText,
        config: defaultOrderCartConfig,
        assets: defaultOrderCartAssets
    };
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(OrderCart, __assign({}, testProps)));
    });
});
//# sourceMappingURL=order-cart.test.js.map