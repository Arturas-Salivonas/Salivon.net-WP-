/**
 * Configuration for Google reCAPTCHA v2
 */
export declare type RecaptchaV2Config = {
    /**
     * The site key from the Google reCAPTCHA API key pair for your site
     * See https://developers.google.com/recaptcha/intro for more information
     */
    siteKey?: string;
    /**
     * Determines how the captcha should be displayed
     */
    size?: RecaptchaV2Size;
};
/**
 * Render options for the Google reCAPTCHA v2
 * 'compact' and 'normal' change the size of the rendered checkbox reCAPTCHA
 * 'invisible' does not render any element needs to be triggered programatically
 */
export declare type RecaptchaV2Size = 'compact' | 'normal' | 'invisible';
