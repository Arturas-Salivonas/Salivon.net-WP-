var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultMemberTransactionHistoryConfig } from './../types/config';
/**
 * Maps the config provided via the API to the config required by Member Transaction History widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 * @param config
 */
export function mapMemberTransactionHistoryConfig(config) {
    if (!config)
        return defaultMemberTransactionHistoryConfig;
    var valid = new Validator().ofType('object').validate(config, 'Member Transaction History config');
    if (!valid)
        return defaultMemberTransactionHistoryConfig;
    var validatedConfig = getValidatedMemberTransactionHistoryConfig(config);
    return __assign({}, defaultMemberTransactionHistoryConfig, validatedConfig);
}
function getValidatedMemberTransactionHistoryConfig(config) {
    if ('numberOfTransactionsToLoad' in config) {
        var valid = new Validator()
            .ofType('number').min(1)
            .validate(config.numberOfTransactionsToLoad, 'Member Transaction History numberOfTransactionsToLoad');
        if (!valid)
            delete config.numberOfTransactionsToLoad;
    }
    return config;
}
//# sourceMappingURL=config-mapper.js.map