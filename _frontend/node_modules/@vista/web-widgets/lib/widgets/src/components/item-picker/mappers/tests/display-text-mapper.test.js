import { defaultItemPickerDisplayText } from '../../types/display-text';
import { mapItemPickerDisplayText } from '../display-text-mapper';
describe('Item Picker Display Text Mapper', function () {
    it('returns the default display text when the provided display text is undefined', function () {
        var result = mapItemPickerDisplayText(undefined);
        expect(result).toEqual(defaultItemPickerDisplayText);
    });
    it('returns the default display text when the provided display text is invalid', function () {
        var result = mapItemPickerDisplayText(true);
        expect(result).toEqual(defaultItemPickerDisplayText);
    });
    var propertyTestCases = function (defaultValue, allowNull, allowEmpty) {
        if (allowNull === void 0) { allowNull = false; }
        if (allowEmpty === void 0) { allowEmpty = false; }
        return [
            { value: undefined, expected: allowNull ? undefined : defaultValue },
            { value: null, expected: allowNull ? null : defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: allowEmpty ? '' : defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    describe('addToOrderLabel', function () {
        var testCases = propertyTestCases(defaultItemPickerDisplayText.addToOrderLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    addToOrderLabel: testCase.value
                };
                var result = mapItemPickerDisplayText(displayText);
                expect(result.addToOrderLabel).toEqual(testCase.expected);
            });
        });
    });
    describe('failedToLoadDataTitle', function () {
        var testCases = propertyTestCases(defaultItemPickerDisplayText.failedToLoadDataTitle);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    failedToLoadDataTitle: testCase.value
                };
                var result = mapItemPickerDisplayText(displayText);
                expect(result.failedToLoadDataTitle).toEqual(testCase.expected);
            });
        });
    });
    describe('failedToLoadDataMessage', function () {
        var testCases = propertyTestCases(defaultItemPickerDisplayText.failedToLoadDataMessage);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    failedToLoadDataMessage: testCase.value
                };
                var result = mapItemPickerDisplayText(displayText);
                expect(result.failedToLoadDataMessage).toEqual(testCase.expected);
            });
        });
    });
});
//# sourceMappingURL=display-text-mapper.test.js.map