var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var _this = this;
import { shallow } from 'enzyme';
import * as React from 'react';
import { browsingService } from '../../../../../api/src/services/browsing/browsing-service';
import { browsingStoreInstance } from '../../../../../api/src/stores/browsing/browsing-store';
import { Logger } from '../../../../../api/src/utils/logger';
import { exampleBrowsingStoreData } from '../../../../../example-data/browsing/browsing-store-data';
import { exampleFilms } from '../../../../../example-data/browsing/film';
import { exampleSite } from '../../../../../example-data/browsing/site';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { runAllPromises } from '../../../tests/run-all-promises';
import { FilmList } from '../film-list';
import { defaultFilmListAssets } from '../types/assets';
import { defaultFilmListConfig, FilmListMode } from '../types/config';
import { defaultFilmListDisplayText } from '../types/display-text';
describe('FilmList', function () {
    var loggerErrorSpy = jest.spyOn(Logger, 'error');
    var loadFilmsSpy = jest.spyOn(browsingService, 'loadFilms').mockResolvedValue(exampleFilms);
    var loadFilmsForSiteSpy = jest.spyOn(browsingService, 'loadFilmsForSite').mockResolvedValue(exampleFilms);
    beforeAll(function () {
        browsingStoreInstance.setData(exampleBrowsingStoreData);
    });
    beforeEach(function () {
        browsingStoreInstance.setData({ allFilmsLoaded: true });
    });
    var testProps = {
        config: defaultFilmListConfig,
        displayText: defaultFilmListDisplayText,
        assets: defaultFilmListAssets
    };
    it('should render the skeleton when films have not been loaded', function () {
        browsingStoreInstance.setData({ allFilmsLoaded: false });
        compareToSnapshot(React.createElement(FilmList, __assign({}, testProps)));
    });
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(FilmList, __assign({}, testProps)));
    });
    it('should render correctly for a given site', function () {
        compareToSnapshot(React.createElement(FilmList, __assign({}, testProps, { siteId: exampleSite.id })));
    });
    it('should render grid mode correctly', function () {
        var props = __assign({}, testProps, { config: __assign({}, testProps.config, { mode: FilmListMode.Grid }) });
        compareToSnapshot(React.createElement(FilmList, __assign({}, props)));
    });
    it('should render carousel mode correctly', function () {
        var props = __assign({}, testProps, { config: __assign({}, testProps.config, { mode: FilmListMode.Carousel }) });
        compareToSnapshot(React.createElement(FilmList, __assign({}, props)));
    });
    describe('componentDidMount', function () {
        beforeEach(function () {
            browsingStoreInstance.flush();
        });
        it('calls loadFilms when no siteId is passed in', function () { return __awaiter(_this, void 0, void 0, function () {
            var wrapper;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        expect.assertions(1);
                        wrapper = shallow(React.createElement(FilmList, __assign({}, testProps, { siteId: undefined })));
                        wrapper.instance().componentDidMount();
                        return [4 /*yield*/, runAllPromises()];
                    case 1:
                        _a.sent();
                        expect(browsingService.loadFilms).toHaveBeenCalled();
                        return [2 /*return*/];
                }
            });
        }); });
        it('logs an error when failing to load the films data', function () { return __awaiter(_this, void 0, void 0, function () {
            var wrapper;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        expect.assertions(1);
                        loggerErrorSpy.mockReturnValueOnce(undefined);
                        loadFilmsSpy.mockRejectedValueOnce(new Error('Test Error'));
                        wrapper = shallow(React.createElement(FilmList, __assign({}, testProps, { siteId: undefined })));
                        wrapper.instance().componentDidMount();
                        return [4 /*yield*/, runAllPromises()];
                    case 1:
                        _a.sent();
                        expect(loggerErrorSpy).toHaveBeenCalledWith('Failed to fetch films. Error: Test Error');
                        return [2 /*return*/];
                }
            });
        }); });
        it('calls loadFilmsForSite when siteId is passed in', function () { return __awaiter(_this, void 0, void 0, function () {
            var wrapper;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        expect.assertions(1);
                        wrapper = shallow(React.createElement(FilmList, __assign({}, testProps, { siteId: "123" })));
                        wrapper.instance().componentDidMount();
                        return [4 /*yield*/, runAllPromises()];
                    case 1:
                        _a.sent();
                        expect(browsingService.loadFilmsForSite).toHaveBeenCalledWith('123');
                        return [2 /*return*/];
                }
            });
        }); });
        it('logs an error when failing to load the films data for a given site', function () { return __awaiter(_this, void 0, void 0, function () {
            var wrapper;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        expect.assertions(1);
                        loggerErrorSpy.mockReturnValueOnce(undefined);
                        loadFilmsForSiteSpy.mockRejectedValueOnce(new Error('Test Error'));
                        wrapper = shallow(React.createElement(FilmList, __assign({}, testProps, { siteId: "123" })));
                        wrapper.instance().componentDidMount();
                        return [4 /*yield*/, runAllPromises()];
                    case 1:
                        _a.sent();
                        expect(loggerErrorSpy).toHaveBeenCalledWith('Failed to fetch films for site: 123. Error: Test Error');
                        return [2 /*return*/];
                }
            });
        }); });
    });
});
//# sourceMappingURL=film-list.test.js.map