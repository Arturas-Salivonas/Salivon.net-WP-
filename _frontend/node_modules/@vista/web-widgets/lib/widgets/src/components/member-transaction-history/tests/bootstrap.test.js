import { loyaltyMemberService } from '../../../../../api/src/services/loyalty/loyalty-member-service';
import { configurationStoreInstance } from '../../../../../api/src/stores/configuration/configuration-store';
import * as createComponent from '../../../bootstrap/create-component';
import { memberTransactionHistory } from '../bootstrap';
import { defaultMemberTransactionHistoryAssets } from '../types/assets';
import { defaultMemberTransactionHistoryConfig } from '../types/config';
import { defaultMemberTransactionHistoryDisplayText } from '../types/display-text';
describe('memberTransactionHistory bootstrap', function () {
    var component = { _component: {} };
    var createComponentSpy = jest.spyOn(createComponent, 'createComponent').mockReturnValue(component);
    afterEach(function () { return jest.clearAllMocks(); });
    it('calls the create component function', function () {
        var response = memberTransactionHistory({ selector: 'test' });
        expect(createComponentSpy).toBeCalledWith('test', expect.anything(), {
            loyaltyMemberService: loyaltyMemberService,
            configurationStore: configurationStoreInstance,
            config: defaultMemberTransactionHistoryConfig,
            displayText: defaultMemberTransactionHistoryDisplayText,
            assets: defaultMemberTransactionHistoryAssets
        });
        expect(response).toEqual(component);
    });
});
//# sourceMappingURL=bootstrap.test.js.map