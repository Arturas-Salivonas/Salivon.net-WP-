/**
 * Configuration options for the Member Transaction History widget.
 */
export interface MemberTransactionHistoryConfig {
    /**
     * Defines the number of transactions to load in the transaction history table when the widget first loads,
     * or after more transactions are requested.
     */
    numberOfTransactionsToLoad: number;
}
/**
 * Default configuration options for the Member Transaction History widget.
 */
export declare const defaultMemberTransactionHistoryConfig: MemberTransactionHistoryConfig;
