var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultFilmListAssets } from '../../types/assets';
import { defaultFilmListDisplayText } from '../../types/display-text';
import { FilmListMessage, FilmListMessageType } from '../film-list-message';
describe('FilmListMessage', function () {
    it('should render a NoFilmsForTab message correctly', function () {
        var testProps = {
            messageType: FilmListMessageType.NoFilmsForTab,
            displayText: defaultFilmListDisplayText,
            assets: defaultFilmListAssets,
            selectedTabLabel: 'Test Label'
        };
        compareToSnapshot(React.createElement(FilmListMessage, __assign({}, testProps)));
    });
    it('should render a FailedToLoadData message correctly', function () {
        var testProps = {
            messageType: FilmListMessageType.FailedToLoadData,
            displayText: defaultFilmListDisplayText,
            assets: defaultFilmListAssets
        };
        compareToSnapshot(React.createElement(FilmListMessage, __assign({}, testProps)));
    });
});
//# sourceMappingURL=film-list-message.test.js.map