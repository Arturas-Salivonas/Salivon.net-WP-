import * as React from 'react';
import { Seat } from '../../../../../types/seating/seat';
import { SeatLayout } from '../../../../../types/seating/seat-layout';
import { ContentTransformation } from '../../zoom-controller/utils/transformation-utils';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerWidgetDisplayText } from '../types/display-text';
export interface SeatPickerZoomControllerProps {
    seatLayout: SeatLayout;
    config: SeatPickerWidgetConfig;
    displayText: SeatPickerWidgetDisplayText;
    onSeatClicked: (selectedSeat: Seat) => void;
    isSeatFirstOrdering: boolean;
}
export declare class SeatPickerZoomController extends React.Component<SeatPickerZoomControllerProps> {
    private rowLabelsRef;
    private zoomControllerRef;
    componentDidUpdate(prevProps: SeatPickerZoomControllerProps): void;
    render(): JSX.Element;
    handleTransformationChanged: (transformation: ContentTransformation) => void;
    setRowLabelsTranslation: (x: number) => void;
}
