import * as React from 'react';
import { SeatLayout } from '../../../../../types/seating/seat-layout';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerWidgetDisplayText } from '../types/display-text';
import { SeatPickerBoundary } from '../types/seat-picker-measurements';
export interface SeatPickerScreenProps {
    seatLayout: SeatLayout;
    scaleFactor: number;
    config: SeatPickerWidgetConfig;
    displayText: SeatPickerWidgetDisplayText;
    mapBoundary: SeatPickerBoundary;
}
export declare class SeatPickerScreen extends React.Component<SeatPickerScreenProps> {
    render(): JSX.Element;
}
export declare function getScreenBackgroundPath(width: number, height: number, config: SeatPickerWidgetConfig): string;
