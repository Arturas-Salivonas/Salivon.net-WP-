import { SeatLayout } from '../../../../../types/seating/seat-layout';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerBoundary } from '../types/seat-picker-measurements';
/**
 * Returns the boundary of the screen in SVG units.
 */
export declare function getScreenBoundary(seatLayout: SeatLayout, mapBoundary: SeatPickerBoundary, scaleFactor: number, config: SeatPickerWidgetConfig): SeatPickerBoundary;
