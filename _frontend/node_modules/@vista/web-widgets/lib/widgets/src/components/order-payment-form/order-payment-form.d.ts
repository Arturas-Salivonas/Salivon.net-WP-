import * as React from 'react';
import { OrderStore } from '../../../../api/src/stores/order/order-store';
import { OrderPaymentFormAssets } from './types/assets';
import { OrderPaymentFormDisplayText } from './types/display-text';
import { OrderPaymentFormEvents } from './types/events';
export interface OrderPaymentFormProps {
    orderStore: OrderStore;
    displayText: OrderPaymentFormDisplayText;
    assets: OrderPaymentFormAssets;
    events?: Partial<OrderPaymentFormEvents>;
}
export interface OrderPaymentFormState {
    failedToCompletePayment: boolean;
}
/**
 * Renders a form that allows a user to pay for an in-progress order.
 */
export declare class OrderPaymentForm extends React.Component<OrderPaymentFormProps, OrderPaymentFormState> {
    constructor(props: OrderPaymentFormProps);
    render(): JSX.Element | null;
    private handlePaymentSuccessful;
    private handlePaymentFailed;
}
