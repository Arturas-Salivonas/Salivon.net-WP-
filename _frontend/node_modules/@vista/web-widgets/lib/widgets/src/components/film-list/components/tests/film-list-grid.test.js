var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleFilms } from '../../../../../../example-data/browsing/film';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { FilmListGrid } from '../film-list-grid';
describe('FilmListGrid', function () {
    it('should render correctly', function () {
        var testProps = {
            films: exampleFilms,
            onFilmClicked: function () { return; }
        };
        compareToSnapshot(React.createElement(FilmListGrid, __assign({}, testProps)));
    });
});
//# sourceMappingURL=film-list-grid.test.js.map