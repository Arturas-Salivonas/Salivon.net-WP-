import * as React from 'react';
import { BemClassBuilder } from '../../utils/bem-class-builder';
export interface ButtonProps {
    className?: string;
    label?: string;
    color?: ButtonColor;
    size?: ButtonSize;
    icon?: string;
    iconOnly?: boolean;
    type?: ButtonType;
    loading?: boolean;
    disabled?: boolean;
    tabIndex?: number;
    onClick?: (event?: object) => void;
}
export declare enum ButtonColor {
    Primary = "Primary",
    Secondary = "Secondary",
    Tertiary = "Tertiary"
}
export declare enum ButtonSize {
    Small = "Small",
    Medium = "Medium",
    Large = "Large"
}
export declare type ButtonType = 'submit' | 'reset' | 'button';
/**
 * Renders a button.
 */
export declare class Button extends React.Component<ButtonProps> {
    render(): JSX.Element;
    handleClick: React.MouseEventHandler;
    getButtonClass(b: BemClassBuilder, props: ButtonProps): string;
}
