var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultTicketPickerDisplayText } from '../types/display-text';
/**
 * Maps the display text provided via the API to the display text required by the Ticket Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapTicketPickerDisplayText(displayText) {
    if (!displayText)
        return defaultTicketPickerDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Ticket picker display text');
    if (!valid)
        return defaultTicketPickerDisplayText;
    var validatedDisplayText = getValidatedTicketPickerDisplayText(displayText);
    return __assign({}, defaultTicketPickerDisplayText, validatedDisplayText);
}
/**
 * Validates the display text values provided for the Ticket Picker.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedTicketPickerDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'counterLabel', 'Ticket picker counter label');
    validateField(validator, 'tableColumnItemHeaderLabel', 'Ticket picker item column header label');
    validateField(validator, 'tableColumnPriceHeaderLabel', 'Ticket picker price column header label');
    validateField(validator, 'tableColumnQuantityHeaderLabel', 'Ticket picker quantity column header label');
    validateField(validator, 'tableColumnSubtotalHeaderLabel', 'Ticket picker subtotal column header label');
    validateField(validator, 'tableFooterRowBookingFeeLabel', 'Ticket picker booking fee footer row label');
    validateField(validator, 'tableFooterRowOrderTotalLabel', 'Ticket picker order total footer row label');
    validateField(validator, 'errorNoTicketsSelectedMessage', 'Ticket picker errorNoTicketsSelectedMessage');
    validateField(validator, 'errorTooManyTicketsSelectedMessage', 'Ticket picker errorTooManyTicketsSelectedMessage');
    validateField(validator, 'errorTooFewTicketsSelectedMessage', 'Ticket picker errorTooFewTicketsSelectedMessage');
    validateField(validator, 'errorTicketsDoNotMatchAreaCategoriesMessage', 'Ticket picker errorTicketsDoNotMatchAreaCategoriesMessage');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map