var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultMemberTransactionHistoryAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Member Transaction History widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapMemberTransactionHistoryAssets(assets) {
    if (!assets)
        return defaultMemberTransactionHistoryAssets;
    var valid = new Validator().ofType('object').validate(assets, 'Member transaction history assets');
    if (!valid)
        return defaultMemberTransactionHistoryAssets;
    var validatedAssets = getValidatedMemberTransactionHistoryAssets(assets);
    return __assign({}, defaultMemberTransactionHistoryAssets, validatedAssets);
}
/**
 * Validates the asset values provided for Member Transaction History.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedMemberTransactionHistoryAssets(assets) {
    var validateField = fieldValidator(assets);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'noTransactionHistoryIconName', 'Member transaction history noTransactionHistoryIconName');
    validateField(validator, 'failedToLoadDataIconName', 'Member transaction history failedToLoadDataIconName');
    return assets;
}
//# sourceMappingURL=assets-mapper.js.map