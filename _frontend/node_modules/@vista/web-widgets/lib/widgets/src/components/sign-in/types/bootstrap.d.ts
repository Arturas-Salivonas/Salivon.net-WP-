import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { SignIn } from '../sign-in';
import { SignInWidgetConfig } from './config';
import { SignInWidgetDisplayText } from './display-text';
import { SignInWidgetEvents } from './events';
/**
 * Bootstrap options for Sign In widget
 */
export interface SignInWidgetBootstrap extends WidgetBootstrap {
    /**
     * Configuration options for the Sign In widget.
     */
    config?: SignInWidgetConfig;
    /**
     * Events for the Sign In widget.
     */
    events?: SignInWidgetEvents;
    /**
     * Display text for the Sign In widget.
     */
    displayText?: Partial<SignInWidgetDisplayText>;
}
export declare type SignInWidgetBootstrapper = Bootstrapper<SignIn, SignInWidgetBootstrap>;
