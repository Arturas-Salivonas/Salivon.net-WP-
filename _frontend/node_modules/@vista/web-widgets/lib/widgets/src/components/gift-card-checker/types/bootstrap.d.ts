import { NestedPartial } from '../../../../../types/nested-partial';
import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { GiftCardChecker } from '../gift-card-checker';
import { GiftCardCheckerWidgetConfig } from './config';
import { GiftCardCheckerWidgetDisplayText } from './display-text';
/**
 * Bootstrap options to render the Gift Card Checker widget.
 * @export
 * @interface GiftCardCheckerWidgetBootstrap
 * @extends {WidgetBootstrap}
 */
export interface GiftCardCheckerWidgetBootstrap extends WidgetBootstrap {
    /**
     * Configuration options for the Gift Card Checker widget.
     */
    config?: NestedPartial<GiftCardCheckerWidgetConfig>;
    /**
     * Display text for the Gift Card Checker widget.
     */
    displayText?: Partial<GiftCardCheckerWidgetDisplayText>;
}
export declare type GiftCardCheckerWidgetBootstrapper = Bootstrapper<GiftCardChecker, GiftCardCheckerWidgetBootstrap>;
