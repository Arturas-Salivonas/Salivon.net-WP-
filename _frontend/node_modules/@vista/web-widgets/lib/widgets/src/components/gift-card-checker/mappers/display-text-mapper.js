var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultGiftCardCheckerDisplayText } from '../types/display-text';
/**
 * Maps the display text provided via the API to the display text required by the Gift Card Checker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapGiftCardCheckerDisplayText(displayText) {
    if (!displayText)
        return defaultGiftCardCheckerDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Gift card checker display text');
    if (!valid)
        return defaultGiftCardCheckerDisplayText;
    var validatedDisplayText = getValidatedGiftCardCheckerDisplayText(displayText);
    return __assign({}, defaultGiftCardCheckerDisplayText, validatedDisplayText);
}
/**
 * Validates the display text values provided for the Gift Card Checker widget.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedGiftCardCheckerDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    var nullableValidator = new Validator().ofType('string').minLength(1).allowNull().allowUndefined();
    validateField(validator, 'fieldCardNumberInputLabel', 'Gift card checker card number input field label');
    validateField(nullableValidator, 'fieldCardNumberPlaceholder', 'Gift card checker card number input field placeholder');
    validateField(validator, 'buttonSubmitLabel', 'Gift card checker submit button label');
    validateField(validator, 'buttonBackLabel', 'Gift card checker back button label');
    validateField(validator, 'detailsCardNumberLabel', 'Gift card checker card number detail label');
    validateField(validator, 'detailsBalanceLabel', 'Gift card checker card balance detail label');
    validateField(validator, 'detailsExpiryDateLabel', 'Gift card checker card expiry date detail label');
    validateField(validator, 'detailsExpiryDate', 'Gift card checker card expiry date label');
    validateField(validator, 'errorCardNumberRequiredMessage', 'Gift card checker card number required error message');
    validateField(validator, 'errorCouldNotGetGiftCardDetailsMessage', 'Gift card checker card details could not be retrieved error message');
    validateField(validator, 'errorMissingCaptchaVerificationMessage', 'Gift card checker missing captcha verification error message');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map