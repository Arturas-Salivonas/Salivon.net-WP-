var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultMemberPointsBalanceDisplayText } from '../types/display-text';
/**
 * Maps the display text provided via the API to the display text required by the  Member Details widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapMemberPointsBalanceDisplayText(displayText) {
    if (!displayText)
        return defaultMemberPointsBalanceDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Member details display text');
    if (!valid)
        return defaultMemberPointsBalanceDisplayText;
    var validatedDisplayText = getValidatedMemberDetailsDisplayText(displayText);
    return __assign({}, defaultMemberPointsBalanceDisplayText, validatedDisplayText);
}
/**
 * Validates the display text values provided for the  Member Details widget.
 * Removes any invalid values and logs appropriate warnings.
 */
function getValidatedMemberDetailsDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'pointsExpiryDetailsLabel', 'Loyalty Member Details pointsExpiryDetailsLabel');
    validateField(validator, 'memberBalanceText', 'Loyalty Member Details memberBalanceText');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map