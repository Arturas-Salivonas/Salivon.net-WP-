var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultSiteDetailsWidgetDisplayText } from '../types/display-text';
export function mapSiteDetailsDisplayText(displayText) {
    if (!displayText)
        return defaultSiteDetailsWidgetDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Site Details display text');
    if (!valid)
        return defaultSiteDetailsWidgetDisplayText;
    var validatedDisplayText = getValidatedSiteDetailsDisplayText(displayText);
    return __assign({}, defaultSiteDetailsWidgetDisplayText, validatedDisplayText);
}
function getValidatedSiteDetailsDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'phoneNumberLabel', 'Site Details phoneNumberLabel');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map