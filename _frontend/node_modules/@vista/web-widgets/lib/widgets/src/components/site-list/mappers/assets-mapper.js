var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultSiteListAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Site List widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapSiteListAssets(assets) {
    if (!assets)
        return defaultSiteListAssets;
    var valid = new Validator().ofType('object').validate(assets, 'Site list assets');
    if (!valid)
        return defaultSiteListAssets;
    var validatedAssets = getValidatedSiteListAssets(assets);
    return __assign({}, defaultSiteListAssets, validatedAssets);
}
/**
 * Validates the asset values provided for the Site List.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedSiteListAssets(assets) {
    var validateField = fieldValidator(assets);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'noSitesFoundIconName', 'Site list noSitesFoundIconName');
    return assets;
}
//# sourceMappingURL=assets-mapper.js.map