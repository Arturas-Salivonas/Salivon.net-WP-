import * as React from 'react';
import { SignInProvider, UserCredentialsAuthenticationProvider } from '../../../../types/loyalty/authentication/sign-in-provider';
import { CaptchaConfig } from '../captcha/captcha-config';
import { SignInWidgetDisplayText } from './types/display-text';
export declare enum SignInDisplayMode {
    Expand = "Expand",
    FadeIn = "FadeIn"
}
export interface SignInState {
    showSignInForm: boolean;
    currentSignInProviderName: string;
}
export interface SignInProps {
    providers: SignInProvider[];
    captchaConfig?: CaptchaConfig;
    displayMode: SignInDisplayMode;
    displayText: SignInWidgetDisplayText;
}
export declare class SignIn extends React.Component<SignInProps, SignInState> {
    static defaultProps: Partial<SignInProps>;
    isExpandDisplayMode: boolean;
    constructor(props: SignInProps);
    render(): JSX.Element;
    onShowSignInForm: (signInProviderName: string) => void;
    onHideSignInForm: () => void;
    getSignwithUserCredentialsProviders(): UserCredentialsAuthenticationProvider[];
    showSplitterForExpandItemInExpandDisplayMode: (itemIndex: number, userCredentialsSignInProvidersCount: number) => boolean;
}
