import { defaultCustomerDetailsFormDisplayText } from '../../types/display-text';
import { mapCustomerDetailsFormDisplayText } from '../display-text-mapper';
describe('mapCustomerDetailsFormDisplayText', function () {
    it('returns the default display text when the provided display text is undefined', function () {
        var result = mapCustomerDetailsFormDisplayText(undefined);
        expect(result).toEqual(defaultCustomerDetailsFormDisplayText);
    });
    it('returns the default display text when the provided display text is invalid', function () {
        var result = mapCustomerDetailsFormDisplayText({});
        expect(result).toEqual(defaultCustomerDetailsFormDisplayText);
    });
    var propertyTestCases = function (defaultValue, allowNull) {
        if (allowNull === void 0) { allowNull = false; }
        return [
            { value: undefined, expected: allowNull ? undefined : defaultValue },
            { value: null, expected: allowNull ? null : defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    describe('fieldGivenNameLabel', function () {
        var testCases = propertyTestCases(defaultCustomerDetailsFormDisplayText.fieldGivenNameLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    fieldGivenNameLabel: testCase.value
                };
                var result = mapCustomerDetailsFormDisplayText(displayText);
                expect(result.fieldGivenNameLabel).toEqual(testCase.expected);
            });
        });
    });
    describe('fieldFamilyNameLabel', function () {
        var testCases = propertyTestCases(defaultCustomerDetailsFormDisplayText.fieldFamilyNameLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    fieldFamilyNameLabel: testCase.value
                };
                var result = mapCustomerDetailsFormDisplayText(displayText);
                expect(result.fieldFamilyNameLabel).toEqual(testCase.expected);
            });
        });
    });
    describe('fieldEmailLabel', function () {
        var testCases = propertyTestCases(defaultCustomerDetailsFormDisplayText.fieldEmailLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    fieldEmailLabel: testCase.value
                };
                var result = mapCustomerDetailsFormDisplayText(displayText);
                expect(result.fieldEmailLabel).toEqual(testCase.expected);
            });
        });
    });
    describe('fieldPhoneLabel', function () {
        var testCases = propertyTestCases(defaultCustomerDetailsFormDisplayText.fieldPhoneLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    fieldPhoneLabel: testCase.value
                };
                var result = mapCustomerDetailsFormDisplayText(displayText);
                expect(result.fieldPhoneLabel).toEqual(testCase.expected);
            });
        });
    });
    describe('fieldGivenNamePlaceholder', function () {
        var testCases = propertyTestCases(defaultCustomerDetailsFormDisplayText.fieldGivenNamePlaceholder, true);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    fieldGivenNamePlaceholder: testCase.value
                };
                var result = mapCustomerDetailsFormDisplayText(displayText);
                expect(result.fieldGivenNamePlaceholder).toEqual(testCase.expected);
            });
        });
    });
    describe('fieldFamilyNamePlaceholder', function () {
        var testCases = propertyTestCases(defaultCustomerDetailsFormDisplayText.fieldFamilyNamePlaceholder, true);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    fieldFamilyNamePlaceholder: testCase.value
                };
                var result = mapCustomerDetailsFormDisplayText(displayText);
                expect(result.fieldFamilyNamePlaceholder).toEqual(testCase.expected);
            });
        });
    });
    describe('fieldEmailPlaceholder', function () {
        var testCases = propertyTestCases(defaultCustomerDetailsFormDisplayText.fieldEmailPlaceholder, true);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    fieldEmailPlaceholder: testCase.value
                };
                var result = mapCustomerDetailsFormDisplayText(displayText);
                expect(result.fieldEmailPlaceholder).toEqual(testCase.expected);
            });
        });
    });
    describe('fieldPhonePlaceholder', function () {
        var testCases = propertyTestCases(defaultCustomerDetailsFormDisplayText.fieldPhonePlaceholder, true);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    fieldPhonePlaceholder: testCase.value
                };
                var result = mapCustomerDetailsFormDisplayText(displayText);
                expect(result.fieldPhonePlaceholder).toEqual(testCase.expected);
            });
        });
    });
});
//# sourceMappingURL=display-text-mapper.test.js.map