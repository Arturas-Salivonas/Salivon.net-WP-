import * as React from 'react';
import { BrowsingStore } from '../../../../api/src/stores/browsing/browsing-store';
import { ConfigurationStore } from '../../../../api/src/stores/configuration/configuration-store';
import { Site } from '../../../../types/browsing/site';
import { SitePickerAssets } from './types/assets';
import { SitePickerDisplayText } from './types/display-text';
export interface SitePickerProps {
    browsingStore: BrowsingStore;
    configurationStore: ConfigurationStore;
    displayText: SitePickerDisplayText;
    assets: SitePickerAssets;
}
export interface SitePickerState {
    filteredSites?: Site[];
    searchTerm: string;
    failedToLoadSites: boolean;
}
/**
 * Renders a component that allows the user to choose the sites they wish to browse.
 */
export declare class SitePicker extends React.Component<SitePickerProps, SitePickerState> {
    constructor(props: SitePickerProps);
    render(): JSX.Element | null;
    componentDidMount(): void;
    private handleSearchResultsChanged;
    private handleSearchTermChanged;
    private handleSelectedSiteClicked;
    private handleUnselectedSiteClicked;
}
