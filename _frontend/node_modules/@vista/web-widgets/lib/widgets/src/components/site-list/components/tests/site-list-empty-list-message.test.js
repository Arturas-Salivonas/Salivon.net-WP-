var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultSiteListAssets } from '../../types/assets';
import { defaultSiteListDisplayText } from '../../types/display-text';
import { SiteListEmptyListMessage } from '../site-list-empty-list-message';
describe('SiteListEmptyListMessage', function () {
    it('should render correctly', function () {
        var testProps = {
            searchTerm: 'search term',
            displayText: defaultSiteListDisplayText,
            assets: defaultSiteListAssets
        };
        compareToSnapshot(React.createElement(SiteListEmptyListMessage, __assign({}, testProps)));
    });
});
//# sourceMappingURL=site-list-empty-list-message.test.js.map