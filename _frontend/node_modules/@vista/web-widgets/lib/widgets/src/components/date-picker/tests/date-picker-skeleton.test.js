import * as React from 'react';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { DatePickerSkeleton } from '../date-picker-skeleton';
describe('DatePickerSkeleton', function () {
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(DatePickerSkeleton, null));
    });
});
//# sourceMappingURL=date-picker-skeleton.test.js.map