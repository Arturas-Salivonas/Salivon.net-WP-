import { OrderPaymentFormBootstrapper } from './types/bootstrap';
declare module '../../bootstrap/widgets' {
    interface Widgets {
        /**
         * Order Payment Form widget. Renders a form that allows a user to pay for an in-progress order.
         */
        orderPaymentForm: OrderPaymentFormBootstrapper;
    }
}
/**
 * Renders the Order Payment Form widget.
 * @param bootstrap
 */
export declare const orderPaymentForm: OrderPaymentFormBootstrapper;
