import { TicketPickerWidgetDisplayText } from '../types/display-text';
/**
 * Maps the display text provided via the API to the display text required by the Ticket Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export declare function mapTicketPickerDisplayText(displayText?: Partial<TicketPickerWidgetDisplayText>): TicketPickerWidgetDisplayText;
/**
 * Validates the display text values provided for the Ticket Picker.
 * Removes any invalid values and logs appropriate warnings.
 */
export declare function getValidatedTicketPickerDisplayText(displayText: Partial<TicketPickerWidgetDisplayText>): Partial<TicketPickerWidgetDisplayText>;
