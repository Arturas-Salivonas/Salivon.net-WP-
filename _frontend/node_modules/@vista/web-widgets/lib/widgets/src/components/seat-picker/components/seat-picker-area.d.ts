import * as React from 'react';
import { Area } from '../../../../../types/seating/area';
import { AreaCategory } from '../../../../../types/seating/area-category';
import { Seat } from '../../../../../types/seating/seat';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerBoundary } from '../types/seat-picker-measurements';
export interface SeatPickerAreaProps {
    area: Area;
    areaCategory: AreaCategory;
    scaleFactor: number;
    config: SeatPickerWidgetConfig;
    mapBoundary: SeatPickerBoundary;
    onSeatClicked: (selectedSeat: Seat) => void;
    limitSeatSelectionByAreaCategoryAllocation: boolean;
}
export declare class SeatPickerArea extends React.Component<SeatPickerAreaProps> {
    render(): JSX.Element;
    isAreaSelectable(areaCategory: AreaCategory): boolean;
}
