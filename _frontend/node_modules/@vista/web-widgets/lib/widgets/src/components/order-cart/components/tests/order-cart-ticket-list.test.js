var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { orderStoreInstance } from '../../../../../../api/src/stores/order/order-store';
import { exampleOrder } from '../../../../../../example-data/order/order';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultOrderCartDisplayText } from '../../types/display-text';
import { OrderCartTicketList } from '../order-cart-ticket-list';
describe('OrderCartTicketList', function () {
    beforeAll(function () {
        orderStoreInstance.actions.setOrderData(exampleOrder);
    });
    it('should render correctly', function () {
        var testProps = {
            orderTickets: exampleOrder.showtimes[0].tickets,
            orderStore: orderStoreInstance,
            showtimeId: exampleOrder.showtimes[0].showtimeId,
            displayText: defaultOrderCartDisplayText
        };
        compareToSnapshot(React.createElement(OrderCartTicketList, __assign({}, testProps)));
    });
    it('should return null if no tickets are passed in', function () {
        var testProps = {
            orderTickets: [],
            orderStore: orderStoreInstance,
            showtimeId: exampleOrder.showtimes[0].showtimeId,
            displayText: defaultOrderCartDisplayText
        };
        compareToSnapshot(React.createElement(OrderCartTicketList, __assign({}, testProps)));
    });
});
//# sourceMappingURL=order-cart-ticket-list.test.js.map