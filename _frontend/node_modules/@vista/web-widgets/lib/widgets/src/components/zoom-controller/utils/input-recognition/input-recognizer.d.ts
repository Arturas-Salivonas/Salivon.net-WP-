import { Point } from '../measurement-utils';
/**
 * Base class for handling mouse and touch inputs.
 * Contains common processing logic for panning.
 */
export declare abstract class InputRecognizer {
    private panThreshold;
    private firstPanPoint?;
    private lastPanPoint?;
    private onPanHandler?;
    onPan: (onPanHandler: (translation: Point) => boolean) => (translation: Point) => boolean;
    protected handlePan(event: TouchEvent | MouseEvent): boolean;
    protected reset(): void;
}
export declare function isTouchEvent(event: Event): event is TouchEvent;
