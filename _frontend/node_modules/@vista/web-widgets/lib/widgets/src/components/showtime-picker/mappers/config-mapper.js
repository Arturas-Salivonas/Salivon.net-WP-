var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultShowtimePickerConfig } from '../types/config';
/**
 * Maps the config provided via the API to the config required by the Showtime Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 * @param config
 */
export function mapShowtimePickerConfig(config) {
    if (!config)
        return defaultShowtimePickerConfig;
    var valid = new Validator().ofType('object').validate(config, 'Showtime Picker config');
    if (!valid)
        return defaultShowtimePickerConfig;
    var validatedConfig = getValidatedShowtimePickerConfig(config);
    return __assign({}, defaultShowtimePickerConfig, validatedConfig);
}
function getValidatedShowtimePickerConfig(config) {
    var validatedConfig = __assign({}, config, { actionButton: mapShowtimePickerActionButtonConfig(config.actionButton) });
    if ('showFilmDetails' in config) {
        var valid = new Validator()
            .ofType('boolean')
            .validate(config.showFilmDetails, 'Showtime Picker showFilmDetails');
        if (!valid)
            delete validatedConfig.showFilmDetails;
    }
    if ('showSiteDetails' in config) {
        var valid = new Validator()
            .ofType('boolean')
            .validate(config.showSiteDetails, 'Showtime Picker showSiteDetails');
        if (!valid)
            delete validatedConfig.showSiteDetails;
    }
    if ('heroAttributeColors' in config) {
        var valid = new Validator()
            .ofType('object')
            .allowUndefined()
            .validate(config.heroAttributeColors, 'Showtime Picker heroAttributeColors');
        if (!valid)
            delete validatedConfig.heroAttributeColors;
    }
    if ('actionButton' in config) {
        var valid = new Validator()
            .ofType('object')
            .validate(config.actionButton, 'Showtime Picker actionButton');
        if (!valid)
            delete validatedConfig.actionButton;
    }
    return validatedConfig;
}
export function mapShowtimePickerActionButtonConfig(config) {
    var defaultConfig = defaultShowtimePickerConfig.actionButton;
    if (!config)
        return defaultConfig;
    var valid = new Validator().ofType('object').validate(config, 'Film details action button config');
    if (!valid)
        return defaultConfig;
    var validatedConfig = getValidatedShowtimePickerActionButtonConfig(config);
    return __assign({}, defaultConfig, validatedConfig);
}
function getValidatedShowtimePickerActionButtonConfig(config) {
    if ('show' in config) {
        var valid = new Validator()
            .ofType('boolean')
            .validate(config.show, 'Showtime Picker action button show');
        if (!valid)
            delete config.show;
    }
    return config;
}
//# sourceMappingURL=config-mapper.js.map