import { clamp } from '../clamp';
describe('clamp', function () {
    var min = 0;
    var max = 100;
    var clampFn = clamp(min, max);
    it('returns the min when value is less than min', function () {
        var result = clampFn(min - 1);
        expect(result).toEqual(min);
    });
    it('returns the min when value is equal to min', function () {
        var result = clampFn(min);
        expect(result).toEqual(min);
    });
    it('returns the max when value is greater than max', function () {
        var result = clampFn(max + 1);
        expect(result).toEqual(max);
    });
    it('returns the max when value is equal to max', function () {
        var result = clampFn(max);
        expect(result).toEqual(max);
    });
    it('returns a value when it is within limits', function () {
        var result = clampFn(50);
        expect(result).toEqual(50);
    });
});
//# sourceMappingURL=clamp.test.js.map