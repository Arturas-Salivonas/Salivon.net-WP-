import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { FilmDetailsSummaryProperty } from '../film-details-summary-property';
describe('FilmDetailsSummaryProperty', function () {
    it('should not render when content is undefined', function () {
        compareToSnapshot(React.createElement(FilmDetailsSummaryProperty, { title: "Title" }));
    });
    it('should not render when content is empty', function () {
        compareToSnapshot(React.createElement(FilmDetailsSummaryProperty, { title: "Title", content: "" }));
    });
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(FilmDetailsSummaryProperty, { title: "Title", content: "Content" }));
    });
});
//# sourceMappingURL=film-details-summary-property.test.js.map