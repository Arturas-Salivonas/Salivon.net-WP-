import * as React from 'react';
import { FormElementProps } from '../form-element-props';
export interface TextAreaProps extends FormElementProps<string | number> {
    rows?: number;
    columns?: number;
}
/**
 * Renders a textarea component.
 */
export declare class TextArea extends React.Component<TextAreaProps> {
    render(): JSX.Element;
    handleChange: (event: any) => void;
}
