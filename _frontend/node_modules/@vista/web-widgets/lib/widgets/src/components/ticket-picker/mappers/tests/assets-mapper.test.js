import { defaultTicketPickerAssets } from '../../types/assets';
import { mapTicketPickerAssets } from '../assets-mapper';
describe('mapTicketPickerAssets', function () {
    it('returns the default assets when the provided asset is undefined', function () {
        var result = mapTicketPickerAssets(undefined);
        expect(result).toEqual(defaultTicketPickerAssets);
    });
    it('returns the default asset when the provided asset is invalid', function () {
        var result = mapTicketPickerAssets(true);
        expect(result).toEqual(defaultTicketPickerAssets);
    });
    var propertyTestCases = function (defaultValue) {
        return [
            { value: undefined, expected: defaultValue },
            { value: null, expected: defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    describe('failedToLoadDataIconName', function () {
        var testCases = propertyTestCases(defaultTicketPickerAssets.failedToLoadDataIconName);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var assets = {
                    failedToLoadDataIconName: testCase.value
                };
                var result = mapTicketPickerAssets(assets);
                expect(result.failedToLoadDataIconName).toEqual(testCase.expected);
            });
        });
    });
});
//# sourceMappingURL=assets-mapper.test.js.map