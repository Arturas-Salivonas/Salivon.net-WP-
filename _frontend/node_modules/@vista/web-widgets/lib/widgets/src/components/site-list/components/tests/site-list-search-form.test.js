var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleSites } from '../../../../../../example-data/browsing/site';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultSiteListDisplayText } from '../../types/display-text';
import { SiteListSearchForm } from '../site-list-search-form';
describe('SiteListSearchForm', function () {
    it('should render correctly', function () {
        var testProps = {
            sites: exampleSites,
            displayText: defaultSiteListDisplayText,
            onResultsChanged: function () { return; },
            onSearchTermChanged: function () { return; }
        };
        compareToSnapshot(React.createElement(SiteListSearchForm, __assign({}, testProps)));
    });
});
//# sourceMappingURL=site-list-search-form.test.js.map