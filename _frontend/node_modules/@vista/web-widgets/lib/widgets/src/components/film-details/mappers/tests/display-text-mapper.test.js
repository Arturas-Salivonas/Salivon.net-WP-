import { defaultFilmDetailsWidgetDisplayText } from '../../types/display-text';
import { mapFilmDetailsDisplayText } from '../display-text-mapper';
describe('mapFilmDetailsDisplayText', function () {
    it('returns the default display text when the provided display text is undefined', function () {
        var result = mapFilmDetailsDisplayText(undefined);
        expect(result).toEqual(defaultFilmDetailsWidgetDisplayText);
    });
    it('returns the default display text when the provided display text is invalid', function () {
        var result = mapFilmDetailsDisplayText(true);
        expect(result).toEqual(defaultFilmDetailsWidgetDisplayText);
    });
    var properties = [
        'statusNowShowingLabel',
        'statusComingSoonLabel',
        'actionButtonLabel',
        'directorsLabel',
        'actorsLabel',
        'plotLabel',
        'runtimeLabel',
        'runtime',
        'releaseDateLabel',
        'releaseDate',
    ];
    var propertyTestCases = function (defaultValue, allowNull) {
        if (allowNull === void 0) { allowNull = false; }
        return [
            { value: undefined, expected: allowNull ? undefined : defaultValue },
            { value: null, expected: allowNull ? null : defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    properties.forEach(function (property) {
        describe(property, function () {
            var testCases = propertyTestCases(defaultFilmDetailsWidgetDisplayText[property]);
            testCases.forEach(function (testCase) {
                it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                    var _a;
                    var displayText = (_a = {},
                        _a[property] = testCase.value,
                        _a);
                    var result = mapFilmDetailsDisplayText(displayText);
                    expect(result[property]).toEqual(testCase.expected);
                });
            });
        });
    });
});
//# sourceMappingURL=display-text-mapper.test.js.map