import { addWidgetBootstrap } from '../../bootstrap/add-widget-bootstrap';
import { createComponent } from '../../bootstrap/create-component';
import { journeyStoreInstance } from './../../../../api/src/stores/journey/journey-store';
import { mapOrderDetailsDisplayText } from './mappers/display-text-mapper';
import { OrderDetails } from './order-details';
export var orderDetails = function (bootstrap) {
    var journeyReference = bootstrap.journeyReference, journeyId = bootstrap.journeyId;
    if (!journeyReference && !journeyId) {
        throw new Error('Cannot render the Order Details widget without a journeyReference or journeyId');
    }
    var props = {
        journeyReference: journeyReference,
        journeyId: journeyId,
        journeyStore: journeyStoreInstance,
        displayText: mapOrderDetailsDisplayText(bootstrap.displayText)
    };
    return createComponent(bootstrap.selector, OrderDetails, props);
};
addWidgetBootstrap('orderDetails', orderDetails);
//# sourceMappingURL=bootstrap.js.map