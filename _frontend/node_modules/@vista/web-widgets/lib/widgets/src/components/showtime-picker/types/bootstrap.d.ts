import { NestedPartial } from '../../../../../types/nested-partial';
import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { ShowtimePicker } from '../showtime-picker';
import { ShowtimePickerAssets } from './assets';
import { HeroAttributeColors, ShowtimePickerWidgetConfig } from './config';
import { ShowtimePickerDisplayText } from './display-text';
import { ShowtimePickerWidgetEvents } from './events';
/**
 * Bootstrap options to render the Showtime Picker widget.
 * @export
 * @interface ShowtimePickerWidgetBootstrap
 * @extends {WidgetBootstrap}
 */
export interface ShowtimePickerWidgetBootstrap extends WidgetBootstrap {
    /**
     * The IDs of the films to display in the Showtime Picker widget.
     */
    filmIds?: string[];
    /**
     * The IDs of the sites to display in the Showtime Picker widget.
     */
    siteIds?: string[];
    /**
     * Optionally provide a date string in the format 'YYYY-MM-DD' to set the initial selected date,
     * otherwise the date picker will select the first date with showtimes by default.
     */
    selectedDate?: string;
    /**
     * Configuration options for the Showtime Picker widget.
     */
    config?: NestedPartial<ShowtimePickerWidgetConfig, HeroAttributeColors>;
    /**
     * Display text for the Showtime Picker widget.
     */
    displayText?: Partial<ShowtimePickerDisplayText>;
    /**
     * Events for the Showtime Picker widget.
     */
    events?: ShowtimePickerWidgetEvents;
    /**
     * Assets for the Showtime Picker widget.
     */
    assets?: Partial<ShowtimePickerAssets>;
}
export declare type ShowtimePickerWidgetBootstrapper = Bootstrapper<ShowtimePicker, ShowtimePickerWidgetBootstrap>;
