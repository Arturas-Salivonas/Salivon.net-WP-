import * as React from 'react';
export interface TagsProps {
    tags: TagValue[];
    onTagClicked?: (tagId: string) => void;
    onTagButtonClicked?: (tagId: string) => void;
}
export interface TagValue {
    /**
     * A unique identifier for this tag
     */
    id: string;
    label?: string;
    buttonIcon?: string;
}
export declare class Tags extends React.Component<TagsProps> {
    render(): JSX.Element;
}
