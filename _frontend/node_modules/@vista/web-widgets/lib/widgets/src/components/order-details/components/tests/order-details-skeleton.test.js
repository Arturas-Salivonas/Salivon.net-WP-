import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { OrderDetailsSkeleton } from '../order-details-skeleton';
describe('OrderDetailsSkeleton', function () {
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(OrderDetailsSkeleton, null));
    });
});
//# sourceMappingURL=order-details-skeleton.test.js.map