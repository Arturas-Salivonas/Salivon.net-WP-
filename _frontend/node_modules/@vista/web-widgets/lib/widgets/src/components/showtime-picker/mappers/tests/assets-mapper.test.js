import { defaultShowtimePickerAssets } from '../../types/assets';
import { mapShowtimePickerAssets } from '../assets-mapper';
describe('mapShowtimePickerAssets', function () {
    it('returns the default assets when the provided display text is undefined', function () {
        var result = mapShowtimePickerAssets(undefined);
        expect(result).toEqual(defaultShowtimePickerAssets);
    });
    it('returns the default display text when the provided display text is invalid', function () {
        var result = mapShowtimePickerAssets(true);
        expect(result).toEqual(defaultShowtimePickerAssets);
    });
    var propertyTestCases = function (defaultValue) {
        return [
            { value: undefined, expected: defaultValue },
            { value: null, expected: defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    describe('noSitesSelectedIconName', function () {
        var testCases = propertyTestCases(defaultShowtimePickerAssets.noSitesSelectedIconName);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    noSitesSelectedIconName: testCase.value
                };
                var result = mapShowtimePickerAssets(displayText);
                expect(result.noSitesSelectedIconName).toEqual(testCase.expected);
            });
        });
    });
    describe('noShowtimesIconName', function () {
        var testCases = propertyTestCases(defaultShowtimePickerAssets.noShowtimesIconName);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    noShowtimesIconName: testCase.value
                };
                var result = mapShowtimePickerAssets(displayText);
                expect(result.noShowtimesIconName).toEqual(testCase.expected);
            });
        });
    });
});
//# sourceMappingURL=assets-mapper.test.js.map