import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { SitePicker } from './../site-picker';
import { SitePickerAssets } from './assets';
import { SitePickerDisplayText } from './display-text';
/**
 * Bootstrap options to render the Site Picker widget.
 * @export
 * @interface SitePickerWidgetBootstrap
 * @extends {WidgetBootstrap}
 */
export interface SitePickerWidgetBootstrap extends WidgetBootstrap {
    /**
     * Display text for the Site Picker widget.
     */
    displayText?: Partial<SitePickerDisplayText>;
    /**
     * Assets for the Site Picker widget.
     */
    assets?: Partial<SitePickerAssets>;
}
export declare type SitePickerWidgetBootstrapper = Bootstrapper<SitePicker, SitePickerWidgetBootstrap>;
