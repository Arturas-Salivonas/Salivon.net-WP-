import { Point } from './measurement-utils';
/**
 * Returns the current window scroll position.
 */
export declare const getWindowScrollPosition: () => Point;
/**
 * Returns a window point relative to an HTMLElement.
 */
export declare const getRelativePoint: (windowPoint: Point, element: HTMLElement) => Point;
