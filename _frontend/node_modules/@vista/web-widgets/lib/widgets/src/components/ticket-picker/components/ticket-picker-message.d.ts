import * as React from 'react';
import { TicketPickerAssets } from '../types/assets';
import { TicketPickerWidgetDisplayText } from '../types/display-text';
export declare enum TicketPickerMessageType {
    FailedToLoadData = "FailedToLoadData"
}
export interface TicketPickerMessageProps {
    messageType: TicketPickerMessageType;
    displayText: TicketPickerWidgetDisplayText;
    assets: TicketPickerAssets;
}
export declare const TicketPickerMessage: React.FC<TicketPickerMessageProps>;
