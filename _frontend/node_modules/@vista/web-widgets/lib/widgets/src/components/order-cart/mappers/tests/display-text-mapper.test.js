import { defaultOrderCartDisplayText } from '../../types/display-text';
import { mapOrderCartDisplayText } from '../display-text-mapper';
describe('Order Cart Display Text Mapper', function () {
    it('returns the default display text when the provided display text is undefined', function () {
        var result = mapOrderCartDisplayText(undefined);
        expect(result).toEqual(defaultOrderCartDisplayText);
    });
    it('returns the default display text when the provided display text is invalid', function () {
        var result = mapOrderCartDisplayText(true);
        expect(result).toEqual(defaultOrderCartDisplayText);
    });
    var propertyTestCases = function (defaultValue, allowNull, allowEmpty) {
        if (allowNull === void 0) { allowNull = false; }
        if (allowEmpty === void 0) { allowEmpty = false; }
        return [
            { value: undefined, expected: allowNull ? undefined : defaultValue },
            { value: null, expected: allowNull ? null : defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: allowEmpty ? '' : defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    describe('ticketDetails', function () {
        var testCases = propertyTestCases(defaultOrderCartDisplayText.ticketDetails);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    ticketDetails: testCase.value
                };
                var result = mapOrderCartDisplayText(displayText);
                expect(result.ticketDetails).toEqual(testCase.expected);
            });
        });
    });
    describe('bookingFeeLabel', function () {
        var testCases = propertyTestCases(defaultOrderCartDisplayText.bookingFeeLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    bookingFeeLabel: testCase.value
                };
                var result = mapOrderCartDisplayText(displayText);
                expect(result.bookingFeeLabel).toEqual(testCase.expected);
            });
        });
    });
    describe('totalCostLabel', function () {
        var testCases = propertyTestCases(defaultOrderCartDisplayText.totalCostLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    totalCostLabel: testCase.value
                };
                var result = mapOrderCartDisplayText(displayText);
                expect(result.totalCostLabel).toEqual(testCase.expected);
            });
        });
    });
    describe('removeItemButtonLabel', function () {
        var testCases = propertyTestCases(defaultOrderCartDisplayText.removeItemButtonLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    removeItemButtonLabel: testCase.value
                };
                var result = mapOrderCartDisplayText(displayText);
                expect(result.removeItemButtonLabel).toEqual(testCase.expected);
            });
        });
    });
    describe('title', function () {
        var testCases = propertyTestCases(defaultOrderCartDisplayText.title);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    title: testCase.value
                };
                var result = mapOrderCartDisplayText(displayText);
                expect(result.title).toEqual(testCase.expected);
            });
        });
    });
    describe('actionButtonLabel', function () {
        var testCases = propertyTestCases(defaultOrderCartDisplayText.actionButtonLabel);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    actionButtonLabel: testCase.value
                };
                var result = mapOrderCartDisplayText(displayText);
                expect(result.actionButtonLabel).toEqual(testCase.expected);
            });
        });
    });
});
//# sourceMappingURL=display-text-mapper.test.js.map