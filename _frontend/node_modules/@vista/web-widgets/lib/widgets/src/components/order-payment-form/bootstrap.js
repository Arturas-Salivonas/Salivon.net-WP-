import { orderStoreInstance } from '../../../../api/src/stores/order/order-store';
import { addWidgetBootstrap } from '../../bootstrap/add-widget-bootstrap';
import { createComponent } from '../../bootstrap/create-component';
import { mapOrderPaymentFormAssets } from './mappers/assets-mapper';
import { mapOrderPaymentFormDisplayText } from './mappers/display-text-mapper';
import { OrderPaymentForm } from './order-payment-form';
/**
 * Renders the Order Payment Form widget.
 * @param bootstrap
 */
export var orderPaymentForm = function (bootstrap) {
    var selector = bootstrap.selector, displayText = bootstrap.displayText, assets = bootstrap.assets, events = bootstrap.events;
    var props = {
        orderStore: orderStoreInstance,
        displayText: mapOrderPaymentFormDisplayText(displayText),
        assets: mapOrderPaymentFormAssets(assets),
        events: events
    };
    return createComponent(selector, OrderPaymentForm, props);
};
addWidgetBootstrap('orderPaymentForm', orderPaymentForm);
//# sourceMappingURL=bootstrap.js.map