var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleSites } from '../../../../../../example-data/browsing/site';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { SitePickerSelectedSiteList } from '../site-picker-selected-site-list';
describe('SitePickerSelectedSiteList', function () {
    it('should render correctly', function () {
        var testProps = {
            sites: exampleSites,
            onSiteClicked: function () { return; }
        };
        compareToSnapshot(React.createElement(SitePickerSelectedSiteList, __assign({}, testProps)));
    });
    it('should return null when there are no selected sites', function () {
        var testProps = {
            sites: [],
            onSiteClicked: function () { return; }
        };
        compareToSnapshot(React.createElement(SitePickerSelectedSiteList, __assign({}, testProps)));
    });
});
//# sourceMappingURL=site-picker-selected-site-list.test.js.map