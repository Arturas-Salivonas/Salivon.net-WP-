import { NestedPartial } from '../../../../../types/nested-partial';
import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { FilmList } from '../film-list';
import { FilmListAssets } from './assets';
import { FilmListConfig } from './config';
import { FilmListDisplayText } from './display-text';
import { FilmListEvents } from './events';
/**
 * Bootstrap options to render the Film List widget.
 */
export interface FilmListBootstrap extends WidgetBootstrap {
    /**
     * Events for the Film Details widget.
     */
    events?: FilmListEvents;
    /**
     * Configuration options for the Film List widget.
     */
    config?: NestedPartial<FilmListConfig>;
    /**
     * Display text for the Film List widget.
     */
    displayText?: Partial<FilmListDisplayText>;
    /**
     * Assets for the Film List widget.
     */
    assets?: Partial<FilmListAssets>;
    /**
     * The unique identifier of the site to display films for.
     */
    siteId?: string;
}
export declare type FilmListBootstrapper = Bootstrapper<FilmList, FilmListBootstrap>;
