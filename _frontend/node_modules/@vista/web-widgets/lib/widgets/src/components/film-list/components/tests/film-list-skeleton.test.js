import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { FilmListMode } from '../../types/config';
import { FilmListSkeleton } from '../film-list-skeleton';
describe('FilmListSkeleton', function () {
    it('should render grid mode correctly', function () {
        compareToSnapshot(React.createElement(FilmListSkeleton, { mode: FilmListMode.Grid }));
    });
    it('should render carousel mode correctly', function () {
        compareToSnapshot(React.createElement(FilmListSkeleton, { mode: FilmListMode.Carousel }));
    });
});
//# sourceMappingURL=film-list-skeleton.test.js.map