var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { journeyStoreInstance } from '../../../../../../api/src/stores/journey/journey-store';
import { exampleJourneyStoreData } from '../../../../../../example-data/journey/journey-store-data';
import { exampleJourneyOrderShowtimes } from '../../../../../../example-data/journey/order-showtime';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultOrderDetailsDisplayText } from '../../types/display-text';
import { OrderDetailsShowtimeList } from '../order-details-showtime-list';
describe('OrderDetailsShowtimeList', function () {
    beforeAll(function () {
        journeyStoreInstance.setData(exampleJourneyStoreData);
    });
    it('should render nothing when no showtimes are passed in', function () {
        var testProps = {
            heading: 'Test heading',
            orderShowtimes: [],
            journeyStore: journeyStoreInstance,
            displayText: defaultOrderDetailsDisplayText
        };
        compareToSnapshot(React.createElement(OrderDetailsShowtimeList, __assign({}, testProps)));
    });
    it('should render with a heading', function () {
        var testProps = {
            heading: 'Test heading',
            orderShowtimes: exampleJourneyOrderShowtimes,
            journeyStore: journeyStoreInstance,
            displayText: defaultOrderDetailsDisplayText
        };
        compareToSnapshot(React.createElement(OrderDetailsShowtimeList, __assign({}, testProps)));
    });
    it('should render without a heading', function () {
        var testProps = {
            heading: undefined,
            orderShowtimes: exampleJourneyOrderShowtimes,
            journeyStore: journeyStoreInstance,
            displayText: defaultOrderDetailsDisplayText
        };
        compareToSnapshot(React.createElement(OrderDetailsShowtimeList, __assign({}, testProps)));
    });
});
//# sourceMappingURL=order-details-showtime-list.test.js.map