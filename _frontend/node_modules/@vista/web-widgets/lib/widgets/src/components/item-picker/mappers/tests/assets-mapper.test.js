import { defaultItemPickerAssets } from '../../types/assets';
import { mapItemPickerAssets } from '../assets-mapper';
describe('Item Picker Assets Mapper', function () {
    it('returns the default assets when the provided assets is undefined', function () {
        var result = mapItemPickerAssets(undefined);
        expect(result).toEqual(defaultItemPickerAssets);
    });
    it('returns the default assets when the provided assets is invalid', function () {
        var result = mapItemPickerAssets(true);
        expect(result).toEqual(defaultItemPickerAssets);
    });
    var propertyTestCases = function (defaultValue) {
        return [
            { value: undefined, expected: defaultValue },
            { value: null, expected: defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    describe('removeItemButtonIconName', function () {
        var testCases = propertyTestCases(defaultItemPickerAssets.failedToLoadDataIconName);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var assets = {
                    failedToLoadDataIconName: testCase.value
                };
                var result = mapItemPickerAssets(assets);
                expect(result.failedToLoadDataIconName).toEqual(testCase.expected);
            });
        });
    });
});
//# sourceMappingURL=assets-mapper.test.js.map