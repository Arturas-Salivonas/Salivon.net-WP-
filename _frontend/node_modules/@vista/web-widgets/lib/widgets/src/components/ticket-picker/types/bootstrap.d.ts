import { Component } from 'react';
import { NestedPartial } from '../../../../../types/nested-partial';
import { Bootstrapper, BootstrapResponse } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { TicketPickerAssets } from './assets';
import { TicketPickerWidgetConfig } from './config';
import { TicketPickerWidgetDisplayText } from './display-text';
import { TicketPickerWidgetEvents } from './events';
/**
 * Bootstrap options to render the Ticket Picker widget.
 * @export
 * @interface TicketPickerWidgetBootstrap
 * @extends {WidgetBootstrap}
 */
export interface TicketPickerWidgetBootstrap extends WidgetBootstrap {
    /**
     * The ID of the showtime in the order to add tickets to.
     */
    showtimeId: string;
    /**
     * Events for the Ticket Picker widget.
     */
    events?: TicketPickerWidgetEvents;
    /**
     * Configuration options for the Ticket Picker widget.
     */
    config?: NestedPartial<TicketPickerWidgetConfig>;
    /**
     * Display text for the Ticket Picker widget.
     */
    displayText?: Partial<TicketPickerWidgetDisplayText>;
    /**
     * Assets for the Ticket Picker widget.
     */
    assets?: Partial<TicketPickerAssets>;
}
export interface TicketPickerWidgetBootstrapResponse extends BootstrapResponse<Component> {
    /**
     * Validates the currently selected tickets. Returns true if the selections are valid, false if not.
     */
    validateTicketSelection: () => boolean;
    /**
     * Saves the current ticket selection.
     */
    saveTicketSelection: () => void;
}
export declare type TicketPickerWidgetBootstrapper = Bootstrapper<Component, TicketPickerWidgetBootstrap, TicketPickerWidgetBootstrapResponse>;
