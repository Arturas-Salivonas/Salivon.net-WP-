import { defaultShowtimePickerConfig } from '../../types/config';
import { mapShowtimePickerConfig } from '../config-mapper';
describe('mapShowtimePickerConfig', function () {
    it('returns the default config when the provided config is undefined', function () {
        var result = mapShowtimePickerConfig(undefined);
        expect(result).toEqual(defaultShowtimePickerConfig);
    });
    it('returns the default config when the provided config is invalid', function () {
        var result = mapShowtimePickerConfig(true);
        expect(result).toEqual(defaultShowtimePickerConfig);
    });
});
//# sourceMappingURL=config-mapper.test.js.map