import * as React from 'react';
import { SeatPickerWidgetConfig, SeatPickerWidgetLegendItemsConfig } from '../types/config';
import { SeatPickerWidgetDisplayText } from '../types/display-text';
export interface SeatPickerLegendProps {
    config: SeatPickerWidgetConfig;
    displayText: SeatPickerWidgetDisplayText;
}
export declare class SeatPickerLegend extends React.Component<SeatPickerLegendProps> {
    render(): JSX.Element;
    getItemArray(config: SeatPickerWidgetLegendItemsConfig, displayText: SeatPickerWidgetDisplayText): SeatPickerLegendItemDetails[];
    getLabelForItem(displayText: SeatPickerWidgetDisplayText, name: string): string;
}
export interface SeatPickerLegendItemDetails {
    name: string;
    icon?: string | null;
    index: number;
    label: string;
}
