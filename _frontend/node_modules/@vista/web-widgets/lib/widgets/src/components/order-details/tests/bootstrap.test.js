import { journeyStoreInstance } from '../../../../../api/src/stores/journey/journey-store';
import * as createComponent from '../../../bootstrap/create-component';
import { orderDetails } from '../bootstrap';
import { defaultOrderDetailsDisplayText } from '../types/display-text';
describe('orderDetails bootstrap', function () {
    var component = { _component: {} };
    var createComponentSpy = jest.spyOn(createComponent, 'createComponent').mockReturnValue(component);
    var journeyReference = '123';
    afterEach(function () {
        jest.clearAllMocks();
    });
    it('calls the create component function', function () {
        var response = orderDetails({
            selector: 'test',
            journeyReference: journeyReference
        });
        expect(createComponentSpy).toBeCalledWith('test', expect.anything(), {
            journeyReference: journeyReference,
            journeyStore: journeyStoreInstance,
            displayText: defaultOrderDetailsDisplayText
        });
        expect(response).toEqual(component);
    });
    it('throws an error when no journeyReference has been passed in', function () {
        expect(function () { return orderDetails({
            selector: 'test',
            journeyReference: undefined
        }); }).toThrowError(/journeyReference/);
    });
});
//# sourceMappingURL=bootstrap.test.js.map