import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { TicketPickerSkeleton } from '../ticket-picker-skeleton';
describe('TicketPickerSkeleton', function () {
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(TicketPickerSkeleton, null));
    });
});
//# sourceMappingURL=ticket-picker-skeleton.test.js.map