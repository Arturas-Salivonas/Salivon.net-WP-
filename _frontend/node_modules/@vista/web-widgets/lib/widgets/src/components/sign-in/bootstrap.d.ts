import { LoyaltyAuthResponse } from '../../../../types/loyalty/authentication/loyalty-auth';
import { SignInWidgetBootstrapper } from './types/bootstrap';
declare module '../../bootstrap/widgets' {
    interface Widgets {
        /**
         * Sign In widget. Allows a customer to sign in to their loyalty account
         */
        signIn: SignInWidgetBootstrapper;
    }
}
export declare type UserCredentialSignIn = (userIdentifier: string, password: string, returnMemberDetails: boolean) => Promise<LoyaltyAuthResponse>;
export declare type SocialSignIn = (returnMemberDetails: boolean) => Promise<string>;
export declare const signIn: SignInWidgetBootstrapper;
