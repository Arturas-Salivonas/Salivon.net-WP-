import * as React from 'react';
import { SiteListAssets } from '../types/assets';
import { SiteListDisplayText } from '../types/display-text';
export declare enum SiteListMessageType {
    FailedToLoadData = "FailedToLoadData"
}
export interface SiteListMessageProps {
    messageType: SiteListMessageType;
    displayText: SiteListDisplayText;
    assets: SiteListAssets;
}
export declare const SiteListMessage: React.FC<SiteListMessageProps>;
