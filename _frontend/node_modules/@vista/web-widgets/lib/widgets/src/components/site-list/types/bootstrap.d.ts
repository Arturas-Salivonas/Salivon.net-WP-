import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { SiteList } from './../site-list';
import { SiteListAssets } from './assets';
import { SiteListDisplayText } from './display-text';
import { SiteListEvents } from './events';
/**
 * Bootstrap options to render the Site List widget.
 * @export
 * @interface SiteListWidgetBootstrap
 * @extends {WidgetBootstrap}
 */
export interface SiteListWidgetBootstrap extends WidgetBootstrap {
    /**
     * Display text for the Site List widget.
     */
    displayText?: Partial<SiteListDisplayText>;
    /**
     * Assets for the Site List widget.
     */
    assets?: Partial<SiteListAssets>;
    /**
     * Events for the Site List widget.
     */
    events?: Partial<SiteListEvents>;
}
export declare type SiteListWidgetBootstrapper = Bootstrapper<SiteList, SiteListWidgetBootstrap>;
