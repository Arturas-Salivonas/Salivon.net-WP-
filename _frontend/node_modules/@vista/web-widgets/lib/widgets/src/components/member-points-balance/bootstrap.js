import { loyaltyStoreInstance } from '../../../../api/src/stores/loyalty/loyalty-store';
import { addWidgetBootstrap } from '../../bootstrap/add-widget-bootstrap';
import { createComponent } from '../../bootstrap/create-component';
import { mapMemberPointsBalanceDisplayText } from './mappers/display-text-mapper';
import { MemberPointsBalance } from './member-points-balance';
export var memberPointBalance = function (bootstrap) {
    var selector = bootstrap.selector, displayText = bootstrap.displayText;
    var props = {
        loyaltyStore: loyaltyStoreInstance,
        displayText: mapMemberPointsBalanceDisplayText(displayText),
    };
    return createComponent(selector, MemberPointsBalance, props);
};
addWidgetBootstrap('memberPointsBalance', memberPointBalance);
//# sourceMappingURL=bootstrap.js.map