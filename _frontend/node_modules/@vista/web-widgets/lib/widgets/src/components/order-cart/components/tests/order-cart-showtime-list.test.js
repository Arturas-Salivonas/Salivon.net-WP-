var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { orderStoreInstance } from '../../../../../../api/src/stores/order/order-store';
import { exampleOrder } from '../../../../../../example-data/order/order';
import { exampleOrderShowtime } from '../../../../../../example-data/order/order-showtime';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultOrderCartDisplayText } from '../../types/display-text';
import { OrderCartShowtimeList } from '../order-cart-showtime-list';
describe('OrderCartShowtimeList', function () {
    beforeAll(function () {
        orderStoreInstance.actions.setOrderData(exampleOrder);
    });
    it('should render correctly', function () {
        var testProps = {
            orderShowtimes: [exampleOrderShowtime],
            orderStore: orderStoreInstance,
            displayText: defaultOrderCartDisplayText
        };
        compareToSnapshot(React.createElement(OrderCartShowtimeList, __assign({}, testProps)));
    });
    it('should return null if no showtimes are passed in', function () {
        var testProps = {
            orderShowtimes: [],
            orderStore: orderStoreInstance,
            displayText: defaultOrderCartDisplayText
        };
        compareToSnapshot(React.createElement(OrderCartShowtimeList, __assign({}, testProps)));
    });
});
//# sourceMappingURL=order-cart-showtime-list.test.js.map