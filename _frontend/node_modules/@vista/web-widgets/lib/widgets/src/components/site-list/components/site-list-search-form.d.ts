import * as React from 'react';
import { Site } from '../../../../../types/browsing/site';
import { SiteListDisplayText } from '../types/display-text';
export interface SiteListSearchFormProps {
    sites: Site[];
    displayText: SiteListDisplayText;
    onResultsChanged: (filteredSites: Site[]) => void;
    onSearchTermChanged: (searchTerm: string) => void;
}
export declare class SiteListSearchForm extends React.Component<SiteListSearchFormProps> {
    render(): JSX.Element;
    private handleSubmit;
}
