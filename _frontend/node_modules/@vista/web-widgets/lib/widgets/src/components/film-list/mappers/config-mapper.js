var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultFilmListConfig } from '../types/config';
import { FilmCategory } from './../../../../../types/browsing/film-category';
import { FilmListMode } from './../types/config';
/**
 * Maps the config provided via the API to the config required by the Film List widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 * @param config
 */
export function mapFilmListConfig(config) {
    if (!config)
        return defaultFilmListConfig;
    var valid = new Validator().ofType('object').validate(config, 'Film List config');
    if (!valid)
        return defaultFilmListConfig;
    var validatedConfig = getValidatedFilmListConfig(config);
    return __assign({}, defaultFilmListConfig, validatedConfig);
}
function getValidatedFilmListConfig(config) {
    if ('mode' in config) {
        var valid = new Validator()
            .ofType('string')
            .includedInValues(Object.values(FilmListMode))
            .validate(config.mode, 'Film List mode');
        if (!valid)
            delete config.mode;
    }
    if ('categories' in config) {
        var valid = new Validator()
            .ofType('array')
            .minLength(1)
            .includedInValues(Object.values(FilmCategory))
            .validate(config.categories, 'Film List categories');
        if (!valid)
            delete config.categories;
    }
    return config;
}
//# sourceMappingURL=config-mapper.js.map