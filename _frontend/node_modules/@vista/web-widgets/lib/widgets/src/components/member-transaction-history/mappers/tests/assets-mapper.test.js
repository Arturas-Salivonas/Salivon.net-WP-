import { defaultMemberTransactionHistoryAssets } from '../../types/assets';
import { mapMemberTransactionHistoryAssets } from '../assets-mapper';
describe('mapMemberTransactionHistoryAssets', function () {
    it('returns the default assets when the provided display text is undefined', function () {
        var result = mapMemberTransactionHistoryAssets(undefined);
        expect(result).toEqual(defaultMemberTransactionHistoryAssets);
    });
    it('returns the default display text when the provided display text is invalid', function () {
        var result = mapMemberTransactionHistoryAssets(true);
        expect(result).toEqual(defaultMemberTransactionHistoryAssets);
    });
    var propertyTestCases = function (defaultValue) {
        return [
            { value: undefined, expected: defaultValue },
            { value: null, expected: defaultValue },
            { value: 0, expected: defaultValue },
            { value: '', expected: defaultValue },
            { value: {}, expected: defaultValue },
            { value: [], expected: defaultValue },
            { value: 'valid', expected: 'valid' },
        ];
    };
    describe('noTransactionHistoryIconName', function () {
        var testCases = propertyTestCases(defaultMemberTransactionHistoryAssets.noTransactionHistoryIconName);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    noTransactionHistoryIconName: testCase.value
                };
                var result = mapMemberTransactionHistoryAssets(displayText);
                expect(result.noTransactionHistoryIconName).toEqual(testCase.expected);
            });
        });
    });
    describe('failedToLoadDataIconName', function () {
        var testCases = propertyTestCases(defaultMemberTransactionHistoryAssets.failedToLoadDataIconName);
        testCases.forEach(function (testCase) {
            it("returns '" + testCase.expected + "' when '" + testCase.value + "' is passed in", function () {
                var displayText = {
                    failedToLoadDataIconName: testCase.value
                };
                var result = mapMemberTransactionHistoryAssets(displayText);
                expect(result.failedToLoadDataIconName).toEqual(testCase.expected);
            });
        });
    });
});
//# sourceMappingURL=assets-mapper.test.js.map