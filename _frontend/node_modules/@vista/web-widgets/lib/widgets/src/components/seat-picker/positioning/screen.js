import { SeatPickerWidgetScreenPosition } from '../types/config';
import { getMapDimension } from './map';
/**
 * Returns the boundary of the screen in SVG units.
 */
export function getScreenBoundary(seatLayout, mapBoundary, scaleFactor, config) {
    var invertSeatMap = config.screen.position === SeatPickerWidgetScreenPosition.Top;
    var leftPercentage = invertSeatMap
        ? 100 - seatLayout.screenStart - seatLayout.screenWidth - mapBoundary.left
        : seatLayout.screenStart - mapBoundary.left;
    var topPercentage = invertSeatMap ? 0 : mapBoundary.height;
    var topMarginOffset = invertSeatMap ? 0 : config.screen.margin;
    return {
        top: getMapDimension(topPercentage, scaleFactor) + topMarginOffset,
        left: getMapDimension(leftPercentage, scaleFactor),
        width: getMapDimension(seatLayout.screenWidth, scaleFactor),
        height: config.screen.height
    };
}
//# sourceMappingURL=screen.js.map