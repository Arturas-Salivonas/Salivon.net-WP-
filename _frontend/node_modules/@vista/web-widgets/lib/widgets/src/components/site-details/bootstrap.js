import { browsingStoreInstance } from '../../../../api/src/stores/browsing/browsing-store';
import { addWidgetBootstrap } from '../../bootstrap/add-widget-bootstrap';
import { createComponent } from '../../bootstrap/create-component';
import { mapSiteDetailsAssets } from './mappers/assets-mapper';
import { mapSiteDetailsDisplayText } from './mappers/display-text-mapper';
import { SiteDetails } from './site-details';
/**
 * Renders the site details widget.
 * @param bootstrap
 */
export var siteDetails = function (bootstrap) {
    var siteId = bootstrap.siteId, displayText = bootstrap.displayText, selector = bootstrap.selector, assets = bootstrap.assets;
    if (!siteId)
        throw new Error('Cannot render the site details widget without a site id');
    var props = {
        browsingStore: browsingStoreInstance,
        siteId: siteId,
        displayText: mapSiteDetailsDisplayText(displayText),
        assets: mapSiteDetailsAssets(assets)
    };
    return createComponent(selector, SiteDetails, props);
};
addWidgetBootstrap('siteDetails', siteDetails);
//# sourceMappingURL=bootstrap.js.map