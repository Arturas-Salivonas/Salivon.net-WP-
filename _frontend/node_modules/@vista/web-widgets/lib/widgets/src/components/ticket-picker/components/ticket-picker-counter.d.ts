import * as React from 'react';
import { OrderStore } from '../../../../../api/src/stores/order/order-store';
import { CounterStyle } from '../../counter/counter';
import { TicketPickerWidgetDisplayText } from '../types/display-text';
export interface TicketPickerCounterProps {
    orderStore: OrderStore;
    showtimeId: string;
    style: CounterStyle;
    displayText: TicketPickerWidgetDisplayText;
}
export declare class TicketPickerCounter extends React.Component<TicketPickerCounterProps> {
    render(): JSX.Element | null;
}
