import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { TicketPickerTableHeader } from '../ticket-picker-table-header';
describe('TicketPickerTableHeader', function () {
    it('should render correctly', function () {
        var displayText = {
            tableColumnItemHeaderLabel: 'Item',
            tableColumnPriceHeaderLabel: 'Price',
            tableColumnQuantityHeaderLabel: 'Quantity',
            tableColumnSubtotalHeaderLabel: 'Subtotal'
        };
        compareToSnapshot(React.createElement(TicketPickerTableHeader, { displayText: displayText }));
    });
});
//# sourceMappingURL=ticket-picker-table-header.test.js.map