/**
 * Base interface for custom captcha implementations
 */
export interface CaptchaBase {
    /**
     * Clears any existing captcha test result
     */
    reset(): void;
    /**
     * Manually runs the captcha test and updates the captcha test result
     */
    runCaptchaTest(): void;
}
