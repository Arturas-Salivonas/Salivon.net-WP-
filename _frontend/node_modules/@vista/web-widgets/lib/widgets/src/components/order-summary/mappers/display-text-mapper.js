var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultOrderSummaryDisplayText } from '../types/display-text';
/**
 * Maps the display text provided via the API to the display text required by the Order Summary widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapOrderSummaryDisplayText(displayText) {
    if (!displayText)
        return defaultOrderSummaryDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Order Summary display text');
    if (!valid)
        return defaultOrderSummaryDisplayText;
    var validatedDisplayText = getValidatedOrderSummaryDisplayText(displayText);
    return __assign({}, defaultOrderSummaryDisplayText, validatedDisplayText);
}
/**
 * Validates the display text values provided for the Order Summary widget.
 * Removes any invalid values and logs appropriate warnings.
 */
function getValidatedOrderSummaryDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'pendingStatusLabel', 'Order Summary pendingStatusLabel');
    validateField(validator, 'admittingStatusLabel', 'Order Summary admittingStatusLabel');
    validateField(validator, 'nowPlayingStatusLabel', 'Order Summary nowPlayingStatusLabel');
    validateField(validator, 'admittedStatusLabel', 'Order Summary admittedStatusLabel');
    validateField(validator, 'refundedStatusLabel', 'Order Summary refundedStatusLabel');
    validateField(validator, 'expiredStatusLabel', 'Order Summary expiredStatusLabel');
    validateField(validator, 'showtimeDetails', 'Order Summary showtimeDetails');
    validateField(validator, 'loadingOrderLabel', 'Order Summary loadingOrderLabel');
    validateField(validator, 'failedToLoadDataTitle', 'Order Summary loadingOrderLabel');
    validateField(validator, 'failedToLoadDataMessage', 'Order Summary loadingOrderLabel');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map