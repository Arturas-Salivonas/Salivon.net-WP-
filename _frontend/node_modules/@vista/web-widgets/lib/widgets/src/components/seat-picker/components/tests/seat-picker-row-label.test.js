var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { SeatPickerRowLabel } from '../seat-picker-row-label';
describe('SeatPickerRowLabel', function () {
    it('should render a seat picker row label', function () {
        var props = {
            left: 0,
            top: 100,
            width: 25,
            height: 30,
            label: 'G',
            borderRadius: 5,
            borderWidth: 2
        };
        compareToSnapshot(React.createElement(SeatPickerRowLabel, __assign({}, props)));
    });
});
//# sourceMappingURL=seat-picker-row-label.test.js.map