import { Area } from '../../../../../types/seating/area';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerBoundary } from '../types/seat-picker-measurements';
/**
 * Returns the boundary of an area in SVG units.
 */
export declare function getAreaBoundary(area: Area, mapBoundary: SeatPickerBoundary, scaleFactor: number, config: SeatPickerWidgetConfig): SeatPickerBoundary;
