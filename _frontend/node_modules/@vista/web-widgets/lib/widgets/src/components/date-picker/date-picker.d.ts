import * as React from 'react';
import { BusinessDate } from '../../../../types/browsing/business-date';
import { LoadingState } from '../../../../types/loading-state';
export interface DatePickerProps {
    loadingState: LoadingState;
    businessDates: BusinessDate[];
    selectedBusinessDate: BusinessDate;
    onDateClicked: (businessDate: BusinessDate) => void;
}
export declare class DatePicker extends React.Component<DatePickerProps> {
    render(): JSX.Element | null;
}
