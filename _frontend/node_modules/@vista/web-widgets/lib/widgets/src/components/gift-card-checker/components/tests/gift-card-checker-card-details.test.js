import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultGiftCardCheckerDisplayText } from '../../types/display-text';
import { GiftCardCheckerCardDetails } from '../gift-card-checker-card-details';
describe('GiftCardCheckerCardDetails', function () {
    it('renders correctly', function () {
        var cardDetails = {
            cardNumber: '123456789101112',
            balance: 19.23,
            expiryDate: new Date('12 Dec 2018')
        };
        compareToSnapshot(React.createElement(GiftCardCheckerCardDetails, { displayText: defaultGiftCardCheckerDisplayText, cardDetails: cardDetails }));
    });
});
//# sourceMappingURL=gift-card-checker-card-details.test.js.map