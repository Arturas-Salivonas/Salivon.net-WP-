import { NestedPartial } from '../../../../../types/nested-partial';
import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { VoucherInputForm } from '../voucher-input-form';
import { VoucherInputFormWidgetConfig } from './config';
import { VoucherInputFormWidgetDisplayText } from './display-text';
/**
 * Bootstrap options to render the voucher input form widget.
 * @export
 * @interface VoucherInputFormWidgetBootstrap
 * @extends {WidgetBootstrap}
 */
export interface VoucherInputFormWidgetBootstrap extends WidgetBootstrap {
    /**
     * The ID of the showtime in the order to add vouchers to.
     */
    showtimeId: string;
    /**
     * Configuration options for the voucher input form widget.
     */
    config?: NestedPartial<VoucherInputFormWidgetConfig>;
    /**
     * Display text for the voucher input form widget.
     */
    displayText?: Partial<VoucherInputFormWidgetDisplayText>;
}
export declare type VoucherInputFormWidgetBootstrapper = Bootstrapper<VoucherInputForm, VoucherInputFormWidgetBootstrap>;
