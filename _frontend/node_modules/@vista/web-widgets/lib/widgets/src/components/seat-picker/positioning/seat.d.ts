import { Area } from '../../../../../types/seating/area';
import { Seat } from '../../../../../types/seating/seat';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerBoundary } from '../types/seat-picker-measurements';
/**
 * Returns the boundary of a seat in SVG units.
 */
export declare function getSeatBoundary(seat: Seat, area: Area, scaleFactor: number, config: SeatPickerWidgetConfig): SeatPickerBoundary;
