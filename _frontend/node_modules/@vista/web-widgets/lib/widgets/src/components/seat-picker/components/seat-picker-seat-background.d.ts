import * as React from 'react';
import { Seat } from '../../../../../types/seating/seat';
import { SeatType } from '../../../../../types/seating/seat-type';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerDimension } from '../types/seat-picker-measurements';
export interface SeatPickerSeatBackgroundProps {
    seat: Seat;
    seatDimensions: SeatPickerDimension;
    config: SeatPickerWidgetConfig;
}
export declare class SeatPickerSeatBackground extends React.Component<SeatPickerSeatBackgroundProps> {
    render(): JSX.Element;
}
/**
 * Returns the SVG path data for the background path of a seat.
 */
export declare function getSeatBackgroundPath(type: SeatType, width: number, height: number, config: SeatPickerWidgetConfig): string;
