import * as React from 'react';
import { OrderStore } from '../../../../../api/src/stores/order/order-store';
import { AllowedTicketQuantity } from '../../../../../api/src/stores/order/selectors/get-allowed-ticket-quantity';
import { OrderTicket } from '../../../../../types/order/order-ticket';
import { TicketType } from '../../../../../types/sales-data/ticket-type';
import { BemClassBuilder } from '../../../utils/bem-class-builder';
import { TicketPickerWidgetConfig } from '../types/config';
export interface TicketPickerTableRowProps {
    orderStore: OrderStore;
    showtimeId: string;
    ticket: OrderTicket;
    ticketType: TicketType;
    config: TicketPickerWidgetConfig;
    onTicketQuantityUpdate: (showtimeId: string, ticketTypeKey: string, quantity: number, voucherBarcode?: string) => void;
}
export declare class TicketPickerTableRow extends React.Component<TicketPickerTableRowProps> {
    render(): JSX.Element | null;
    getClass(b: BemClassBuilder, ticketType: TicketType, allowedQuantity: AllowedTicketQuantity): string;
    handleQuantityChange: (newQuantity: number) => void;
}
