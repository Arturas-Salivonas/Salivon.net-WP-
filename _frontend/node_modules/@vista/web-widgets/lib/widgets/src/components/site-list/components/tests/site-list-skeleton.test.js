import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { SiteListSkeleton } from '../site-list-skeleton';
describe('SiteListSkeleton', function () {
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(SiteListSkeleton, null));
    });
});
//# sourceMappingURL=site-list-skeleton.test.js.map