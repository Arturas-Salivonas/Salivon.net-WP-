import * as React from 'react';
import { OrderStore } from '../../../../../api/src/stores/order/order-store';
import { TicketTypeKey } from '../../../../../types/sales-data/ticket-type';
import { TicketPickerWidgetConfig } from '../types/config';
import { TicketPickerWidgetDisplayText } from '../types/display-text';
export interface TicketPickerTableProps {
    orderStore: OrderStore;
    showtimeId: string;
    config: TicketPickerWidgetConfig;
    displayText: TicketPickerWidgetDisplayText;
    onTicketQuantityUpdate: (showtimeId: string, ticketTypeKey: TicketTypeKey, quantity: number, voucherBarcode?: string) => void;
}
export declare class TicketPickerTable extends React.Component<TicketPickerTableProps> {
    render(): JSX.Element;
}
