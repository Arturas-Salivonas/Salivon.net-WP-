var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { SearchInput } from '../search-input';
describe('SearchInput', function () {
    it('it should render correctly', function () {
        var props = {
            searchValues: [],
            searchKeys: [],
            onResultsChanged: function () { return; }
        };
        compareToSnapshot(React.createElement(SearchInput, __assign({}, props)));
    });
});
//# sourceMappingURL=search-input.test.js.map