var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleFilms } from '../../../../../../example-data/browsing/film';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultFilmListAssets } from '../../types/assets';
import { defaultFilmListConfig } from '../../types/config';
import { defaultFilmListDisplayText } from '../../types/display-text';
import { FilmListTabs } from '../film-list-tabs';
describe('FilmListTabs', function () {
    it('should render correctly', function () {
        var testProps = {
            films: exampleFilms,
            displayText: defaultFilmListDisplayText,
            config: defaultFilmListConfig,
            assets: defaultFilmListAssets,
            onFilmClicked: function () { return; }
        };
        compareToSnapshot(React.createElement(FilmListTabs, __assign({}, testProps)));
    });
});
//# sourceMappingURL=film-list-tabs.test.js.map