import { compareAsc } from 'date-fns';
import * as React from 'react';
import { bemClassBuilderFactory } from '../../../utils/bem-class-builder';
import { ShowtimePickerButton } from './showtime-picker-showtime-button';
export var ShowtimePickerShowtimeList = function (_a) {
    var config = _a.config, displayText = _a.displayText, showtimes = _a.showtimes, hiddenAttributeIds = _a.hiddenAttributeIds, browsingStore = _a.browsingStore, onShowtimeClicked = _a.onShowtimeClicked;
    var b = bemClassBuilderFactory('v-showtime-picker-showtime-list');
    return (React.createElement("ul", { className: b.block() }, showtimes.sort(function (showtime1, showtime2) { return compareAsc(showtime1.startTime, showtime2.startTime); }).map(function (showtime) {
        return React.createElement("li", { className: b.element('item'), key: showtime.id },
            React.createElement("div", { className: b.element('button-wrapper') },
                React.createElement(ShowtimePickerButton, { showtime: showtime, browsingStore: browsingStore, hiddenAttributeIds: hiddenAttributeIds, onShowtimeClicked: onShowtimeClicked, displayText: displayText, heroAttributeColors: config.heroAttributeColors })));
    })));
};
//# sourceMappingURL=showtime-picker-showtime-list.js.map