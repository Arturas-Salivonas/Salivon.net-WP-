var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleFilm } from '../../../../../example-data/browsing/film';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { FilmPoster } from '../film-poster';
describe('FilmPoster', function () {
    it('should render img correctly', function () {
        var filmPosterProps = {
            film: exampleFilm
        };
        compareToSnapshot(React.createElement(FilmPoster, __assign({}, filmPosterProps)));
    });
    it('should render poster with link correctly', function () {
        var filmPosterProps = {
            film: exampleFilm,
            linkUrl: 'https://www.vista.co'
        };
        compareToSnapshot(React.createElement(FilmPoster, __assign({}, filmPosterProps)));
    });
});
//# sourceMappingURL=film-poster.test.js.map