import { configurationStoreInstance } from '../../../../api/src/stores/configuration/configuration-store';
import { getGiftCardDetails as getGiftCardDetailsApi } from '../../../../mx-api-client/src/mxt/api/get-gift-card-details';
import { addWidgetBootstrap } from '../../bootstrap/add-widget-bootstrap';
import { createComponent } from '../../bootstrap/create-component';
import { GiftCardChecker } from './gift-card-checker';
import { mapGiftCardCheckerConfig } from './mappers/config-mapper';
import { mapGiftCardCheckerDisplayText } from './mappers/display-text-mapper';
export var giftCardChecker = function (bootstrap) {
    var cinemaChainId = configurationStoreInstance.data.api.cinemaChainId;
    var getGiftCardDetails = function (cardNumber, captchaResponse) {
        return getGiftCardDetailsApi(cinemaChainId, cardNumber, captchaResponse);
    };
    var props = {
        config: mapGiftCardCheckerConfig(bootstrap.config),
        captchaConfig: configurationStoreInstance.data.security.captcha,
        displayText: mapGiftCardCheckerDisplayText(bootstrap.displayText),
        getGiftCardDetails: getGiftCardDetails
    };
    return createComponent(bootstrap.selector, GiftCardChecker, props);
};
addWidgetBootstrap('giftCardChecker', giftCardChecker);
//# sourceMappingURL=bootstrap.js.map