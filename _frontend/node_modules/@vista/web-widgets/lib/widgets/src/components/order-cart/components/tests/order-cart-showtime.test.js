var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { orderStoreInstance } from '../../../../../../api/src/stores/order/order-store';
import { exampleOrder } from '../../../../../../example-data/order/order';
import { exampleOrderShowtime } from '../../../../../../example-data/order/order-showtime';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultOrderCartDisplayText } from '../../types/display-text';
import { OrderCartShowtime } from '../order-cart-showtime';
describe('OrderCartShowtime', function () {
    beforeAll(function () {
        orderStoreInstance.actions.setOrderData(exampleOrder);
    });
    it('should render correctly', function () {
        var testProps = {
            orderShowtime: exampleOrderShowtime,
            orderStore: orderStoreInstance,
            displayText: defaultOrderCartDisplayText
        };
        compareToSnapshot(React.createElement(OrderCartShowtime, __assign({}, testProps)));
    });
});
//# sourceMappingURL=order-cart-showtime.test.js.map