var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultOrderCartConfig } from '../types/config';
/**
 * Maps the config provided via the API to the config required by the Order Cart widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 * @param config
 */
export function mapOrderCartConfig(config) {
    if (!config)
        return defaultOrderCartConfig;
    var valid = new Validator().ofType('object').validate(config, 'Order Cart config');
    if (!valid)
        return defaultOrderCartConfig;
    var validatedConfig = getValidatedOrderCartConfig(config);
    return __assign({}, defaultOrderCartConfig, validatedConfig);
}
function getValidatedOrderCartConfig(config) {
    var validatedConfig = __assign({}, config, { actionButton: mapOrderCartActionButtonConfig(config.actionButton) });
    return validatedConfig;
}
export function mapOrderCartActionButtonConfig(config) {
    var defaultConfig = defaultOrderCartConfig.actionButton;
    if (!config)
        return defaultConfig;
    var valid = new Validator().ofType('object').validate(config, 'Order Cart action button config');
    if (!valid)
        return defaultConfig;
    var validatedConfig = getValidatedOrderCartActionButtonConfig(config);
    return __assign({}, defaultConfig, validatedConfig);
}
function getValidatedOrderCartActionButtonConfig(config) {
    if ('show' in config) {
        var valid = new Validator()
            .ofType('boolean')
            .validate(config.show, 'Order Cart action button show');
        if (!valid)
            delete config.show;
    }
    return config;
}
//# sourceMappingURL=config-mapper.js.map