var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { OrderStore } from '../../../../../api/src/stores/order/order-store';
import { exampleOrderCustomerDetails } from '../../../../../example-data/order/order-customer-details';
import { defaultConfigurationStoreData } from '../../../../../types/stores/configuration-store-data';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { CustomerDetailsForm } from '../customer-details-form';
import { defaultCustomerDetailsFormConfig } from '../types/config';
import { defaultCustomerDetailsFormDisplayText } from '../types/display-text';
var orderStore = new OrderStore();
var testProps = {
    orderStore: orderStore,
    validationConfig: defaultConfigurationStoreData.customerDetails.validation,
    config: defaultCustomerDetailsFormConfig,
    displayText: defaultCustomerDetailsFormDisplayText,
};
describe('CustomerDetailsForm', function () {
    it('renders correctly', function () {
        orderStore.data.customerDetails = exampleOrderCustomerDetails;
        compareToSnapshot(React.createElement(CustomerDetailsForm, __assign({}, testProps)));
    });
});
//# sourceMappingURL=customer-details-form.test.js.map