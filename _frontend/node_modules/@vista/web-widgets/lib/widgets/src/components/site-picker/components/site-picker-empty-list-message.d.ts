import * as React from 'react';
import { SitePickerAssets } from '../types/assets';
import { SitePickerDisplayText } from '../types/display-text';
export interface SitePickerEmptyListMessageProps {
    searchTerm: string;
    displayText: SitePickerDisplayText;
    assets: SitePickerAssets;
}
export declare const SitePickerEmptyListMessage: React.FC<SitePickerEmptyListMessageProps>;
