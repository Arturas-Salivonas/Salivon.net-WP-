var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleSite } from '../../../../../../example-data/browsing/site';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { SitePickerUnselectedSite } from '../site-picker-unselected-site';
describe('SitePickerUnselectedSite', function () {
    it('should render correctly', function () {
        var testProps = {
            site: exampleSite,
            disabled: false,
            onClick: function () { return; }
        };
        compareToSnapshot(React.createElement(SitePickerUnselectedSite, __assign({}, testProps)));
    });
    it('should render disabled correctly', function () {
        var testProps = {
            site: exampleSite,
            disabled: true,
            onClick: function () { return; }
        };
        compareToSnapshot(React.createElement(SitePickerUnselectedSite, __assign({}, testProps)));
    });
});
//# sourceMappingURL=site-picker-unselected-site.test.js.map