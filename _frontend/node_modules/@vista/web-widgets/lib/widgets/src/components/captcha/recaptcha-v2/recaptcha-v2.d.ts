import * as React from 'react';
import { CaptchaBase } from '../captcha-base';
import { RecaptchaV2Size } from './recaptcha-v2-config';
declare global {
    interface Window {
        grecaptcha: any;
    }
}
export interface RecaptchaV2State {
    isReady: boolean;
    recaptchaId?: string;
}
export interface RecaptchaV2Props {
    /**
     * Site key for domains
     */
    siteKey: string;
    /**
     * Callback function to be executed when the captcha value has changed
     */
    onValueChanged: (captchaResponse?: string) => void;
    /**
     * Size of the recaptcha widget, default: 'normal'
     */
    size?: RecaptchaV2Size;
    /**
     * Forces the widget to render in a specific language. Auto-detects the user's language if unspecified.
     * See https://developers.google.com/recaptcha/docs/language for available languages
     */
    ietfLanguageTag?: string;
}
/**
 * Component that wraps a Google reCAPTCHA v2 element
 */
export declare class RecaptchaV2 extends React.Component<RecaptchaV2Props, RecaptchaV2State> implements CaptchaBase {
    private readyCheckTimerId;
    private recaptchaRef;
    constructor(props: RecaptchaV2Props);
    render(): JSX.Element;
    componentDidMount(): void;
    componentDidUpdate(_prevProps: RecaptchaV2Props, prevState: RecaptchaV2State): void;
    componentWillUnmount(): void;
    reset(): void;
    runCaptchaTest(): void;
    private handleValueChanged;
    private handleExpired;
    private updateReadyState;
    private renderRecaptcha;
}
