import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { MemberPointsBalance } from '../member-points-balance';
import { MemberPointsBalanceWidgetDisplayText } from './display-text';
/**
 * Bootstrap options to render the Member Points Balance widget.
 */
export interface MemberPointsBalanceBootstrap extends WidgetBootstrap {
    /**
     * Display text for the Member Points Balance widget.
     */
    displayText?: Partial<MemberPointsBalanceWidgetDisplayText>;
}
export declare type MemberPointsBalanceBootstrapper = Bootstrapper<MemberPointsBalance, MemberPointsBalanceBootstrap>;
