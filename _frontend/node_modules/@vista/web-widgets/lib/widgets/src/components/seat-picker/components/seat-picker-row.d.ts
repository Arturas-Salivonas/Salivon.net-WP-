import * as React from 'react';
import { Area } from '../../../../../types/seating/area';
import { Row } from '../../../../../types/seating/row';
import { Seat } from '../../../../../types/seating/seat';
import { SeatPickerWidgetConfig } from '../types/config';
export interface SeatPickerRowProps {
    row: Row;
    area: Area;
    scaleFactor: number;
    config: SeatPickerWidgetConfig;
    isSelectable: boolean;
    onSeatClicked: (selectedSeat: Seat) => void;
}
export declare class SeatPickerRow extends React.Component<SeatPickerRowProps> {
    render(): JSX.Element;
}
