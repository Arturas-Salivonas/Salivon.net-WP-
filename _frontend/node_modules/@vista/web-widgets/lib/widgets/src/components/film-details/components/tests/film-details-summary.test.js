var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { browsingStoreInstance } from '../../../../../../api/src/stores/browsing/browsing-store';
import { exampleBrowsingStoreData } from '../../../../../../example-data/browsing/browsing-store-data';
import { exampleFilm } from '../../../../../../example-data/browsing/film';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultFilmDetailsWidgetDisplayText } from '../../types/display-text';
import { FilmDetailsSummary } from '../film-details-summary';
describe('FilmDetailsSummary', function () {
    var testProps = {
        film: exampleFilm,
        browsingStore: browsingStoreInstance,
        displayText: defaultFilmDetailsWidgetDisplayText,
    };
    beforeAll(function () {
        browsingStoreInstance.setData(exampleBrowsingStoreData);
    });
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(FilmDetailsSummary, __assign({}, testProps)));
    });
    it('should render without runtime', function () {
        var film = __assign({}, exampleFilm, { runtime: undefined });
        compareToSnapshot(React.createElement(FilmDetailsSummary, __assign({}, testProps, { film: film })));
    });
    it('should render without releaseDate', function () {
        var film = __assign({}, exampleFilm, { releaseDate: undefined });
        compareToSnapshot(React.createElement(FilmDetailsSummary, __assign({}, testProps, { film: film })));
    });
    it('should render without categories', function () {
        var film = __assign({}, exampleFilm, { categories: [] });
        compareToSnapshot(React.createElement(FilmDetailsSummary, __assign({}, testProps, { film: film })));
    });
    it('should render without classificationNote', function () {
        var film = __assign({}, exampleFilm, { censorRatingId: undefined, classificationNote: undefined });
        compareToSnapshot(React.createElement(FilmDetailsSummary, __assign({}, testProps, { film: film })));
    });
    it('should render without directors', function () {
        var film = __assign({}, exampleFilm, { directorIds: [] });
        compareToSnapshot(React.createElement(FilmDetailsSummary, __assign({}, testProps, { film: film })));
    });
    it('should render without actors', function () {
        var film = __assign({}, exampleFilm, { actorIds: [] });
        compareToSnapshot(React.createElement(FilmDetailsSummary, __assign({}, testProps, { film: film })));
    });
    it('should render without plot', function () {
        var film = __assign({}, exampleFilm, { plot: undefined });
        compareToSnapshot(React.createElement(FilmDetailsSummary, __assign({}, testProps, { film: film })));
    });
});
//# sourceMappingURL=film-details-summary.test.js.map