var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultTicketPickerAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Ticket Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapTicketPickerAssets(assets) {
    if (!assets)
        return defaultTicketPickerAssets;
    var valid = new Validator().ofType('object').validate(assets, 'Ticket Picker assets');
    if (!valid)
        return defaultTicketPickerAssets;
    var validatedAssets = getValidatedTicketPickerAssets(assets);
    return __assign({}, defaultTicketPickerAssets, validatedAssets);
}
/**
 * Validates the asset values provided for the Ticket Picker.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedTicketPickerAssets(assets) {
    var validateField = fieldValidator(assets);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'failedToLoadDataIconName', 'Ticket Picker failedToLoadDataIconName');
    return assets;
}
//# sourceMappingURL=assets-mapper.js.map