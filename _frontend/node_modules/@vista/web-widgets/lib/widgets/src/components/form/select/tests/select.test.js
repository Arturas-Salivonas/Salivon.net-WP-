var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { FormElementStyle } from '../../form-element-style';
import { Select } from '../select';
describe('Select', function () {
    it('should render the default Select component correctly', function () {
        var props = {
            id: 'select-id',
            name: 'select-name',
            placeholder: 'select placeholder',
            value: 'select value'
        };
        compareToSnapshot(React.createElement(Select, __assign({}, props),
            React.createElement("option", null, "1"),
            React.createElement("option", null, "2"),
            React.createElement("option", null, "3")));
    });
    it('should render the warning Select component correctly', function () {
        var props = {
            id: 'select-id-warning',
            style: FormElementStyle.Warning
        };
        compareToSnapshot(React.createElement(Select, __assign({}, props)));
    });
    it('should render the error Select component correctly', function () {
        var props = {
            id: 'select-id-error',
            style: FormElementStyle.Error
        };
        compareToSnapshot(React.createElement(Select, __assign({}, props)));
    });
    it('should render the success Select component correctly', function () {
        var props = {
            id: 'select-id-success',
            style: FormElementStyle.Success
        };
        compareToSnapshot(React.createElement(Select, __assign({}, props)));
    });
});
//# sourceMappingURL=select.test.js.map