/**
 * Transforms a point by a given scale.
 */
export var scalePoint = function (scale) { return function (point) { return ({
    x: point.x * scale,
    y: point.y * scale
}); }; };
export var addPoints = function (p1, p2) {
    return {
        x: p1.x + p2.x,
        y: p1.y + p2.y
    };
};
/**
 * Subtracts point p2 from point p1.
 */
export var subtractPoints = function (p1, p2) {
    return {
        x: p1.x - p2.x,
        y: p1.y - p2.y
    };
};
/**
 * Returns the center point for given dimensions.
 */
export var centerPoint = function (dimensions) {
    return {
        x: dimensions.width / 2,
        y: dimensions.height / 2
    };
};
/**
 * Returns the euclidian distance between two points.
 */
export var distanceBetweenPoints = function (p1, p2) {
    return Math.sqrt(Math.pow(p2.x - p1.x, 2) + Math.pow(p2.y - p1.y, 2));
};
//# sourceMappingURL=measurement-utils.js.map