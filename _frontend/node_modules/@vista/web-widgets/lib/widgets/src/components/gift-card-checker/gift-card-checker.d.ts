import * as React from 'react';
import { MxGiftCardDetails } from '../../../../mx-api-client/src/mxt/types/gift-card-details';
import { CaptchaConfig } from '../captcha/captcha-config';
import { GiftCardCheckerWidgetConfig } from './types/config';
import { GiftCardCheckerWidgetDisplayText } from './types/display-text';
export interface GiftCardCheckerProps {
    displayText: GiftCardCheckerWidgetDisplayText;
    config: GiftCardCheckerWidgetConfig;
    captchaConfig?: CaptchaConfig;
    getGiftCardDetails: (giftCardNumber?: string, captchaResponse?: string) => Promise<MxGiftCardDetails>;
}
export interface GiftCardCheckerState {
    cardDetails?: MxGiftCardDetails;
    displayMode: GiftCardCheckerDisplayMode;
}
export declare enum GiftCardCheckerDisplayMode {
    Form = 0,
    Details = 1
}
/**
 * Renders a component that allows the user to input a gift card number to retrieve details about the card.
 */
export declare class GiftCardChecker extends React.Component<GiftCardCheckerProps, GiftCardCheckerState> {
    constructor(props: GiftCardCheckerProps);
    render(): JSX.Element;
    setCardDetails: (cardDetails?: MxGiftCardDetails | undefined) => void;
    clearCardDetails: () => void;
}
