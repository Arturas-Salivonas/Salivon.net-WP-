import * as React from 'react';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { Tag } from '../tag';
describe('Tag', function () {
    it('should render correctly', function () {
        var value = {
            id: '1'
        };
        compareToSnapshot(React.createElement(Tag, { value: value }));
    });
    it('should render with a label correctly', function () {
        var value = {
            id: '1',
            label: 'label'
        };
        compareToSnapshot(React.createElement(Tag, { value: value }));
    });
    it('should render with a button correctly', function () {
        var value = {
            id: '1',
            buttonIcon: 'icon'
        };
        compareToSnapshot(React.createElement(Tag, { value: value }));
    });
    it('should render with a label and button correctly', function () {
        var value = {
            id: '1',
            label: 'label',
            buttonIcon: 'icon'
        };
        compareToSnapshot(React.createElement(Tag, { value: value }));
    });
});
//# sourceMappingURL=tag.test.js.map