var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultItemPickerAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Item Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapItemPickerAssets(assets) {
    if (!assets)
        return defaultItemPickerAssets;
    var valid = new Validator().ofType('object').validate(assets, 'Item Picker assets');
    if (!valid)
        return defaultItemPickerAssets;
    var validatedAssets = getValidatedItemPickerAssets(assets);
    return __assign({}, defaultItemPickerAssets, validatedAssets);
}
/**
 * Validates the asset values provided for the Item Picker.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedItemPickerAssets(assets) {
    var validateField = fieldValidator(assets);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'failedToLoadDataIconName', 'Item Picker failedToLoadDataIconName');
    return assets;
}
//# sourceMappingURL=assets-mapper.js.map