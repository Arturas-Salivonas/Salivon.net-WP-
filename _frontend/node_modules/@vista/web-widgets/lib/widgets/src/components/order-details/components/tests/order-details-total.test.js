var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultOrderDetailsDisplayText } from '../../types/display-text';
import { OrderDetailsTotal } from '../order-details-total';
describe('OrderDetailsTotal', function () {
    it('should render correctly', function () {
        var orderDetailsTotalProps = {
            bookingFee: 2.5,
            totalPrice: 30,
            displayText: defaultOrderDetailsDisplayText
        };
        compareToSnapshot(React.createElement(OrderDetailsTotal, __assign({}, orderDetailsTotalProps)));
    });
});
//# sourceMappingURL=order-details-total.test.js.map