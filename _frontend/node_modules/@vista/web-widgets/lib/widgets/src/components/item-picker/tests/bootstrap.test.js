var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { orderService } from '../../../../../api/src/services/order/order-service';
import { orderStoreInstance } from '../../../../../api/src/stores/order/order-store';
import { salesDataStoreInstance } from '../../../../../api/src/stores/sales-data/sales-data-store';
import { exampleSite } from '../../../../../example-data/browsing/site';
import { exampleOrder } from '../../../../../example-data/order/order';
import * as createComponent from '../../../bootstrap/create-component';
import { itemPicker } from '../bootstrap';
import { defaultItemPickerAssets } from '../types/assets';
import { defaultItemPickerDisplayText } from '../types/display-text';
describe('itemPicker bootstrap', function () {
    var component = { _component: {} };
    var createComponentSpy = jest.spyOn(createComponent, 'createComponent').mockReturnValue(component);
    beforeAll(function () {
        orderStoreInstance.actions.setOrderData(__assign({}, exampleOrder, { site: __assign({}, exampleSite, { id: '123' }) }));
    });
    afterEach(function () { return jest.clearAllMocks(); });
    it('calls the create component function', function () {
        expect.assertions(2);
        var response = itemPicker({
            selector: 'test',
        });
        expect(createComponentSpy).toBeCalledWith('test', expect.anything(), {
            siteId: '123',
            orderStore: orderStoreInstance,
            salesDataStore: salesDataStoreInstance,
            displayText: defaultItemPickerDisplayText,
            assets: defaultItemPickerAssets,
        });
        expect(response).toEqual(__assign({}, component, { saveItemSelection: orderService.saveItemSelection }));
    });
});
//# sourceMappingURL=bootstrap.test.js.map