import * as React from 'react';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { Button, ButtonColor, ButtonSize } from '../button';
describe('button', function () {
    it('should render the default button', function () {
        compareToSnapshot(React.createElement(Button, null, "Default Button"));
    });
    describe('color', function () {
        it("should render primary color", function () {
            compareToSnapshot(React.createElement(Button, { color: ButtonColor.Primary }, "Primary Button"));
        });
        it("should render secondary color", function () {
            compareToSnapshot(React.createElement(Button, { color: ButtonColor.Secondary }, "Secondary Button"));
        });
    });
    describe('size', function () {
        it("should render small size", function () {
            compareToSnapshot(React.createElement(Button, { size: ButtonSize.Small }, "Small Button"));
        });
        it("should render medium size", function () {
            compareToSnapshot(React.createElement(Button, { size: ButtonSize.Medium }, "Medium Button"));
        });
        it("should render large size", function () {
            compareToSnapshot(React.createElement(Button, { size: ButtonSize.Large }, "Large Button"));
        });
    });
    describe('disabled', function () {
        it("should render as enabled", function () {
            compareToSnapshot(React.createElement(Button, { disabled: false }, "Enabled Button"));
        });
        it("should render as disabled", function () {
            compareToSnapshot(React.createElement(Button, { disabled: true }, "Disabled Button"));
        });
    });
    describe('loading', function () {
        it("should render as loading", function () {
            compareToSnapshot(React.createElement(Button, { loading: false }, "Normal Button"));
        });
        it("should render as disabled", function () {
            compareToSnapshot(React.createElement(Button, { loading: true }, "Loading Button"));
        });
    });
});
//# sourceMappingURL=button.test.js.map