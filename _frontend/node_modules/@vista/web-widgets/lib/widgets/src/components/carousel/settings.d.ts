import { Omit } from './../../../../api/src/utils/omit';
/**
 * Configuration options for the Glider.js carousel.
 */
export interface CarouselSettings {
    /**
     * The number of slides to show in the carousel viewport.
     * If the value is set to 'auto', it will be automatically calculated,
     * based upon the number of items able to fit within the container viewport.
     * This requires setting the `itemWidth` option.
     */
    slidesToShow?: number | 'auto';
    /**
     * The number of slides to scroll when arrow navigation is used.
     * If this value is set to 'auto', it will match the value of `slidesToScroll`.
     */
    slidesToScroll?: number | 'auto';
    /**
     * The width of each item/slide in the carousel in px.
     * This value is ignored unless `slidesToShow` is set to 'auto', in which case it is then required.
     */
    itemWidth?: number;
    /**
     * This prevents resizing items to fit when slidesToShow is set to auto.
     * NOTE: This will yield fractional slides if your container is not sized appropriately.
     */
    exactWidth?: boolean;
    /**
     * If true, Glider.js will scroll to the nearest slide after any scroll interactions.
     */
    scrollLock?: boolean;
    /**
     * If true, Glider.js will lock to the nearest slide on resizing of the window.
     */
    resizeLock?: boolean;
    /**
     * Custom settings per provided breakpoint.
     */
    responsive?: CarouselBreakpoint[];
    /**
     * If true, the list can be scrolled by click and dragging with the mouse.
     */
    draggable?: boolean;
    /**
     * Sets the duration of animations in seconds. Higher is slower!
     */
    duration?: number;
}
export interface CarouselBreakpoint {
    breakpoint: number;
    settings: Omit<CarouselSettings, 'responsive'>;
}
