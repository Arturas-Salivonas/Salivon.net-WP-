import { SitePickerAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Site Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export declare function mapSitePickerAssets(assets?: Partial<SitePickerAssets>): SitePickerAssets;
/**
 * Validates the asset values provided for the Site Picker.
 * Removes any invalid values and logs appropriate warnings.
 */
export declare function getValidatedSitePickerAssets(assets: Partial<SitePickerAssets>): Partial<SitePickerAssets>;
