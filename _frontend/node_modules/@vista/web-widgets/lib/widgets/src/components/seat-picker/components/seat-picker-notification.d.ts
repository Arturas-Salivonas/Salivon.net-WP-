import * as React from 'react';
import { SeatingError } from '../../../../../api/src/stores/seating/helpers/types';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerWidgetDisplayText } from '../types/display-text';
export interface SeatPickerNotificationProps {
    errors: SeatingError[];
    config: SeatPickerWidgetConfig;
    displayText: SeatPickerWidgetDisplayText;
    onNotificationClosed: () => void;
}
/**
 * @observer Wrapper around Notification, which must be a standard (non-observer) component
 * in order for the CSSTransitionGroup to work.
 */
export declare class SeatPickerNotification extends React.Component<SeatPickerNotificationProps> {
    render(): JSX.Element | null;
    getUniqueKeyForError(error: SeatingError): number;
}
