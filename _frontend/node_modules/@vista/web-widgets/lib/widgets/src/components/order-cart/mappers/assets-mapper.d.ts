import { OrderCartAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Order Cart widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export declare function mapOrderCartAssets(assets?: Partial<OrderCartAssets>): OrderCartAssets;
/**
 * Validates the asset values provided for the Order Cart.
 * Removes any invalid values and logs appropriate warnings.
 */
export declare function getValidatedOrderCartAssets(assets: Partial<OrderCartAssets>): Partial<OrderCartAssets>;
