import { browsingStoreInstance } from '../../../../api/src/stores/browsing/browsing-store';
import { configurationStoreInstance } from '../../../../api/src/stores/configuration/configuration-store';
import { addWidgetBootstrap } from '../../bootstrap/add-widget-bootstrap';
import { createComponent } from '../../bootstrap/create-component';
import { mapSitePickerAssets } from './mappers/assets-mapper';
import { mapSitePickerDisplayText } from './mappers/display-text-mapper';
import { SitePicker } from './site-picker';
/**
 * Renders the Site Picker widget.
 * @param bootstrap
 */
export var sitePicker = function (bootstrap) {
    var selector = bootstrap.selector, displayText = bootstrap.displayText, assets = bootstrap.assets;
    var props = {
        browsingStore: browsingStoreInstance,
        configurationStore: configurationStoreInstance,
        displayText: mapSitePickerDisplayText(displayText),
        assets: mapSitePickerAssets(assets)
    };
    return createComponent(selector, SitePicker, props);
};
addWidgetBootstrap('sitePicker', sitePicker);
//# sourceMappingURL=bootstrap.js.map