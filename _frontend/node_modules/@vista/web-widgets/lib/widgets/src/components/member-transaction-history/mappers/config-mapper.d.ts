import { NestedPartial } from '../../../../../types/nested-partial';
import { MemberTransactionHistoryConfig } from './../types/config';
/**
 * Maps the config provided via the API to the config required by Member Transaction History widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 * @param config
 */
export declare function mapMemberTransactionHistoryConfig(config?: NestedPartial<MemberTransactionHistoryConfig>): MemberTransactionHistoryConfig;
