import { getMouseEventPoint } from '../mouse-event-utils';
describe('getMouseEventPoint', function () {
    it('gets the point from the mouse event', function () {
        var point = getMouseEventPoint({
            pageX: 100,
            pageY: 200
        });
        expect(point).toEqual({ x: 100, y: 200 });
    });
});
//# sourceMappingURL=mouse-event-utils.test.js.map