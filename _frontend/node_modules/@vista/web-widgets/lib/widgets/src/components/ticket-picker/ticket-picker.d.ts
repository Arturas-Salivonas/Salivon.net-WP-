import * as React from 'react';
import { OrderService } from '../../../../api/src/services/order/order-service';
import { OrderStore } from '../../../../api/src/stores/order/order-store';
import { TicketPickerAssets } from './types/assets';
import { TicketPickerWidgetConfig } from './types/config';
import { TicketPickerWidgetDisplayText } from './types/display-text';
import { TicketPickerWidgetEvents } from './types/events';
export interface TicketPickerProps {
    orderStore: OrderStore;
    orderService: OrderService;
    showtimeId: string;
    config: TicketPickerWidgetConfig;
    displayText: TicketPickerWidgetDisplayText;
    assets: TicketPickerAssets;
    events?: TicketPickerWidgetEvents;
}
export interface TicketPickerState {
    failedToLoadTickets?: boolean;
}
/**
 * Renders a ticket selection widget which allows users to
 * choose a quantity of ticket types and them to an orderService.
 *
 * @export
 * @class TicketPicker
 * @extends {React.Component<TicketPickerProps, TicketPickerState>}
 */
export declare class TicketPicker extends React.Component<TicketPickerProps, TicketPickerState> {
    constructor(props: any);
    render(): JSX.Element | null;
    componentDidMount(): void;
    private handleTicketQuantityUpdate;
    private handleNotificationClosed;
    private getTicketTypes;
}
