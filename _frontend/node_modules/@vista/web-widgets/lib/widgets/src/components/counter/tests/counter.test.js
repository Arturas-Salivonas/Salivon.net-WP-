import * as React from 'react';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { Counter, CounterStyle } from '../counter';
describe('Counter', function () {
    it('should render the info style correctly', function () {
        compareToSnapshot(React.createElement(Counter, { denominator: 4, numerator: 2, label: 'Tickets have been chosen', style: CounterStyle.Info }));
    });
    it('should render the warning style correctly', function () {
        compareToSnapshot(React.createElement(Counter, { denominator: 4, numerator: 2, label: 'Tickets have been chosen', style: CounterStyle.Warning }));
    });
});
//# sourceMappingURL=counter.test.js.map