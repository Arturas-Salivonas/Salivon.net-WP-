var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleFilm } from '../../../../../../example-data/browsing/film';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { FilmListFilm } from '../film-list-film';
describe('FilmListFilm', function () {
    it('should render grid mode correctly', function () {
        var testProps = {
            film: exampleFilm,
            onClick: function () { return; }
        };
        compareToSnapshot(React.createElement(FilmListFilm, __assign({}, testProps)));
    });
});
//# sourceMappingURL=film-list-film.test.js.map