var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultOrderCartDisplayText } from '../../types/display-text';
import { OrderCartTotals } from '../order-cart-totals';
describe('OrderCartTotals', function () {
    it('should render correctly', function () {
        var testProps = {
            bookingFee: 2.50,
            orderTotal: 30,
            displayText: defaultOrderCartDisplayText
        };
        compareToSnapshot(React.createElement(OrderCartTotals, __assign({}, testProps)));
    });
});
//# sourceMappingURL=order-cart-totals.test.js.map