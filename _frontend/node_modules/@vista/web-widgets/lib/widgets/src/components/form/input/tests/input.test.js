var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { FormElementStyle } from '../../form-element-style';
import { Input } from '../input';
describe('Input', function () {
    it('should render the default Input component correctly', function () {
        var props = {
            id: 'input-id',
            name: 'input-name',
            placeholder: 'input placeholder',
            value: 'input value'
        };
        compareToSnapshot(React.createElement(Input, __assign({}, props)));
    });
    it('should render the warning Input component correctly', function () {
        var props = {
            id: 'input-id-warning',
            style: FormElementStyle.Warning
        };
        compareToSnapshot(React.createElement(Input, __assign({}, props)));
    });
    it('should render the error Input component correctly', function () {
        var props = {
            id: 'input-id-error',
            style: FormElementStyle.Error
        };
        compareToSnapshot(React.createElement(Input, __assign({}, props)));
    });
    it('should render the success Input component correctly', function () {
        var props = {
            id: 'input-id-success',
            style: FormElementStyle.Success
        };
        compareToSnapshot(React.createElement(Input, __assign({}, props)));
    });
});
//# sourceMappingURL=input.test.js.map