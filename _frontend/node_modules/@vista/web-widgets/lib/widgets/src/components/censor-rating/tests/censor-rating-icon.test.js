import * as React from 'react';
import { browsingStoreInstance } from '../../../../../api/src/stores/browsing/browsing-store';
import { exampleBrowsingStoreData } from '../../../../../example-data/browsing/browsing-store-data';
import { exampleFilm } from '../../../../../example-data/browsing/film';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { CensorRatingIcon } from '../censor-rating-icon';
describe('CensorRatingIcon', function () {
    beforeAll(function () {
        browsingStoreInstance.setData(exampleBrowsingStoreData);
    });
    it('should render img correctly', function () {
        var censorRating = exampleFilm.censorRatingId ? browsingStoreInstance.selectors.getCensorRatingById(exampleFilm.censorRatingId) : undefined;
        compareToSnapshot(React.createElement(CensorRatingIcon, { censorRating: censorRating }));
    });
    it('should not be rendered', function () {
        compareToSnapshot(React.createElement(CensorRatingIcon, null));
    });
});
//# sourceMappingURL=censor-rating-icon.test.js.map