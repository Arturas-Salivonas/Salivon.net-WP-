var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultFilmListDisplayText } from '../types/display-text';
export function mapFilmListDisplayText(displayText) {
    if (!displayText)
        return defaultFilmListDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Film List display text');
    if (!valid)
        return defaultFilmListDisplayText;
    var validatedDisplayText = getValidatedFilmListDisplayText(displayText);
    return __assign({}, defaultFilmListDisplayText, validatedDisplayText);
}
function getValidatedFilmListDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'tabNowShowingLabel', 'Film List tabNowShowingLabel');
    validateField(validator, 'tabComingSoonLabel', 'Film List tabComingSoonLabel');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map