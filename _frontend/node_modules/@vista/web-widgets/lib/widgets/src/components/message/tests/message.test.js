var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../tests/compare-to-snapshot';
import { Message } from '../message';
test('Message should render the icon provided', function () {
    var props = {
        icon: 'example-icon'
    };
    compareToSnapshot(React.createElement(Message, __assign({}, props)));
});
test('Message should render title and description if provided', function () {
    var props = {
        title: 'Here is an example Title',
        description: 'Along with an example Subtitle',
    };
    compareToSnapshot(React.createElement(Message, __assign({}, props)));
});
test('Message should render wrapperClassName if provided', function () {
    var props = {
        className: 'v-class-wrapper-component'
    };
    compareToSnapshot(React.createElement(Message, __assign({}, props)));
});
test('Message should render everything if provided', function () {
    var props = {
        icon: 'example-icon',
        title: 'Here is an example Title',
        description: 'Along with an example Description',
        className: 'v-class-wrapper-component'
    };
    compareToSnapshot(React.createElement(Message, __assign({}, props)));
});
//# sourceMappingURL=message.test.js.map