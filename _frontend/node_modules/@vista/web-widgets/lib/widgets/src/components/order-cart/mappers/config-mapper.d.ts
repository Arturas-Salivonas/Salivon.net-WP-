import { NestedPartial } from '../../../../../types/nested-partial';
import { OrderCartActionButtonConfig, OrderCartConfig } from '../types/config';
/**
 * Maps the config provided via the API to the config required by the Order Cart widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 * @param config
 */
export declare function mapOrderCartConfig(config?: NestedPartial<OrderCartConfig>): OrderCartConfig;
export declare function mapOrderCartActionButtonConfig(config?: Partial<OrderCartActionButtonConfig>): OrderCartActionButtonConfig;
