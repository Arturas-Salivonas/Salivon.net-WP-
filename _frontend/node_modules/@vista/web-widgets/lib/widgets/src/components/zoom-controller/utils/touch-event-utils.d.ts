import { Point } from './measurement-utils';
/**
 * Returns the center coordinates of the touches in a touch event.
 */
export declare const getCenterPointOfTouchEvent: (event: TouchEvent) => Point;
/**
 * Returns the average distance from the center of touches in a touch event.
 */
export declare const getAverageTouchDistanceFromTouchCenter: (event: TouchEvent) => number;
/**
 * Returns the scale of a pinch event relative to the initial pinch distance.
 */
export declare const getPinchScale: (event: TouchEvent, initialPinchDistance: number) => number;
