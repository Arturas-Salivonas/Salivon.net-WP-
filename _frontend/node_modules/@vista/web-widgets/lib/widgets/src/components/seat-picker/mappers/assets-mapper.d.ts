import { SeatPickerAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Seat Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export declare function mapSeatPickerAssets(assets?: Partial<SeatPickerAssets>): SeatPickerAssets;
/**
 * Validates the asset values provided for the Seat Picker.
 * Removes any invalid values and logs appropriate warnings.
 */
export declare function getValidatedSeatPickerAssets(assets: Partial<SeatPickerAssets>): Partial<SeatPickerAssets>;
