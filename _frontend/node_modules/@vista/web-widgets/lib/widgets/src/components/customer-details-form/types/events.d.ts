import { OrderCustomerDetails } from '../../../../../types/order/order-customer-details';
/**
 * Events for the Customer Details Form widget.
 */
export interface CustomerDetailsFormWidgetEvents {
    /**
     * Callback function that returns the customer details when the form has been submitted.
     */
    onSubmit?: (customerDetails: OrderCustomerDetails) => void;
}
