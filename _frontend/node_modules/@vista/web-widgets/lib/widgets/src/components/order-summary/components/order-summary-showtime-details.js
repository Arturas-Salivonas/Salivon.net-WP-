var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { observer } from 'mobx-react';
import * as React from 'react';
import { getJourneyOrderShowtimeSeats } from '../../../../../api/src/widgets-api/journey/utils/get-journey-order-showtime-seats';
import { bemClassBuilderFactory } from '../../../utils/bem-class-builder';
import { Formatter } from '../../../utils/formatter';
import { getTranslatedTextOrDefault } from '../../../utils/get-translated-text';
import { getFormattedSeatsLabel } from '../../../utils/seat-utils';
function getFormattedShowtimeSummary(template, screenName, seats) {
    return Formatter.Instance.template(template, {
        screenName: screenName,
        seats: seats
    });
}
var OrderSummaryShowtimeDetails = /** @class */ (function (_super) {
    __extends(OrderSummaryShowtimeDetails, _super);
    function OrderSummaryShowtimeDetails() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    OrderSummaryShowtimeDetails.prototype.render = function () {
        var _a = this.props, orderShowtime = _a.orderShowtime, journeyStore = _a.journeyStore, displayText = _a.displayText;
        if (!orderShowtime)
            return null;
        var showtime = journeyStore.selectors.getShowtimeById(orderShowtime.showtimeId);
        if (!showtime)
            return null; // or throw?
        var film = journeyStore.selectors.getFilmById(showtime.filmId);
        if (!film)
            return null; // or throw?
        var screen = journeyStore.selectors.getScreenById(showtime.screenId);
        var screenName = screen && screen.screenName || '';
        var seats = getJourneyOrderShowtimeSeats(orderShowtime);
        var seatsLabel = getFormattedSeatsLabel(seats);
        var showtimeSummary = getFormattedShowtimeSummary(displayText.showtimeDetails, screenName, seatsLabel);
        var b = bemClassBuilderFactory('v-order-summary-showtime-details');
        return (React.createElement("div", { className: b.block() },
            React.createElement("span", { className: b.element('film-title') }, getTranslatedTextOrDefault(film, 'title')),
            React.createElement("span", { className: b.element('summary') }, showtimeSummary)));
    };
    OrderSummaryShowtimeDetails = __decorate([
        observer
    ], OrderSummaryShowtimeDetails);
    return OrderSummaryShowtimeDetails;
}(React.Component));
export { OrderSummaryShowtimeDetails };
//# sourceMappingURL=order-summary-showtime-details.js.map