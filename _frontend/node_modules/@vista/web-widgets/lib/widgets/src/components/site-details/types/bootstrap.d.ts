import { Bootstrapper } from '../../../bootstrap/bootstrapper';
import { WidgetBootstrap } from '../../../bootstrap/widget-bootstrap';
import { SiteDetails } from '../site-details';
import { SiteDetailsWidgetAssets } from './assets';
import { SiteDetailsWidgetDisplayText } from './display-text';
/**
 * Bootstrap options to render the Site Details widget.
 */
export interface SiteDetailsWidgetBootstrap extends WidgetBootstrap {
    /**
     * The unique ID of the site to be displayed.
     */
    siteId: string;
    /**
     * Display text for the Site Details widget.
     */
    displayText?: Partial<SiteDetailsWidgetDisplayText>;
    /**
     * Assets for the Site Details widget.
     */
    assets?: Partial<SiteDetailsWidgetAssets>;
}
export declare type SiteDetailsWidgetBootstrapper = Bootstrapper<SiteDetails, SiteDetailsWidgetBootstrap>;
