import { ItemModel } from '../../../../../types/sales-data/item-models/item-model';
/**
 * Events for the Item Picker widget.
 */
export interface ItemPickerEvents {
    /**
     * Callback function that is called when an item is clicked.
     */
    onItemClicked?: (item: ItemModel) => void;
}
