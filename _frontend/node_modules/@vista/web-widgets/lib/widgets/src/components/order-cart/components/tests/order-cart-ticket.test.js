var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleStandardTicket1 } from '../../../../../../example-data/order/order-ticket';
import { exampleStandardTicketType1 } from '../../../../../../example-data/order/order-ticket-type';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultOrderCartDisplayText } from '../../types/display-text';
import { OrderCartTicket } from '../order-cart-ticket';
describe('OrderCartTicket', function () {
    it('should render correctly', function () {
        var testProps = {
            orderTicket: exampleStandardTicket1,
            ticketType: exampleStandardTicketType1,
            displayText: defaultOrderCartDisplayText
        };
        compareToSnapshot(React.createElement(OrderCartTicket, __assign({}, testProps)));
    });
});
//# sourceMappingURL=order-cart-ticket.test.js.map