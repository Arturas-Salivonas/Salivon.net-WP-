import { SiteListAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Site List widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export declare function mapSiteListAssets(assets?: Partial<SiteListAssets>): SiteListAssets;
/**
 * Validates the asset values provided for the Site List.
 * Removes any invalid values and logs appropriate warnings.
 */
export declare function getValidatedSiteListAssets(assets: Partial<SiteListAssets>): Partial<SiteListAssets>;
