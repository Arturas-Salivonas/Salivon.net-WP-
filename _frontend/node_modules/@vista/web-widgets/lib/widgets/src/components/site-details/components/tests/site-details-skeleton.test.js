import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { SiteDetailsSkeleton } from '../site-details-skeleton';
describe('SiteDetailsSkeleton', function () {
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(SiteDetailsSkeleton, null));
    });
});
//# sourceMappingURL=site-details-skeleton.test.js.map