import { SeatLayout } from '../../../../../types/seating/seat-layout';
import { SeatPickerRowLabelProps } from '../components/seat-picker-row-label';
import { SeatPickerWidgetConfig } from '../types/config';
import { SeatPickerBoundary } from '../types/seat-picker-measurements';
/**
 * Returns row label props for each row in each area in SVG units.
 */
export declare function getRowLabelProps(seatLayout: SeatLayout, scaleFactor: number, config: SeatPickerWidgetConfig, mapBoundary: SeatPickerBoundary): SeatPickerRowLabelProps[];
