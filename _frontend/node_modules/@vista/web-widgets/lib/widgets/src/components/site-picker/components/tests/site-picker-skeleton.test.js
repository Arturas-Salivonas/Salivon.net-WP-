import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { SitePickerSkeleton } from '../site-picker-skeleton';
describe('SitePickerSkeleton', function () {
    it('should render correctly', function () {
        compareToSnapshot(React.createElement(SitePickerSkeleton, null));
    });
});
//# sourceMappingURL=site-picker-skeleton.test.js.map