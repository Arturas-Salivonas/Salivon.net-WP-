var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultShowtimePickerAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Showtime Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapShowtimePickerAssets(assets) {
    if (!assets)
        return defaultShowtimePickerAssets;
    var valid = new Validator().ofType('object').validate(assets, 'Showtime picker assets');
    if (!valid)
        return defaultShowtimePickerAssets;
    var validatedAssets = getValidatedShowtimePickerAssets(assets);
    return __assign({}, defaultShowtimePickerAssets, validatedAssets);
}
/**
 * Validates the asset values provided for the Showtime Picker.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedShowtimePickerAssets(assets) {
    var validateField = fieldValidator(assets);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'noSitesSelectedIconName', 'Showtime picker noSitesSelectedIconName');
    validateField(validator, 'noShowtimesIconName', 'Showtime picker noShowtimesIconName');
    validateField(validator, 'failedToLoadDataIconName', 'Showtime picker failedToLoadDataIconName');
    return assets;
}
//# sourceMappingURL=assets-mapper.js.map