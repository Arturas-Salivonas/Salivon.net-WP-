var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultSiteListDisplayText } from '../types/display-text';
import { fieldValidator } from './../../../../../api/src/utils/field-validator';
/**
 * Maps the display text provided via the API to the display text required by the Site List widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapSiteListDisplayText(displayText) {
    if (!displayText)
        return defaultSiteListDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Site List display text');
    if (!valid)
        return defaultSiteListDisplayText;
    var validatedDisplayText = getValidatedSiteListDisplayText(displayText);
    return __assign({}, defaultSiteListDisplayText, validatedDisplayText);
}
/**
 * Validates the display text values provided for the Site List.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedSiteListDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'noSitesFoundForSearchTermMessage', 'Site List noSitesFoundForSearchTermMessage');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map