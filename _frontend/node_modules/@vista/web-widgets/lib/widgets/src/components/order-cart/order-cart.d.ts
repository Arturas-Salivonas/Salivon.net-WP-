import * as React from 'react';
import { OrderStore } from '../../../../api/src/stores/order/order-store';
import { SalesDataStore } from '../../../../api/src/stores/sales-data/sales-data-store';
import { OrderCartAssets } from './types/assets';
import { OrderCartConfig } from './types/config';
import { OrderCartDisplayText } from './types/display-text';
export interface OrderCartProps {
    orderStore: OrderStore;
    salesDataStore: SalesDataStore;
    displayText: OrderCartDisplayText;
    config: OrderCartConfig;
    assets: OrderCartAssets;
    onActionButtonClicked?(): Promise<void>;
}
export interface OrderCartState {
    isActionLoading: boolean;
}
/**
 * Renders a shopping cart widget that displays the current in-progress order.
 */
export declare class OrderCart extends React.Component<OrderCartProps, OrderCartState> {
    constructor(props: OrderCartProps);
    render(): JSX.Element;
    private handleRemoveItem;
    private handleActionButtonClicked;
}
