import * as React from 'react';
import { OrderStore } from '../../../../../api/src/stores/order/order-store';
import { TicketPickerWidgetDisplayText } from '../types/display-text';
export interface TicketPickerTableFooterProps {
    orderStore: OrderStore;
    showtimeId: string;
    displayText: TicketPickerWidgetDisplayText;
}
/**
 * Renders the footer element of the TicketPickerTable.
 * Includes the calculated booking fee and total cost of the ticket selections.
 */
export declare class TicketPickerTableFooter extends React.Component<TicketPickerTableFooterProps> {
    render(): JSX.Element;
}
