var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { exampleSites } from '../../../../../../example-data/browsing/site';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { SitePickerUnselectedSiteList } from '../site-picker-unselected-site-list';
describe('SitePickerUnselectedSiteList', function () {
    it('should render correctly', function () {
        var testProps = {
            sites: exampleSites,
            disabled: false,
            onSiteClicked: function () { return; }
        };
        compareToSnapshot(React.createElement(SitePickerUnselectedSiteList, __assign({}, testProps)));
    });
    it('should render disabled correctly', function () {
        var testProps = {
            sites: exampleSites,
            disabled: true,
            onSiteClicked: function () { return; }
        };
        compareToSnapshot(React.createElement(SitePickerUnselectedSiteList, __assign({}, testProps)));
    });
});
//# sourceMappingURL=site-picker-unselected-site-list.test.js.map