import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { TicketPickerTableFooter } from '../ticket-picker-table-footer';
describe('TicketPickerTableFooter', function () {
    it('should render correctly', function () {
        var orderStore = {
            data: { order: { bookingFee: 100 } },
            selectors: {
                getTicketsTotalCostForShowtime: jest.fn().mockReturnValue(200)
            }
        };
        var displayText = {
            tableFooterRowBookingFeeLabel: 'Booking Fee',
            tableFooterRowOrderTotalLabel: 'Total Cost'
        };
        compareToSnapshot(React.createElement(TicketPickerTableFooter, { orderStore: orderStore, showtimeId: "1234", displayText: displayText }));
    });
});
//# sourceMappingURL=ticket-picker-table-footer.test.js.map