var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultShowtimePickerDisplayText } from '../types/display-text';
/**
 * Maps the display text provided via the API to the display text required by the Showtime Picker widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapShowtimePickerDisplayText(displayText) {
    if (!displayText)
        return defaultShowtimePickerDisplayText;
    var valid = new Validator().ofType('object').validate(displayText, 'Showtime picker display text');
    if (!valid)
        return defaultShowtimePickerDisplayText;
    var validatedDisplayText = getValidatedShowtimePickerDisplayText(displayText);
    return __assign({}, defaultShowtimePickerDisplayText, validatedDisplayText);
}
/**
 * Validates the display text values provided for the Showtime Picker.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedShowtimePickerDisplayText(displayText) {
    var validateField = fieldValidator(displayText);
    var validator = new Validator().ofType('string').minLength(1);
    var allowEmptyValidator = new Validator().ofType('string').minLength(0);
    validateField(validator, 'showtimeTimeFormat', 'Showtime picker showtimeTimeFormat');
    validateField(allowEmptyValidator, 'showtimeAmPmFormat', 'Showtime picker showtimeAmPmFormat');
    validateField(validator, 'commonAttributesListLabel', 'Showtime picker commonAttributesListLabel');
    validateField(validator, 'runtimeFormat', 'Showtime picker runtimeFormat');
    validateField(validator, 'noSitesSelectedTitle', 'Showtime picker noSitesSelectedTitle');
    validateField(validator, 'noSitesSelectedMessage', 'Showtime picker noSitesSelectedMessage');
    validateField(validator, 'noShowtimesForSiteOnDateMessage', 'Showtime picker noShowtimesForSiteOnDateMessage');
    validateField(validator, 'noShowtimesForFilmMessage', 'Showtime picker noShowtimesForFilmMessage');
    validateField(validator, 'noShowtimesGenericMessage', 'Showtime picker noShowtimesGenericMessage');
    validateField(validator, 'actionButtonLabel', 'Showtime picker actionButtonLabel');
    validateField(validator, 'noShowtimesForAnyDateTitle', 'Showtime picker noShowtimesForAnyDateTitle');
    validateField(validator, 'noShowtimesForAnyDateMessage', 'Showtime picker noShowtimesForAnyDateMessage');
    validateField(validator, 'failedToLoadDataTitle', 'Showtime picker failedToLoadDataTitle');
    validateField(validator, 'failedToLoadDataMessage', 'Showtime picker failedToLoadDataMessage');
    return displayText;
}
//# sourceMappingURL=display-text-mapper.js.map