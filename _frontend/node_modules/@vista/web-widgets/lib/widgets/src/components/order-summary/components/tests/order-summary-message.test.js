var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { compareToSnapshot } from '../../../../tests/compare-to-snapshot';
import { defaultOrderSummaryAssets } from '../../types/assets';
import { defaultOrderSummaryDisplayText } from '../../types/display-text';
import { OrderSummaryMessage, OrderSummaryMessageType } from '../order-summary-message';
describe('OrderSummaryMessage', function () {
    it('should render a FailedToLoadData message correctly', function () {
        var testProps = {
            messageType: OrderSummaryMessageType.FailedToLoadData,
            displayText: defaultOrderSummaryDisplayText,
            assets: defaultOrderSummaryAssets
        };
        compareToSnapshot(React.createElement(OrderSummaryMessage, __assign({}, testProps)));
    });
});
//# sourceMappingURL=order-summary-message.test.js.map