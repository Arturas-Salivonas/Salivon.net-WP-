import { Validator } from '../../../../../api/src/utils/validator';
var isValidPosition = function (position) {
    var propertyValidator = new Validator().ofType('number');
    return position != null
        && propertyValidator.validate(position.areaNumber, "Seat position areaNumber (" + position.areaNumber + ")")
        && propertyValidator.validate(position.rowIndex, "Seat position rowIndex (" + position.rowIndex + ")")
        && propertyValidator.validate(position.columnIndex, "Seat position columnIndex (" + position.columnIndex + ")");
};
export var getValidatedFriendSeats = function (friendSeats) { return friendSeats.filter(isValidPosition); };
//# sourceMappingURL=get-validated-friends-seats.js.map