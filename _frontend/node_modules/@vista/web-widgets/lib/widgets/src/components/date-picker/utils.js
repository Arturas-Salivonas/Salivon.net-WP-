import { eachDay, max, min } from 'date-fns';
import { getBusinessDateFromDate } from '../../utils/business-date-utils';
export var getDatePickerDateValues = function (businessDates, selectedBusinessDate) {
    var minDate = min.apply(void 0, businessDates.concat([new Date(Date.now())])); // Uses Date.now() so the date can be mocked in unit tests.
    var maxDate = max.apply(void 0, businessDates);
    var allDates = eachDay(minDate, maxDate);
    return allDates.map(function (date) {
        var businessDate = getBusinessDateFromDate(date);
        return {
            businessDate: businessDate,
            isActive: businessDates.includes(businessDate),
            isSelected: businessDate === selectedBusinessDate
        };
    });
};
//# sourceMappingURL=utils.js.map