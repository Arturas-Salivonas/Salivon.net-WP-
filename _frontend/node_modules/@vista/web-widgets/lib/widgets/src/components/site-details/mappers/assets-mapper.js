var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { fieldValidator } from '../../../../../api/src/utils/field-validator';
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultSiteDetailsAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Site Details widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapSiteDetailsAssets(assets) {
    if (!assets)
        return defaultSiteDetailsAssets;
    var valid = new Validator().ofType('object').validate(assets, 'Site Details assets');
    if (!valid)
        return defaultSiteDetailsAssets;
    var validatedAssets = getValidatedSiteDetailsAssets(assets);
    return __assign({}, defaultSiteDetailsAssets, validatedAssets);
}
/**
 * Validates the asset values provided for the Site Details.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedSiteDetailsAssets(assets) {
    var validateField = fieldValidator(assets);
    var validator = new Validator().ofType('string').minLength(1);
    validateField(validator, 'failedToLoadDataIconName', 'Site Details failedToLoadDataIconName');
    return assets;
}
//# sourceMappingURL=assets-mapper.js.map