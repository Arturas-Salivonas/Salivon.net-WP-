/**
 * Events for the Order Cart widget.
 */
export interface OrderCartEvents {
    /**
     * Callback function that is called when the action button is clicked.
     */
    onActionButtonClicked?(): Promise<void>;
}
