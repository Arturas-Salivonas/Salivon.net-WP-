/**
 * Returns the current window scroll position.
 */
export var getWindowScrollPosition = function () {
    return {
        x: window.pageXOffset || document.documentElement.scrollLeft,
        y: window.pageYOffset || document.documentElement.scrollTop
    };
};
/**
 * Returns a window point relative to an HTMLElement.
 */
export var getRelativePoint = function (windowPoint, element) {
    var windowScrollPosition = getWindowScrollPosition();
    var elementRect = element.getBoundingClientRect();
    return {
        x: windowPoint.x + windowScrollPosition.x - elementRect.left,
        y: windowPoint.y - windowScrollPosition.y - elementRect.top
    };
};
//# sourceMappingURL=window-utils.js.map