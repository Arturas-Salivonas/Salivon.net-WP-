import { loyaltyStoreInstance } from '../../../../api/src/stores/loyalty/loyalty-store';
import { addWidgetBootstrap } from '../../bootstrap/add-widget-bootstrap';
import { createComponent } from '../../bootstrap/create-component';
import { mapMemberDetailsDisplayText } from './mappers/display-text-mapper';
import { MemberDetails } from './member-details';
export var memberDetails = function (bootstrap) {
    if (!loyaltyStoreInstance.selectors.isSignedIn)
        throw new Error('Cannot render the Member Details widget without a signed-in Loyalty member');
    var selector = bootstrap.selector, displayText = bootstrap.displayText;
    var props = {
        loyaltyStore: loyaltyStoreInstance,
        displayText: mapMemberDetailsDisplayText(displayText),
    };
    return createComponent(selector, MemberDetails, props);
};
addWidgetBootstrap('memberDetails', memberDetails);
//# sourceMappingURL=bootstrap.js.map