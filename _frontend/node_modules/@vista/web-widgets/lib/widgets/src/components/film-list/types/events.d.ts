import { Film } from './../../../../../types/browsing/film';
/**
 * Events for the Film List widget.
 */
export interface FilmListEvents {
    /**
     * Callback function that is called when a film is clicked.
     */
    onFilmClicked: (film: Film) => void;
}
