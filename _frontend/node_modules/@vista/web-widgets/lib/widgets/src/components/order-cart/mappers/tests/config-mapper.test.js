import { defaultOrderCartConfig } from '../../types/config';
import { mapOrderCartConfig } from '../config-mapper';
describe('Order Cart Config Mapper', function () {
    it('returns the default config when the provided config is undefined', function () {
        var result = mapOrderCartConfig(undefined);
        expect(result).toEqual(defaultOrderCartConfig);
    });
    it('returns the default config when the provided config is invalid', function () {
        var result = mapOrderCartConfig(true);
        expect(result).toEqual(defaultOrderCartConfig);
    });
});
//# sourceMappingURL=config-mapper.test.js.map