var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Validator } from '../../../../../api/src/utils/validator';
import { defaultOrderPaymentFormAssets } from '../types/assets';
/**
 * Maps the assets provided via the API to the assets required by the Order Payment Form widget.
 * Nulls out any invalid values and logs appropriate warnings.
 * Any undefined values will be replaced during mapping by the defaults.
 */
export function mapOrderPaymentFormAssets(assets) {
    if (!assets)
        return defaultOrderPaymentFormAssets;
    var valid = new Validator().ofType('object').validate(assets, 'Order Payment Form assets');
    if (!valid)
        return defaultOrderPaymentFormAssets;
    var validatedAssets = getValidatedOrderPaymentFormAssets(assets);
    return __assign({}, defaultOrderPaymentFormAssets, validatedAssets);
}
/**
 * Validates the asset values provided for the Order Payment Form.
 * Removes any invalid values and logs appropriate warnings.
 */
export function getValidatedOrderPaymentFormAssets(assets) {
    // const validateField = fieldValidator(assets);
    // const validator = new Validator().ofType('string').minLength(1);
    // validateField(validator, 'x', 'Order Payment Form x');
    return assets;
}
//# sourceMappingURL=assets-mapper.js.map