import domready from 'domready';
import { deviceHasTouch } from './device-has-touch';
/**
 * Sets the 'data-input-method' attribute to the DOM with a value of 'cursor' or 'touch'.
 * Todo: Maybe delete this function depending on whether the new hover approach is suitable or not
 */
export function setInputMethodAttribute() {
    if (document.documentElement) {
        var inputMethod = deviceHasTouch() ? 'touch' : 'cursor';
        document.documentElement.setAttribute('data-input-method', inputMethod);
    }
    else {
        domready(setInputMethodAttribute);
    }
}
//# sourceMappingURL=set-input-method-attribute.js.map