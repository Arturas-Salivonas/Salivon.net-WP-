/**
 * Returns the positive numeric value of an input element.
 * If the input value is non-numeric undefined is
 * returned.
 *
 * @export
 * @param {HTMLInputElement} input
 * @returns {(number | undefined)}
 */
export function numericInputValue(input) {
    var inputValue = input.value;
    if (!/^\d+$/.test(inputValue)) {
        return undefined;
    }
    return parseInt(inputValue, 10);
}
//# sourceMappingURL=numeric-input-value.js.map