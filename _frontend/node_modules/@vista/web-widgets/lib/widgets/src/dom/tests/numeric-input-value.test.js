import { numericInputValue } from '../numeric-input-value';
describe('numericInputValue', function () {
    it('returns undefined when the input is no a positive integer value', function () {
        var testCases = [
            '',
            ' ',
            '.',
            'x',
            '1.',
            '1,',
            '1,0',
            '1.0',
            '-1'
        ];
        var inputEl = document.createElement('input');
        testCases.forEach(function (value) {
            inputEl.value = value;
            expect(numericInputValue(inputEl)).toBeUndefined();
        });
    });
    it('returns the number when the input is not an integer value', function () {
        var inputEl = document.createElement('input');
        inputEl.value = '1';
        expect(numericInputValue(inputEl)).toBe(1);
    });
});
//# sourceMappingURL=numeric-input-value.test.js.map