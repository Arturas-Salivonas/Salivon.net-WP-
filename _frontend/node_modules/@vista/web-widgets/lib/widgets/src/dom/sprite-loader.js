import domready from 'domready';
import BrowserSprite from 'svg-baker-runtime/src/browser-sprite';
/**
 * This file is used by the svg-sprite-loader Webpack loader to mount the SVG sprite to the DOM.
 */
var spriteNodeId = 'v-icons';
var spriteGlobalVarName = 'vista-icons';
var sprite;
if (!window[spriteGlobalVarName]) {
    var attrs = {
        id: spriteNodeId,
        style: 'display:none;',
        width: 0,
        height: 0,
    };
    sprite = new BrowserSprite({ attrs: attrs });
    window[spriteGlobalVarName] = sprite;
}
else {
    sprite = window[spriteGlobalVarName];
}
var loadSprite = function () {
    /**
     * Check for page already contains sprite node
     * If found - attach to and reuse it's content
     * If not - render and mount the new sprite
     */
    var existing = document.getElementById(spriteNodeId);
    if (existing) {
        sprite.attach(existing);
    }
    else {
        sprite.mount(document.body, true);
    }
};
if (document.body) {
    loadSprite();
}
else {
    domready(loadSprite);
}
// Must have a default export, as required by the svg-sprite-loader package.
// tslint:disable-next-line:no-default-export
export default sprite;
//# sourceMappingURL=sprite-loader.js.map