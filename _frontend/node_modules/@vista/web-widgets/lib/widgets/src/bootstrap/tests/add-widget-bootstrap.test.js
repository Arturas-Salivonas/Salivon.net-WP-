import { addWidgetBootstrap } from '../add-widget-bootstrap';
describe('addWidgetBootstrap', function () {
    var bootstrapFunction = function () { return undefined; };
    describe('can add a widget bootstrap', function () {
        it('can add a widget bootstrap to existing widgets object', function () {
            window.vista = { widgets: {} };
            addWidgetBootstrap('ticketPicker', bootstrapFunction);
            expect(window.vista.widgets.ticketPicker).toBe(bootstrapFunction);
        });
        it('can add widget bootstrap with no vista object', function () {
            window.vista = undefined;
            addWidgetBootstrap('ticketPicker', bootstrapFunction);
            expect(window.vista.widgets.ticketPicker).toBe(bootstrapFunction);
        });
        it('can add widget bootstrap with widgets object', function () {
            window.vista = {};
            addWidgetBootstrap('ticketPicker', bootstrapFunction);
            expect(window.vista.widgets.ticketPicker).toBe(bootstrapFunction);
        });
    });
});
//# sourceMappingURL=add-widget-bootstrap.test.js.map