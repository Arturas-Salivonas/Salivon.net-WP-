import { create } from 'react-test-renderer';
export function compareToSnapshot(nextElement, options) {
    var tree = create(nextElement, options);
    expect(tree).toMatchSnapshot();
}
//# sourceMappingURL=compare-to-snapshot.js.map