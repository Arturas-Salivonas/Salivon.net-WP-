import { performPostRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/**
 * @hidden
 */
export function createOrder(cinemaChainId, createOrderRequest) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId argument is required to create an order');
    if (!createOrderRequest)
        return Promise.reject('createOrderRequest argument is required to cancel an order');
    return performPostRequest(urls.ticketingUrl + "/" + cinemaChainId + "/orders", createOrderRequest);
}
//# sourceMappingURL=create-order.js.map