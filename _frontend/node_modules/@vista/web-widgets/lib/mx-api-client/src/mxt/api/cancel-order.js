import { performDeleteRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/**
 * @hidden
 */
export function cancelOrder(cinemaChainId, orderId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId argument is required to cancel an order');
    if (!orderId)
        return Promise.reject('orderId argument is required to cancel an order');
    return performDeleteRequest(urls.ticketingUrl + "/" + cinemaChainId + "/orders/" + orderId);
}
//# sourceMappingURL=cancel-order.js.map