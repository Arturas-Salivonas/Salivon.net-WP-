import { performPutRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/*
* @hidden
*/
export var setOrderItems = function (cinemaChainId, orderId, request) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId argument is required to set order items');
    if (!orderId)
        return Promise.reject('orderId argument is required to set order items');
    if (!request)
        return Promise.reject('request argument is required to set order items');
    return performPutRequest(urls.ticketingUrl + "/" + cinemaChainId + "/orders/" + orderId + "/items", request);
};
//# sourceMappingURL=set-order-items.js.map