import { performGetRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/**
 * @hidden
 */
export var getJourneyById = function (cinemaChainId, journeyId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to get a journey by id');
    if (!journeyId)
        return Promise.reject('journeyId is required to get a journey by id');
    return performGetRequest(urls.ticketingUrl + "/" + cinemaChainId + "/journeys/" + journeyId);
};
//# sourceMappingURL=get-journey-by-id.js.map