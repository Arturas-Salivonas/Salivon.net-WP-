import { performGetRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
export var getOrderCompletionStatus = function (cinemaChainId, orderId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId argument is required to start embedded payment');
    if (!orderId)
        return Promise.reject('orderId argument is required to get order completion status');
    return performGetRequest(urls.ticketingUrl + "/" + cinemaChainId + "/orders/" + orderId + "/completion-status");
};
//# sourceMappingURL=get-order-completion-status.js.map