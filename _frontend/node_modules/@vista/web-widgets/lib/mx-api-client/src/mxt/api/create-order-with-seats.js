import { performPostRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/**
 * @hidden
 */
export function createOrderWithSeats(cinemaChainId, showtimeId, createOrderWithSeatsRequest) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId argument is required to create an order');
    if (!createOrderWithSeatsRequest)
        return Promise.reject('createOrderRequest argument is required to cancel an order');
    return performPostRequest(urls.ticketingUrl + "/" + cinemaChainId + "/orders/showtimes/" + showtimeId + "/seats", createOrderWithSeatsRequest);
}
//# sourceMappingURL=create-order-with-seats.js.map