import { performPostRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/*
* @hidden
*/
export function setCustomerDetails(cinemaChainId, orderId, customerDetails) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId argument is required to set customer details');
    if (!orderId)
        return Promise.reject('orderId argument is required to set customer details');
    if (!customerDetails)
        return Promise.reject('createOrderRequest argument is required to set customer details');
    return performPostRequest(urls.ticketingUrl + "/" + cinemaChainId + "/orders/" + orderId + "/customer", customerDetails);
}
//# sourceMappingURL=set-customer-details.js.map