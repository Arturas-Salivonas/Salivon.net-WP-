import { performPutRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/**
 * @hidden
 */
export function updateShowtimeTicketingDetails(cinemaChainId, orderId, showtimeId, showtimeTicketingDetails) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId argument is required to set seats in the order');
    if (!showtimeId)
        return Promise.reject('showtimeId argument is required to set seats in the order');
    if (!orderId)
        return Promise.reject('orderId argument is required to set seats in the order');
    if (!showtimeTicketingDetails)
        return Promise.reject('showtimeTicketingDetails argument is required to set seats in the order');
    return performPutRequest(urls.ticketingUrl + "/" + cinemaChainId + "/orders/" + orderId + "/showtimes/" + showtimeId, showtimeTicketingDetails);
}
//# sourceMappingURL=update-showtime-ticketing-details.js.map