import { performGetRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
export function getSeatLayoutForOrder(cinemaChainId, orderId, showtimeId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to get seat layout for a showtime');
    if (!orderId)
        return Promise.reject('orderId is required to get seat layout for a showtime');
    if (!showtimeId)
        return Promise.reject('showtimeId is required to get seat layout for a showtime');
    return performGetRequest(urls.seatingUrl + "/" + cinemaChainId + "/orders/" + orderId + "/showtimes/" + showtimeId + "/seats");
}
//# sourceMappingURL=get-seat-layout-for-order.js.map