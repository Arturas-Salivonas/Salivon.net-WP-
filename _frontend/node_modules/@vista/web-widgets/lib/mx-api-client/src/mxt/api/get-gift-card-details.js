import { performGetRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/**
 * @hidden
 */
export function getGiftCardDetails(cinemaChainId, giftCardNumber, captchaResponse) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to get the details of a gift card');
    if (!giftCardNumber)
        return Promise.reject('cardNumber is required to get the details of a gift card');
    var queryString = captchaResponse ? "?captchaResponse=" + captchaResponse : '';
    return performGetRequest(urls.ticketingUrl + "/" + cinemaChainId + "/gift-cards/" + giftCardNumber + queryString);
}
//# sourceMappingURL=get-gift-card-details.js.map