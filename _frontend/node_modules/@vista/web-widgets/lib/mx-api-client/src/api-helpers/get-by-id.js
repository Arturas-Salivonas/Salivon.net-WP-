import { performGetRequest } from './perform-request';
/**
 * @hidden
 */
export var getById = function (id, url) {
    if (!url)
        return Promise.reject('Missing required param "url"');
    if (!id)
        return Promise.reject('Missing required param "id"');
    return performGetRequest(url + "/" + id);
};
//# sourceMappingURL=get-by-id.js.map