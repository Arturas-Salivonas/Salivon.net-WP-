import { performGetRequest } from '../../api-helpers/perform-request';
import { urls } from '../../api-helpers/urls';
/**
 * @hidden
 */
export function getLoyaltyMemberTransactions(cinemaChainId, memberId, skip, top) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId argument is required to get loyalty member details');
    if (!memberId)
        return Promise.reject('memberId argument is required to get loyalty member details');
    if (skip === undefined)
        return Promise.reject('skip argument is required to get loyalty member details');
    if (!top)
        return Promise.reject('top argument is required to get loyalty member details');
    return performGetRequest(urls.loyaltyUrl + "/" + cinemaChainId + "/members/" + memberId + "/transactions?top=" + top + "&skip=" + skip);
}
//# sourceMappingURL=get-member-transactions.js.map