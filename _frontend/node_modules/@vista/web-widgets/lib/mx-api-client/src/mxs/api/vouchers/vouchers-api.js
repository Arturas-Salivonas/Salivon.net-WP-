import { performPostRequest } from '../../../api-helpers/perform-request';
import { urls } from '../../../api-helpers/urls';
/**
 * @hidden
 */
export var vouchersApi = {
    validateVoucher: function (siteId, cinemaChainId, voucherBarcode, voucherPin) {
        if (!cinemaChainId)
            return Promise.reject('cinemaChainId is required to fetch a voucher');
        if (!voucherBarcode)
            return Promise.reject('voucherBarcode is required to fetch a voucher');
        var body = { barcode: voucherBarcode, pin: voucherPin };
        return performPostRequest(urls.referenceDataUrl + "/" + cinemaChainId + "/showtimes/" + siteId + "/vouchers/validate", body);
    }
};
//# sourceMappingURL=vouchers-api.js.map