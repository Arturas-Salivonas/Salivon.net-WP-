import { performGetRequest } from '../../../api-helpers/perform-request';
import { urls } from '../../../api-helpers/urls';
export var getTicketTypesForShowtime = function (cinemaChainId, showtimeId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to fetch ticket types for showtime');
    if (!showtimeId)
        return Promise.reject('showtimeId is required to fetch ticket types for showtime');
    return performGetRequest(urls.referenceDataUrl + "/" + cinemaChainId + "/showtimes/" + showtimeId + "/ticket-types");
};
//# sourceMappingURL=get-ticket-types-for-showtime.js.map