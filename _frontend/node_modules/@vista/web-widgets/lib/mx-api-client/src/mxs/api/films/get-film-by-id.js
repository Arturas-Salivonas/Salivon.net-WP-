import { performGetRequest } from '../../../api-helpers/perform-request';
import { urls } from '../../../api-helpers/urls';
export var getFilmById = function (cinemaChainId, filmId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to fetch film by id');
    if (!filmId)
        return Promise.reject('filmId is required to fetch film by id');
    return performGetRequest(urls.referenceDataUrl + "/" + cinemaChainId + "/films/" + filmId);
};
//# sourceMappingURL=get-film-by-id.js.map