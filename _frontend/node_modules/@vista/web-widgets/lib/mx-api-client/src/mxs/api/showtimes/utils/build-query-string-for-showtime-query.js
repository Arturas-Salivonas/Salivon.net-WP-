export function buildQueryStringForShowtimeQuery(showtimeQuery) {
    var queryParams = [];
    showtimeQuery.siteIds.forEach(function (siteId) { queryParams.push("siteIds=" + siteId); });
    showtimeQuery.filmIds && showtimeQuery.filmIds.forEach(function (filmId) { queryParams.push("filmIds=" + filmId); });
    return queryParams.length ? "?" + queryParams.join('&') : '';
}
//# sourceMappingURL=build-query-string-for-showtime-query.js.map