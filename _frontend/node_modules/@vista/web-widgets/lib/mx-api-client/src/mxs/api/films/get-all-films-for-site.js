import { performGetRequest } from '../../../api-helpers/perform-request';
import { urls } from '../../../api-helpers/urls';
export var getAllFilmsForSite = function (cinemaChainId, siteId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to fetch all films for site');
    if (!siteId)
        return Promise.reject('siteId is required to fetch all films for site');
    return performGetRequest(urls.referenceDataUrl + "/" + cinemaChainId + "/sites/" + siteId + "/films");
};
//# sourceMappingURL=get-all-films-for-site.js.map