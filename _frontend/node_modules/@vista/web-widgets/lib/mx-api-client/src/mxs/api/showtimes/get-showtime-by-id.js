import { performGetRequest } from '../../../api-helpers/perform-request';
import { urls } from '../../../api-helpers/urls';
export var getShowtimeById = function (cinemaChainId, showtimeId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to fetch showtime by id');
    if (!showtimeId)
        return Promise.reject('showtimeId is required to fetch showtime by id');
    var url = urls.referenceDataUrl + "/" + cinemaChainId + "/showtimes/" + showtimeId;
    return performGetRequest(url);
};
//# sourceMappingURL=get-showtime-by-id.js.map