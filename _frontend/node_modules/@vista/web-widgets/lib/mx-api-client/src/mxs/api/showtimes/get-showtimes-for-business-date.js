import { performGetRequest } from '../../../api-helpers/perform-request';
import { urls } from '../../../api-helpers/urls';
import { buildQueryStringForShowtimeQuery } from './utils/build-query-string-for-showtime-query';
export var getShowtimesForBusinessDate = function (cinemaChainId, businessDate, showtimeQuery) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to fetch showtimes for a business date');
    var queryString = buildQueryStringForShowtimeQuery(showtimeQuery);
    var url = urls.referenceDataUrl + "/" + cinemaChainId + "/showtimes/business-date/" + businessDate;
    if (queryString.length)
        url += queryString;
    return performGetRequest(url);
};
//# sourceMappingURL=get-showtimes-for-business-date.js.map