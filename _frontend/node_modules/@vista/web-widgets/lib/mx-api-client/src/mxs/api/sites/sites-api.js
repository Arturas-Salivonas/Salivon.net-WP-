import { getAll } from '../../../api-helpers/get-all';
import { getById } from '../../../api-helpers/get-by-id';
import { urls } from '../../../api-helpers/urls';
/**
 * @hidden
 */
export var sitesApi = {
    getById: function (cinemaChainId, siteId) {
        if (!cinemaChainId)
            return Promise.reject(new Error('cinemaChainId is required to fetch a site by id'));
        if (!siteId)
            return Promise.reject(new Error('siteId is required to fetch a site by id'));
        return getById(siteId, urls.referenceDataUrl + "/" + cinemaChainId + "/sites");
    },
    getAll: function (cinemaChainId) {
        if (!cinemaChainId)
            return Promise.reject(new Error('cinemaChainId is required to fetch sites for a cinema chain'));
        return getAll(urls.referenceDataUrl + "/" + cinemaChainId + "/sites");
    },
};
//# sourceMappingURL=sites-api.js.map