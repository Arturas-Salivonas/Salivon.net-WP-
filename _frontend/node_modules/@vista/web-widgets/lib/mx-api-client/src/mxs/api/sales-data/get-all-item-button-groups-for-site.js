import { performGetRequest } from '../../../api-helpers/perform-request';
import { urls } from '../../../api-helpers/urls';
export var getAllItemButtonGroupsForSite = function (cinemaChainId, siteId) {
    if (!cinemaChainId)
        return Promise.reject('cinemaChainId is required to fetch all item button groups for site');
    if (!siteId)
        return Promise.reject('siteId is required to fetch all item button groups for site');
    return performGetRequest(urls.referenceDataUrl + "/" + cinemaChainId + "/sites/" + siteId + "/item-button-groups");
};
//# sourceMappingURL=get-all-item-button-groups-for-site.js.map