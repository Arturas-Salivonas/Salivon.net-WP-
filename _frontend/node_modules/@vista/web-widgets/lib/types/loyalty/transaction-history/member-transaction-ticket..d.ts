/**
 * A customer's Member Transaction Ticket
 * @interface MemberTransactionTicket
 */
export interface MemberTransactionTicket {
    transactionLineNumber: number;
    ticketTypeId: string;
    price: number;
    quantity: number;
}
