import { Translation } from '../../translation';
/**
 * A customer's Loyalty Ticket Type
 * @interface Translatable
 */
export interface Translatable {
    text: string;
    translations?: Translation[];
}
