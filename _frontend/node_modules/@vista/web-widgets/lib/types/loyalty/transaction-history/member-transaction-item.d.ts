/**
 * A customer's Member Transaction Item
 * @interface MemberTransactionItem
 */
export interface MemberTransactionItem {
    transactionLineNumber: number;
    itemId: string;
    price: number;
    quantity?: number;
}
