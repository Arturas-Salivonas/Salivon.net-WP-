import { Translation } from '../translation';
export interface PackageTicketTypePackageContents {
    items: ChildContent[];
    tickets: ChildContent[];
}
export interface ChildContent {
    description: string;
    descriptionTranslations: Translation[];
    quantity: number;
}
