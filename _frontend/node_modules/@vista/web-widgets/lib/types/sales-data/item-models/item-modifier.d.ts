import { ModifierItem } from './item-model';
/**
 * An item that modifies another item.
 */
export interface ItemModifier {
    /**
     * The sequence in which the modifier item should be displayed within the modifier group.
     */
    displaySequence: number;
    /**
     * The item associated with the modifier.
     */
    item: ModifierItem;
}
