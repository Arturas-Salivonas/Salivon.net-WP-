import { ItemModel } from './item-model';
/**
 * An item that can be selected for a parent sales item.
 */
export interface ItemOption {
    /**
     * The sequence in which the option should be displayed among the parent's options.
     */
    displaySequence: number;
    /**
     * The item associated with the option.
     */
    item: ItemModel;
}
