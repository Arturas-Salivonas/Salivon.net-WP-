import { Translation } from '../translation';
import { SiteAddress } from './site-address';
import { SiteContactDetails } from './site-contact-details';
import { SiteLocation } from './site-location';
export interface Site {
    id: string;
    name: string;
    nameTranslations: Translation[];
    address?: SiteAddress;
    location?: SiteLocation;
    contactDetails: SiteContactDetails;
}
