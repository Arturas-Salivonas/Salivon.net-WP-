import { Translation } from '../translation';
export interface CensorRating {
    id: string;
    classification: string;
    classificationTranslations: Translation[];
    description: string;
    descriptionTranslations: Translation[];
}
