import { Translation } from '../translation';
import { FilmCategory } from './film-category';
export interface Film {
    id: string;
    title: string;
    titleTranslations: Translation[];
    plot?: string;
    plotTranslations: Translation[];
    classificationNote?: string;
    classificationNoteTranslations: Translation[];
    censorRatingId?: string;
    releaseDate?: Date;
    runtime?: number;
    trailerUrl?: string;
    displayPriority: number;
    actorIds: string[];
    directorIds: string[];
    genreIds: string[];
    categories: FilmCategory[];
}
