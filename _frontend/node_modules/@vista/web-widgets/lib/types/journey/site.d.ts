import { Translation } from '../translation';
import { JourneySiteAddress } from './site-address';
export interface JourneySite {
    id: string;
    name: string;
    nameTranslations: Translation[];
    address?: JourneySiteAddress;
}
