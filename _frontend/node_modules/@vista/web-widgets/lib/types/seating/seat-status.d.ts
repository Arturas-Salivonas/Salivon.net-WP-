/** Seat status codes which indicate either a seat's type. */
export declare enum SeatStatus {
    /** Seat is available for selection. */
    Empty = 0,
    /** Seat has been sold. */
    Sold = 1,
    /** Wheelchair seat. */
    Wheelchair = 3,
    /** Seat selected for this order. */
    Selected = 4,
    /** Broken seat. */
    Broken = 5,
    /** Wheelchair companion seat. */
    Companion = 7
}
