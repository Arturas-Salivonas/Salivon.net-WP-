/** Store a seat's position within a particular seat area. */
export interface SeatPosition {
    /** Identifies which area the seat belongs to. */
    areaNumber: number;
    /** The row within the area the seat belongs to. */
    rowIndex: number;
    /** The column within the area the seat belongs to. */
    columnIndex: number;
}
