import { Site } from '../browsing/site';
import { OrderItem } from './order-item';
import { OrderShowtime } from './order-showtime';
import { OrderVouchers } from './order-vouchers';
/**
 * A customer's order data containing tickets and fees.
 * @interface Order
 */
export interface Order {
    orderId?: string;
    site?: Site;
    hasUnsavedChanges: boolean;
    bookingFee: number;
    showtimes: OrderShowtime[];
    items: OrderItem[];
    cinemaChainId: string;
    vouchers: OrderVouchers;
}
/**
 * Default value for OrderService
 * @type {Order}
 * @hidden
 */
export declare const defaultOrderData: Order;
