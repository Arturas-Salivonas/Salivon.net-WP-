import { TicketTypeKey } from '../sales-data/ticket-type';
/**
 * A ticket type that a voucher can be redeemed for.
 *
 * @export
 * @interface VoucherTicketType
 */
export interface VoucherTicketType {
    ticketTypeId: string;
    ticketTypeKey: TicketTypeKey;
}
