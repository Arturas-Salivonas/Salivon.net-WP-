import { Film } from '../browsing/film';
import { Showtime } from '../browsing/showtime';
import { ShowtimeAttribute } from '../browsing/showtime-attribute';
import { Site } from '../browsing/site';
import { CensorRating } from './../browsing/censor-rating';
import { Genre } from './../browsing/genre';
import { Person } from './../browsing/person';
import { ScreeningDate } from './../browsing/screening-date';
import { ScreeningDatesByQueryKey } from './browsing-store/screening-dates-for-query';
import { ShowtimesByQueryKey } from './browsing-store/showtimes-for-query';
export interface BrowsingStoreData {
    filmsById: Map<string, Film>;
    sitesById: Map<string, Site>;
    showtimeAttributesById: Map<string, ShowtimeAttribute>;
    censorRatingsById: Map<string, CensorRating>;
    genresById: Map<string, Genre>;
    peopleById: Map<string, Person>;
    screeningDates?: ScreeningDate[];
    selectedSiteIds: string[];
    allFilmsLoaded: boolean;
    allSitesLoaded: boolean;
    showtimesByQuery: ShowtimesByQueryKey;
    showtimesById: Map<string, Showtime>;
    screeningDatesByQuery: ScreeningDatesByQueryKey;
    filmsBySiteId: Map<string, Film[]>;
}
export declare const defaultBrowsingStoreData: BrowsingStoreData;
