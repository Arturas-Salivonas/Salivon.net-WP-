import { FetchedData } from '../../../api/src/stores/cache-tools/fetched-data';
/**
 * Showtime Data organised by queryKey, which describes the site and film filters.
 */
export declare type ShowtimesByQueryKey = {
    [queryKey: string]: ShowtimesByBusinessDate | undefined;
};
/**
 * Showtime Data for a request query, organised by business date.
 */
export declare type ShowtimesByBusinessDate = {
    [businessDate: string]: ShowtimesData | undefined;
};
export declare type ShowtimesData = FetchedData<string[]>;
