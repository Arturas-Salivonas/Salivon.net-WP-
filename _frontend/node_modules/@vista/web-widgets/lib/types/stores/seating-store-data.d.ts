import { SeatingError } from '../../api/src/stores/seating/helpers/types';
import { SeatLayout } from '../seating/seat-layout';
import { SeatPosition } from '../seating/seat-position';
export interface SeatingStoreData {
    seatLayout: SeatLayout;
    seatLayoutAutoAllocationStatus: SeatLayoutAutoAllocationStatus;
    friendSeats?: SeatPosition[];
    errors: SeatingError[];
}
export declare type SeatLayoutAutoAllocationStatus = {
    autoAllocated: false;
} | {
    autoAllocated: true;
    seatPositions: SeatPosition[];
};
