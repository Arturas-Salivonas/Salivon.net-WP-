"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var date_fns_1 = require("date-fns");
var types_1 = require("../../types");
/**
 * Formats a given date as a string representation of a date (or date & time)
 * Wrapper for date-fns format (https://date-fns.org/v1.28.0/docs/format)
 *
 * @param date The date to be formatted
 * @param dateFormat The override for default's date format to use (see date-fns documentation for all available options)
 * @param languageTag The override for default's specific IETF locale tag to be used for this transformation
 */
function formatDate(date, dateFormat, languageTag) {
    var locale = languageTag &&
        window.vistaI18N &&
        window.vistaI18N.locales &&
        window.vistaI18N.locales[languageTag];
    return locale
        ? date_fns_1.format(date, dateFormat || types_1.defaultDateConfiguration.default, { locale: locale })
        : date_fns_1.format(date, dateFormat || types_1.defaultDateConfiguration.default);
}
exports.formatDate = formatDate;
//# sourceMappingURL=date.js.map